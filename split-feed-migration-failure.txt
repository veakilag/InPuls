\n===== Apply recovery =====
/home/runner/work/InPuls/InPuls/.github/scripts/apply-split-market-public-feed-v1.py:635: SyntaxWarning: invalid escape sequence '\('
  '''
/home/runner/work/InPuls/InPuls/.github/scripts/apply-split-market-public-feed-v1.py:635: SyntaxWarning: invalid escape sequence '\]'
  '''
/home/runner/work/InPuls/InPuls/.github/scripts/apply-split-market-public-feed-v1.py:678: SyntaxWarning: invalid escape sequence '\.'
  '''
\n===== node --check app.js =====
\n===== node --check binance-stream-routing.js =====
\n===== node --check chart.js =====
\n===== node --check orderbook-worker.js =====
\n===== node --check orderbook.js =====
\n===== node --check orderbook-flow-workspace.js =====
\n===== node --check sw.js =====
\n===== node --test test-global-connection-radar-cleanup-v1.mjs =====
TAP version 13
# Subtest: global feed separates Binance market and public namespaces
ok 1 - global feed separates Binance market and public namespaces
  ---
  duration_ms: 1.532167
  type: 'test'
  ...
# Subtest: fallback transport advances exactly once before required data
ok 2 - fallback transport advances exactly once before required data
  ---
  duration_ms: 0.152475
  type: 'test'
  ...
# Subtest: online requires a real miniTicker batch and subscription errors stay visible
ok 3 - online requires a real miniTicker batch and subscription errors stay visible
  ---
  duration_ms: 0.161785
  type: 'test'
  ...
# Subtest: runtime no longer mixes market and public streams on root endpoints
ok 4 - runtime no longer mixes market and public streams on root endpoints
  ---
  duration_ms: 9.690064
  type: 'test'
  ...
# Subtest: Event Radar Beta assets are removed from runtime and PWA cache
ok 5 - Event Radar Beta assets are removed from runtime and PWA cache
  ---
  duration_ms: 2.316393
  type: 'test'
  ...
1..5
# tests 5
# suites 0
# pass 5
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 77.27673
\n===== node --test test-market-feed-footprint-series-v1.mjs =====
TAP version 13
# Subtest: global market feed goes online only after a valid miniTicker packet
ok 1 - global market feed goes online only after a valid miniTicker packet
  ---
  duration_ms: 2.441777
  type: 'test'
  ...
# Subtest: footprint bootstraps from stable Tape and switches once to guarded flow
ok 2 - footprint bootstraps from stable Tape and switches once to guarded flow
  ---
  duration_ms: 0.682905
  type: 'test'
  ...
# Subtest: Series groups raw aggressive trades for 100 ms and closes on the first opposite trade
ok 3 - Series groups raw aggressive trades for 100 ms and closes on the first opposite trade
  ---
  duration_ms: 0.735768
  type: 'test'
  ...
# Subtest: same-side trades outside the 100 ms window start a new Series
ok 4 - same-side trades outside the 100 ms window start a new Series
  ---
  duration_ms: 0.208593
  type: 'test'
  ...
# Subtest: footprint uses order-book USD formatting and highlights the column maximum
ok 5 - footprint uses order-book USD formatting and highlights the column maximum
  ---
  duration_ms: 0.297066
  type: 'test'
  ...
1..5
# tests 5
# suites 0
# pass 5
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 71.445056
\n===== node --test test-footprint-live-source-v1.mjs =====
TAP version 13
# Subtest: footprint buckets live trades by browser arrival time
ok 1 - footprint buckets live trades by browser arrival time
  ---
  duration_ms: 0.80442
  type: 'test'
  ...
# Subtest: footprint bootstraps from stable Tape and locks to guarded flow after promotion
ok 2 - footprint bootstraps from stable Tape and locks to guarded flow after promotion
  ---
  duration_ms: 0.790623
  type: 'test'
  ...
# Subtest: promotion replaces stable bootstrap volume instead of duplicating it
ok 3 - promotion replaces stable bootstrap volume instead of duplicating it
  ---
  duration_ms: 0.726031
  type: 'test'
  ...
# Subtest: empty guarded packets never fall back or duplicate accumulated volume
ok 4 - empty guarded packets never fall back or duplicate accumulated volume
  ---
  duration_ms: 0.197514
  type: 'test'
  ...
# Subtest: current footprint interval accumulates repeated live packets
ok 5 - current footprint interval accumulates repeated live packets
  ---
  duration_ms: 0.279728
  type: 'test'
  ...
1..5
# tests 5
# suites 0
# pass 5
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 58.394739
\n===== node --test test-parallel-chart-phasing-v1.mjs =====
TAP version 13
# Subtest: parallel chart phases spread canvas work across the interval
ok 1 - parallel chart phases spread canvas work across the interval
  ---
  duration_ms: 0.744343
  type: 'test'
  ...
# Subtest: extra charts use phased updates while the primary chart remains unrestricted
ok 2 - extra charts use phased updates while the primary chart remains unrestricted
  ---
  duration_ms: 1.556258
  type: 'test'
  ...
1..2
# tests 2
# suites 0
# pass 2
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 58.680841
\n===== node --test test-orderbook-runtime-stability.mjs =====
TAP version 13
# Subtest: normal reload keeps one consistent runtime build
ok 1 - normal reload keeps one consistent runtime build
  ---
  duration_ms: 0.972386
  type: 'test'
  ...
# Subtest: worker-unavailable fallback does not leave a health interval open
ok 2 - worker-unavailable fallback does not leave a health interval open
  ---
  duration_ms: 0.253317
  type: 'test'
  ...
# Subtest: hidden tabs close sockets instead of accumulating a stale queue
ok 3 - hidden tabs close sockets instead of accumulating a stale queue
  ---
  duration_ms: 0.264204
  type: 'test'
  ...
# Subtest: workspace order is footprint then Tape then book
ok 4 - workspace order is footprint then Tape then book
  ---
  duration_ms: 0.231579
  type: 'test'
  ...
# Subtest: Tape and footprint visibility controls stay independent
ok 5 - Tape and footprint visibility controls stay independent
  ---
  duration_ms: 0.337609
  type: 'test'
  ...
# Subtest: RX uses calibrated Binance server time
ok 6 - RX uses calibrated Binance server time
  ---
  duration_ms: 0.290602
  type: 'test'
  ...
# Subtest: multi-book worker prioritizes UI without truncating local depth
ok 7 - multi-book worker prioritizes UI without truncating local depth
  ---
  duration_ms: 0.451154
  type: 'test'
  ...
1..7
# tests 7
# suites 0
# pass 7
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 69.140264
\n===== node --test test-orderbook-flow-workspace.mjs =====
TAP version 13
# Subtest: normalizes and deduplicates trade history
ok 1 - normalizes and deduplicates trade history
  ---
  duration_ms: 159.962797
  type: 'test'
  ...
# Subtest: footprint columns preserve time, price and aggressor totals
ok 2 - footprint columns preserve time, price and aggressor totals
  ---
  duration_ms: 0.476921
  type: 'test'
  ...
# Subtest: flow window and visible count stay independent from pane width
ok 3 - flow window and visible count stay independent from pane width
  ---
  duration_ms: 0.281774
  type: 'test'
  ...
# Subtest: footprint tone reports buy and sell dominance
ok 4 - footprint tone reports buy and sell dominance
  ---
  duration_ms: 0.138551
  type: 'test'
  ...
# Subtest: 1M and 5M clusters use aligned live intervals without delta
ok 5 - 1M and 5M clusters use aligned live intervals without delta
  ---
  duration_ms: 0.79624
  type: 'test'
  ...
# Subtest: a live reset clears both cluster timeframes
ok 6 - a live reset clears both cluster timeframes
  ---
  duration_ms: 0.227823
  type: 'test'
  ...
# Subtest: cluster history keeps aligned 1M and 5M columns
ok 7 - cluster history keeps aligned 1M and 5M columns
  ---
  duration_ms: 1.302521
  type: 'test'
  ...
# Subtest: Ctrl+wheel compacts columns toward LIVE and dragging can inspect older intervals
ok 8 - Ctrl+wheel compacts columns toward LIVE and dragging can inspect older intervals
  ---
  duration_ms: 0.339724
  type: 'test'
  ...
# Subtest: Flow Workspace redraw observer cannot trigger itself
ok 9 - Flow Workspace redraw observer cannot trigger itself
  ---
  duration_ms: 1.42106
  type: 'test'
  ...
1..9
# tests 9
# suites 0
# pass 9
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 227.678444
\n===== Full test suite =====

> test
> node --test

TAP version 13
# Subtest: negative calibrated RX is unavailable instead of a false zero
ok 1 - negative calibrated RX is unavailable instead of a false zero
  ---
  duration_ms: 1.696724
  type: 'test'
  ...
# Subtest: header RX uses only the stable aggTrade feed and never renders literal zero
ok 2 - header RX uses only the stable aggTrade feed and never renders literal zero
  ---
  duration_ms: 0.32275
  type: 'test'
  ...
# Subtest: AGG painter renders and clips the aggregate price sweep
ok 3 - AGG painter renders and clips the aggregate price sweep
  ---
  duration_ms: 0.674641
  type: 'test'
  ...
# Subtest: arrival time owns live X while exchange time still owns zero-ms grouping
ok 4 - arrival time owns live X while exchange time still owns zero-ms grouping
  ---
  duration_ms: 2.106922
  type: 'test'
  ...
# Subtest: full market rendering is not driven by exact seconds or high-rate auxiliary feeds
ok 5 - full market rendering is not driven by exact seconds or high-rate auxiliary feeds
  ---
  duration_ms: 0.855571
  type: 'test'
  ...
# Subtest: footprint buckets live trades by browser arrival time
ok 6 - footprint buckets live trades by browser arrival time
  ---
  duration_ms: 1.338634
  type: 'test'
  ...
# Subtest: footprint bootstraps from stable Tape and locks to guarded flow after promotion
ok 7 - footprint bootstraps from stable Tape and locks to guarded flow after promotion
  ---
  duration_ms: 1.283004
  type: 'test'
  ...
# Subtest: promotion replaces stable bootstrap volume instead of duplicating it
ok 8 - promotion replaces stable bootstrap volume instead of duplicating it
  ---
  duration_ms: 1.143963
  type: 'test'
  ...
# Subtest: empty guarded packets never fall back or duplicate accumulated volume
ok 9 - empty guarded packets never fall back or duplicate accumulated volume
  ---
  duration_ms: 0.327089
  type: 'test'
  ...
# Subtest: current footprint interval accumulates repeated live packets
ok 10 - current footprint interval accumulates repeated live packets
  ---
  duration_ms: 0.43174
  type: 'test'
  ...
# Subtest: global feed separates Binance market and public namespaces
ok 11 - global feed separates Binance market and public namespaces
  ---
  duration_ms: 2.412869
  type: 'test'
  ...
# Subtest: fallback transport advances exactly once before required data
ok 12 - fallback transport advances exactly once before required data
  ---
  duration_ms: 0.213742
  type: 'test'
  ...
# Subtest: online requires a real miniTicker batch and subscription errors stay visible
ok 13 - online requires a real miniTicker batch and subscription errors stay visible
  ---
  duration_ms: 0.26942
  type: 'test'
  ...
# Subtest: runtime no longer mixes market and public streams on root endpoints
ok 14 - runtime no longer mixes market and public streams on root endpoints
  ---
  duration_ms: 11.882477
  type: 'test'
  ...
# Subtest: Event Radar Beta assets are removed from runtime and PWA cache
ok 15 - Event Radar Beta assets are removed from runtime and PWA cache
  ---
  duration_ms: 2.617259
  type: 'test'
  ...
# Subtest: global market feed goes online only after a valid miniTicker packet
ok 16 - global market feed goes online only after a valid miniTicker packet
  ---
  duration_ms: 1.487831
  type: 'test'
  ...
# Subtest: footprint bootstraps from stable Tape and switches once to guarded flow
ok 17 - footprint bootstraps from stable Tape and switches once to guarded flow
  ---
  duration_ms: 1.696032
  type: 'test'
  ...
# Subtest: Series groups raw aggressive trades for 100 ms and closes on the first opposite trade
ok 18 - Series groups raw aggressive trades for 100 ms and closes on the first opposite trade
  ---
  duration_ms: 0.925154
  type: 'test'
  ...
# Subtest: same-side trades outside the 100 ms window start a new Series
ok 19 - same-side trades outside the 100 ms window start a new Series
  ---
  duration_ms: 0.309296
  type: 'test'
  ...
# Subtest: footprint uses order-book USD formatting and highlights the column maximum
ok 20 - footprint uses order-book USD formatting and highlights the column maximum
  ---
  duration_ms: 0.429578
  type: 'test'
  ...
# Subtest: files keep correct identities
ok 21 - files keep correct identities
  ---
  duration_ms: 1.204477
  type: 'test'
  ...
# Subtest: background return reuses Worker with guarded staggered recovery
ok 22 - background return reuses Worker with guarded staggered recovery
  ---
  duration_ms: 0.500604
  type: 'test'
  ...
# Subtest: normalizes and deduplicates trade history
ok 23 - normalizes and deduplicates trade history
  ---
  duration_ms: 12.911527
  type: 'test'
  ...
# Subtest: footprint columns preserve time, price and aggressor totals
ok 24 - footprint columns preserve time, price and aggressor totals
  ---
  duration_ms: 0.634133
  type: 'test'
  ...
# Subtest: flow window and visible count stay independent from pane width
ok 25 - flow window and visible count stay independent from pane width
  ---
  duration_ms: 0.434439
  type: 'test'
  ...
# Subtest: footprint tone reports buy and sell dominance
ok 26 - footprint tone reports buy and sell dominance
  ---
  duration_ms: 0.218631
  type: 'test'
  ...
# Subtest: 1M and 5M clusters use aligned live intervals without delta
ok 27 - 1M and 5M clusters use aligned live intervals without delta
  ---
  duration_ms: 1.182895
  type: 'test'
  ...
# Subtest: a live reset clears both cluster timeframes
ok 28 - a live reset clears both cluster timeframes
  ---
  duration_ms: 0.359192
  type: 'test'
  ...
# Subtest: cluster history keeps aligned 1M and 5M columns
ok 29 - cluster history keeps aligned 1M and 5M columns
  ---
  duration_ms: 0.724793
  type: 'test'
  ...
# Subtest: Ctrl+wheel compacts columns toward LIVE and dragging can inspect older intervals
ok 30 - Ctrl+wheel compacts columns toward LIVE and dragging can inspect older intervals
  ---
  duration_ms: 1.432315
  type: 'test'
  ...
# Subtest: Flow Workspace redraw observer cannot trigger itself
ok 31 - Flow Workspace redraw observer cannot trigger itself
  ---
  duration_ms: 1.449136
  type: 'test'
  ...
# Subtest: starts on aggregate live and promotes raw only inside the shadow guard
ok 32 - starts on aggregate live and promotes raw only inside the shadow guard
  ---
  duration_ms: 1.535785
  type: 'test'
  ...
# Subtest: raw sequence gap immediately falls back without emitting the broken trade
ok 33 - raw sequence gap immediately falls back without emitting the broken trade
  ---
  duration_ms: 0.41177
  type: 'test'
  ...
# Subtest: aggregate fallback waits for a fully new range to avoid double volume
ok 34 - aggregate fallback waits for a fully new range to avoid double volume
  ---
  duration_ms: 1.723596
  type: 'test'
  ...
# Subtest: recovery to raw requires consecutive IDs beyond the aggregate boundary
ok 35 - recovery to raw requires consecutive IDs beyond the aggregate boundary
  ---
  duration_ms: 0.241948
  type: 'test'
  ...
# Subtest: invalid, duplicate and out-of-order events never reach the tape
ok 36 - invalid, duplicate and out-of-order events never reach the tape
  ---
  duration_ms: 0.25259
  type: 'test'
  ...
# Subtest: production worker keeps visual RAW stable and routes guarded raw trades only to AGG
not ok 37 - production worker keeps visual RAW stable and routes guarded raw trades only to AGG
  ---
  duration_ms: 3.427254
  type: 'test'
  location: '/home/runner/work/InPuls/InPuls/test-orderbook-guarded-raw-tape.mjs:103:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /return \[`\$\{name\}@aggTrade`, `\$\{name\}@trade`\];/. Input:
    
    'importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-network.js?v=obs-pr1-1");\n' +
      'importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-depth-projection.js?v=deep-book-v1");\n' +
      'importScripts("./orderbook-events.js?v=orderbook-events-core-v1");\n' +
      'importScripts("./orderbook-density.js?v=density-trades-correlation-v1");\n' +
      '\n' +
      'const MAX_BOOK_LEVELS_PER_SIDE = 20_000;\n' +
      'const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;\n' +
      'const MAX_BUFFERED_DEPTH_EVENTS = 4_000;\n' +
      'const MAX_TRADE_HISTORY = 12_000;\n' +
      'const MAX_RESUME_LEVELS_PER_SIDE = 700;\n' +
      'const RESUME_STAGGER_MS = 180;\n' +
      'const BACKGROUND_GRACE_MS = 2_000;\n' +
      'const RECOVERY_TIMEOUT_MS = 8_000;\n' +
      'const RESUME_STALE_MS = 3_500;\n' +
      'const DEPTH_STALE_NOTICE_MS = 3_000;\n' +
      'const ACTIVE_STALE_MS = 9_000;\n' +
      'const SNAPSHOT_TIMEOUT_MS = 2_800;\n' +
      'const IDLE_CLOSE_MS = 10_000;\n' +
      'const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;\n' +
      'const MAX_TAPE_BATCH_PER_POST = 500;\n' +
      'const MAX_PENDING_TAPE_TRADES = 800;\n' +
      'const TAPE_FLUSH_MS = 25;\n' +
      'const ACTIVE_SOURCE_LAG_MS = 4_000;\n' +
      'const TRADE_ACTIVE_STALE_MS = 30_000;\n' +
      'const RECONNECT_BASE_MS = 400;\n' +
      'const RECONNECT_MAX_MS = 8_000;\n' +
      'const WORKER_HEARTBEAT_MS = 2_000;\n' +
      'const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;\n' +
      '\n' +
      'const feeds = new Map();\n' +
      'let tabVisible = true;\n' +
      'let emitTimer = 0;\n' +
      'let emitCursor = 0;\n' +
      'let visibilityEpoch = 0;\n' +
      'let backgroundPauseTimer = 0;\n' +
      'let watchdogTimer = 0;\n' +
      'let prioritySymbols = [];\n' +
      'let serverClockOffsetMs = null;\n' +
      'let serverClockRttMs = null;\n' +
      'let serverClockSyncAt = 0;\n' +
      'let serverClockSyncPromise = null;\n' +
      'let observabilityEnabled = false;\n' +
      'let observabilityPostCount = 0;\n' +
      '\n' +
      'function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {\n' +
      '  if (!observabilityEnabled) {\n' +
      '    self.postMessage({ type, symbol, ...payload });\n' +
      '    return;\n' +
      '  }\n' +
      '  const observerStartedAt = performance.now();\n' +
      '  const message = { type, symbol, ...payload };\n' +
      '  const samplePayload = observabilityPostCount++ % 50 === 0;\n' +
      '  let payloadBytes = null;\n' +
      '  if (samplePayload) {\n' +
      '    try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}\n' +
      '  }\n' +
      '  const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);\n' +
      '  message.__obs = {\n' +
      '    sentAtEpochMs: Date.now(),\n' +
      '    processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,\n' +
      '    observerOverheadMs: performance.now() - observerStartedAt,\n' +
      '    payloadBytes,\n' +
      '    payloadSampleRate: 50,\n' +
      '    sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,\n' +
      '    sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,\n' +
      '    sourceKind: obsContext.sourceKind ?? null,\n' +
      '  };\n' +
      '  self.postMessage(message);\n' +
      '}\n' +
      '\n' +
      'function diagnose(symbol, phase, details = {}) {\n' +
      '  if (!observabilityEnabled) return;\n' +
      '  post("diagnostic", symbol, {\n' +
      '    diagnostic: {\n' +
      '      phase,\n' +
      '      atEpochMs: Date.now(),\n' +
      '      ...details,\n' +
      '    },\n' +
      '  });\n' +
      '}\n' +
      '\n' +
      'function reconnectDelay(attempt = 0) {\n' +
      '  const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));\n' +
      '  const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));\n' +
      '  return exponential + jitter;\n' +
      '}\n' +
      '\n' +
      'function bookSideQuoteScale(levels, sampleLimit = 1_024) {\n' +
      '  const totalLevels = Number.isFinite(levels?.size)\n' +
      '    ? levels.size\n' +
      '    : (Number(levels?.length) || 0);\n' +
      '  const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));\n' +
      '  const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));\n' +
      '  const sample = [];\n' +
      '  let maximum = 1;\n' +
      '  let validIndex = 0;\n' +
      '  for (const [priceValue, quantityValue] of levels ?? []) {\n' +
      '    const quote = Number(priceValue) * Number(quantityValue);\n' +
      '    if (!Number.isFinite(quote) || quote <= 0) continue;\n' +
      '    if (quote > maximum) maximum = quote;\n' +
      '    if (validIndex % sampleStride === 0) sample.push(quote);\n' +
      '    validIndex += 1;\n' +
      '  }\n' +
      '  sample.sort((left, right) => left - right);\n' +
      '  const quantile = (ratio) => sample.length\n' +
      '    ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]\n' +
      '    : 0;\n' +
      '  const median = quantile(.5);\n' +
      '  const upper = quantile(.95);\n' +
      '  const extreme = quantile(.99);\n' +
      '  return {\n' +
      '    maximum,\n' +
      '    anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),\n' +
      '    sampledLevels: sample.length,\n' +
      '    totalLevels: validIndex,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function bookQuoteScale(bids, asks, sampleLimit = 2_048) {\n' +
      '  const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));\n' +
      '  const bid = bookSideQuoteScale(bids ?? [], sideLimit);\n' +
      '  const ask = bookSideQuoteScale(asks ?? [], sideLimit);\n' +
      '  return {\n' +
      '    maximum: Math.max(bid.maximum, ask.maximum),\n' +
      '    anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),\n' +
      '    bidAnomalyThreshold: bid.anomalyThreshold,\n' +
      '    askAnomalyThreshold: ask.anomalyThreshold,\n' +
      '    sampledLevels: bid.sampledLevels + ask.sampledLevels,\n' +
      '    totalLevels: bid.totalLevels + ask.totalLevels,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function parsePayload(raw) {\n' +
      '  let payload;\n' +
      '  try { payload = JSON.parse(raw); } catch { return null; }\n' +
      '  if (payload?.result === null || payload?.id) return null;\n' +
      '  return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };\n' +
      '}\n' +
      '\n' +
      'function normalizeTrade(event, sourceHint = null, receivedAt = null) {\n' +
      '  const eventType = String(event?.e ?? "").toLowerCase();\n' +
      '  const inferredRaw = eventType === "trade"\n' +
      '    || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));\n' +
      '  const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";\n' +
      '  const price = Number(event?.p);\n' +
      '  const quantity = Number(event?.q);\n' +
      '  const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);\n' +
      '  const time = timing.tradeTime;\n' +
      '  const id = source === "raw" ? Number(event?.t) : Number(event?.a);\n' +
      '  const firstTradeId = source === "raw" ? id : Number(event?.f);\n' +
      '  const lastTradeId = source === "raw" ? id : Number(event?.l);\n' +
      '  if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;\n' +
      '  if (price <= 0 || quantity <= 0 || time <= 0) return null;\n' +
      '  if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;\n' +
      '  return {\n' +
      '    id,\n' +
      '    firstTradeId,\n' +
      '    lastTradeId,\n' +
      '    source,\n' +
      '    price,\n' +
      '    quantity,\n' +
      '    quote: price * quantity,\n' +
      '    time,\n' +
      '    tradeTime: timing.tradeTime,\n' +
      '    eventTime: timing.eventTime,\n' +
      '    receivedAt: timing.receivedAt,\n' +
      '    rxLatencyMs: timing.rxLatencyMs,\n' +
      '    side: event?.m ? "sell" : "buy",\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function calibratedSourceLag(eventTime, receivedAt = Date.now()) {\n' +
      '  const sourceTime = Number(eventTime);\n' +
      '  const localTime = Number(receivedAt);\n' +
      '  if (\n' +
      '    !Number.isFinite(sourceTime)\n' +
      '    || !Number.isFinite(localTime)\n' +
      '    || !Number.isFinite(serverClockOffsetMs)\n' +
      '  ) return null;\n' +
      '  const lag = localTime + serverClockOffsetMs - sourceTime;\n' +
      '  if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;\n' +
      '  return Math.max(0, lag);\n' +
      '}\n' +
      '\n' +
      'function sequenceDecision(lastUpdateId, event, firstEvent = false) {\n' +
      '  const first = Number(event?.U);\n' +
      '  const final = Number(event?.u);\n' +
      '  const previous = Number(event?.pu);\n' +
      '  const local = Number(lastUpdateId);\n' +
      '  if (![first, final, local].every(Number.isFinite)) return "resync";\n' +
      '  if (final <= local) return "ignore";\n' +
      '  if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";\n' +
      '  if (Number.isFinite(previous) && previous !== local) return "resync";\n' +
      '  if (!Number.isFinite(previous) && first > local + 1) return "resync";\n' +
      '  return "apply";\n' +
      '}\n' +
      '\n' +
      'async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {\n' +
      '  const controller = typeof AbortController === "function" ? new AbortController() : null;\n' +
      '  let timer;\n' +
      '  const abortFromExternal = () => controller?.abort();\n' +
      '  if (externalSignal?.aborted) abortFromExternal();\n' +
      '  else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });\n' +
      '  try {\n' +
      '    const timeout = new Promise((_, reject) => {\n' +
      '      timer = setTimeout(() => {\n' +
      '        controller?.abort();\n' +
      '        reject(new Error("timeout"));\n' +
      '      }, timeoutMs);\n' +
      '    });\n' +
      '    const request = fetch(url, {\n' +
      '      cache: "no-store",\n' +
      '      ...(controller ? { signal: controller.signal } : {}),\n' +
      '    }).then(async (response) => {\n' +
      '      if (!response.ok) throw new Error(`HTTP ${response.status}`);\n' +
      '      return response.json();\n' +
      '    });\n' +
      '    return await Promise.race([request, timeout]);\n' +
      '  } finally {\n' +
      '    clearTimeout(timer);\n' +
      '    externalSignal?.removeEventListener?.("abort", abortFromExternal);\n' +
      '  }\n' +
      '}\n' +
      '\n' +
      'async function syncServerClock(force = false) {\n' +
      '  const now = Date.now();\n' +
      '  if (\n' +
      '    !force\n' +
      '    && Number.isFinite(serverClockOffsetMs)\n' +
      '    && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS\n' +
      '  ) return serverClockOffsetMs;\n' +
      '  if (serverClockSyncPromise) return serverClockSyncPromise;\n' +
      '\n' +
      '  serverClockSyncPromise = (async () => {\n' +
      '    const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];\n' +
      '    diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });\n' +
      '    let result = null;\n' +
      '    try {\n' +
      '      result = await self.InPulsOrderBookNetwork.firstSuccessful(\n' +
      '        hosts,\n' +
      '        async (host, { signal }) => {\n' +
      '          const startedAt = Date.now();\n' +
      '          const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);\n' +
      '          const finishedAt = Date.now();\n' +
      '          const serverTime = Number(data?.serverTime);\n' +
      '          if (!Number.isFinite(serverTime)) throw new Error("invalid server time");\n' +
      '          return {\n' +
      '            rtt: Math.max(0, finishedAt - startedAt),\n' +
      '            offset: serverTime - (startedAt + finishedAt) / 2,\n' +
      '          };\n' +
      '        },\n' +
      '        {\n' +
      '          onAttempt: (event) => diagnose("", "clock.rest.host", {\n' +
      '            ...event,\n' +
      '            host: event.target,\n' +
      '            target: undefined,\n' +
      '          }),\n' +
      '        },\n' +
      '      );\n' +
      '    } catch {}\n' +
      '    const best = result?.value;\n' +
      '    if (best) {\n' +
      '      serverClockOffsetMs = best.offset;\n' +
      '      serverClockRttMs = best.rtt;\n' +
      '      serverClockSyncAt = Date.now();\n' +
      '      '... 50929 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");
    importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");
    importScripts("./orderbook-network.js?v=obs-pr1-1");
    importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");
    importScripts("./orderbook-depth-projection.js?v=deep-book-v1");
    importScripts("./orderbook-events.js?v=orderbook-events-core-v1");
    importScripts("./orderbook-density.js?v=density-trades-correlation-v1");
    
    const MAX_BOOK_LEVELS_PER_SIDE = 20_000;
    const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;
    const MAX_BUFFERED_DEPTH_EVENTS = 4_000;
    const MAX_TRADE_HISTORY = 12_000;
    const MAX_RESUME_LEVELS_PER_SIDE = 700;
    const RESUME_STAGGER_MS = 180;
    const BACKGROUND_GRACE_MS = 2_000;
    const RECOVERY_TIMEOUT_MS = 8_000;
    const RESUME_STALE_MS = 3_500;
    const DEPTH_STALE_NOTICE_MS = 3_000;
    const ACTIVE_STALE_MS = 9_000;
    const SNAPSHOT_TIMEOUT_MS = 2_800;
    const IDLE_CLOSE_MS = 10_000;
    const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;
    const MAX_TAPE_BATCH_PER_POST = 500;
    const MAX_PENDING_TAPE_TRADES = 800;
    const TAPE_FLUSH_MS = 25;
    const ACTIVE_SOURCE_LAG_MS = 4_000;
    const TRADE_ACTIVE_STALE_MS = 30_000;
    const RECONNECT_BASE_MS = 400;
    const RECONNECT_MAX_MS = 8_000;
    const WORKER_HEARTBEAT_MS = 2_000;
    const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;
    
    const feeds = new Map();
    let tabVisible = true;
    let emitTimer = 0;
    let emitCursor = 0;
    let visibilityEpoch = 0;
    let backgroundPauseTimer = 0;
    let watchdogTimer = 0;
    let prioritySymbols = [];
    let serverClockOffsetMs = null;
    let serverClockRttMs = null;
    let serverClockSyncAt = 0;
    let serverClockSyncPromise = null;
    let observabilityEnabled = false;
    let observabilityPostCount = 0;
    
    function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {
      if (!observabilityEnabled) {
        self.postMessage({ type, symbol, ...payload });
        return;
      }
      const observerStartedAt = performance.now();
      const message = { type, symbol, ...payload };
      const samplePayload = observabilityPostCount++ % 50 === 0;
      let payloadBytes = null;
      if (samplePayload) {
        try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}
      }
      const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);
      message.__obs = {
        sentAtEpochMs: Date.now(),
        processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,
        observerOverheadMs: performance.now() - observerStartedAt,
        payloadBytes,
        payloadSampleRate: 50,
        sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,
        sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,
        sourceKind: obsContext.sourceKind ?? null,
      };
      self.postMessage(message);
    }
    
    function diagnose(symbol, phase, details = {}) {
      if (!observabilityEnabled) return;
      post("diagnostic", symbol, {
        diagnostic: {
          phase,
          atEpochMs: Date.now(),
          ...details,
        },
      });
    }
    
    function reconnectDelay(attempt = 0) {
      const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));
      const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));
      return exponential + jitter;
    }
    
    function bookSideQuoteScale(levels, sampleLimit = 1_024) {
      const totalLevels = Number.isFinite(levels?.size)
        ? levels.size
        : (Number(levels?.length) || 0);
      const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));
      const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));
      const sample = [];
      let maximum = 1;
      let validIndex = 0;
      for (const [priceValue, quantityValue] of levels ?? []) {
        const quote = Number(priceValue) * Number(quantityValue);
        if (!Number.isFinite(quote) || quote <= 0) continue;
        if (quote > maximum) maximum = quote;
        if (validIndex % sampleStride === 0) sample.push(quote);
        validIndex += 1;
      }
      sample.sort((left, right) => left - right);
      const quantile = (ratio) => sample.length
        ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]
        : 0;
      const median = quantile(.5);
      const upper = quantile(.95);
      const extreme = quantile(.99);
      return {
        maximum,
        anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),
        sampledLevels: sample.length,
        totalLevels: validIndex,
      };
    }
    
    function bookQuoteScale(bids, asks, sampleLimit = 2_048) {
      const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));
      const bid = bookSideQuoteScale(bids ?? [], sideLimit);
      const ask = bookSideQuoteScale(asks ?? [], sideLimit);
      return {
        maximum: Math.max(bid.maximum, ask.maximum),
        anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),
        bidAnomalyThreshold: bid.anomalyThreshold,
        askAnomalyThreshold: ask.anomalyThreshold,
        sampledLevels: bid.sampledLevels + ask.sampledLevels,
        totalLevels: bid.totalLevels + ask.totalLevels,
      };
    }
    
    function parsePayload(raw) {
      let payload;
      try { payload = JSON.parse(raw); } catch { return null; }
      if (payload?.result === null || payload?.id) return null;
      return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };
    }
    
    function normalizeTrade(event, sourceHint = null, receivedAt = null) {
      const eventType = String(event?.e ?? "").toLowerCase();
      const inferredRaw = eventType === "trade"
        || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));
      const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";
      const price = Number(event?.p);
      const quantity = Number(event?.q);
      const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);
      const time = timing.tradeTime;
      const id = source === "raw" ? Number(event?.t) : Number(event?.a);
      const firstTradeId = source === "raw" ? id : Number(event?.f);
      const lastTradeId = source === "raw" ? id : Number(event?.l);
      if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;
      if (price <= 0 || quantity <= 0 || time <= 0) return null;
      if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;
      return {
        id,
        firstTradeId,
        lastTradeId,
        source,
        price,
        quantity,
        quote: price * quantity,
        time,
        tradeTime: timing.tradeTime,
        eventTime: timing.eventTime,
        receivedAt: timing.receivedAt,
        rxLatencyMs: timing.rxLatencyMs,
        side: event?.m ? "sell" : "buy",
      };
    }
    
    function calibratedSourceLag(eventTime, receivedAt = Date.now()) {
      const sourceTime = Number(eventTime);
      const localTime = Number(receivedAt);
      if (
        !Number.isFinite(sourceTime)
        || !Number.isFinite(localTime)
        || !Number.isFinite(serverClockOffsetMs)
      ) return null;
      const lag = localTime + serverClockOffsetMs - sourceTime;
      if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;
      return Math.max(0, lag);
    }
    
    function sequenceDecision(lastUpdateId, event, firstEvent = false) {
      const first = Number(event?.U);
      const final = Number(event?.u);
      const previous = Number(event?.pu);
      const local = Number(lastUpdateId);
      if (![first, final, local].every(Number.isFinite)) return "resync";
      if (final <= local) return "ignore";
      if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";
      if (Number.isFinite(previous) && previous !== local) return "resync";
      if (!Number.isFinite(previous) && first > local + 1) return "resync";
      return "apply";
    }
    
    async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {
      const controller = typeof AbortController === "function" ? new AbortController() : null;
      let timer;
      const abortFromExternal = () => controller?.abort();
      if (externalSignal?.aborted) abortFromExternal();
      else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });
      try {
        const timeout = new Promise((_, reject) => {
          timer = setTimeout(() => {
            controller?.abort();
            reject(new Error("timeout"));
          }, timeoutMs);
        });
        const request = fetch(url, {
          cache: "no-store",
          ...(controller ? { signal: controller.signal } : {}),
        }).then(async (response) => {
          if (!response.ok) throw new Error(`HTTP ${response.status}`);
          return response.json();
        });
        return await Promise.race([request, timeout]);
      } finally {
        clearTimeout(timer);
        externalSignal?.removeEventListener?.("abort", abortFromExternal);
      }
    }
    
    async function syncServerClock(force = false) {
      const now = Date.now();
      if (
        !force
        && Number.isFinite(serverClockOffsetMs)
        && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS
      ) return serverClockOffsetMs;
      if (serverClockSyncPromise) return serverClockSyncPromise;
    
      serverClockSyncPromise = (async () => {
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });
        let result = null;
        try {
          result = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const startedAt = Date.now();
              const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);
              const finishedAt = Date.now();
              const serverTime = Number(data?.serverTime);
              if (!Number.isFinite(serverTime)) throw new Error("invalid server time");
              return {
                rtt: Math.max(0, finishedAt - startedAt),
                offset: serverTime - (startedAt + finishedAt) / 2,
              };
            },
            {
              onAttempt: (event) => diagnose("", "clock.rest.host", {
                ...event,
                host: event.target,
                target: undefined,
              }),
            },
          );
        } catch {}
        const best = result?.value;
        if (best) {
          serverClockOffsetMs = best.offset;
          serverClockRttMs = best.rtt;
          serverClockSyncAt = Date.now();
          diagnose("", "clock.rest", {
            state: "succeeded",
            host: result.target,
            durationMs: result.durationMs,
            rttMs: best.rtt,
          });
        } else {
          diagnose("", "clock.rest", { state: "failed" });
        }
        return serverClockOffsetMs;
      })().finally(() => {
        serverClockSyncPromise = null;
      });
      return serverClockSyncPromise;
    }
    
    function depthTransports(symbol, mode) {
      const stream = `${symbol.toLowerCase()}@${mode === "partial" ? "depth20" : "depth"}@100ms`;
      return [
        { name: "public · combined", url: `wss://fstream.binance.com/public/stream?streams=${stream}`, subscribe: false, stream },
        { name: "public · raw", url: `wss://fstream.binance.com/public/ws/${stream}`, subscribe: false, stream },
      ];
    }
    
    function tradeStreams(symbol) {
      const name = symbol.toLowerCase();
      // Production Tape uses the documented USD-M Futures @aggTrade stream.
      // Undocumented @trade remains excluded until a separate shadow study proves
      // continuity, duplication and latency behaviour over a long observation.
      return [`${name}@aggTrade`];
    }
    
    function tradeTransports(streams) {
      const stream = String(streams?.[0] ?? "");
      return [
        { name: "market · combined", url: `wss://fstream.binance.com/market/stream?streams=${stream}`, subscribe: false, streams: [stream] },
        { name: "market · raw", url: `wss://fstream.binance.com/market/ws/${stream}`, subscribe: false, streams: [stream] },
      ];
    }
    
    function trimSide(levels, side, limit) {
      if (levels.size <= limit) return;
      const keys = [...levels.keys()].sort(side === "bid" ? (a, b) => b - a : (a, b) => a - b);
      for (const price of keys.slice(limit)) levels.delete(price);
    }
    
    class SymbolFeed {
      constructor(symbol) {
        this.symbol = symbol;
        this.subscribers = 0;
        this.closeTimer = 0;
        this.socket = null;
        this.tradeSocket = null;
        this.reconnectTimer = 0;
        this.tradeReconnectTimer = 0;
        this.tradeFirstMessageTimer = 0;
        this.firstDepthTimer = 0;
        this.snapshotTimer = 0;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.generation = 0;
        this.bids = new Map();
        this.asks = new Map();
        this.partialBidKeys = new Set();
        this.partialAskKeys = new Set();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.resyncCount = 0;
        this.dirty = false;
        this.forceEmit = false;
        this.lastEmitAt = 0;
        this.cachedSorted = null;
        this.bookEvents = new self.InPulsOrderBookEvents.DepthEventJournal({ symbol });
        this.densityLifecycle = new self.InPulsOrderBookDensity.DensityLifecycleTracker({ symbol });
        this.statusKey = "";
        this.trades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.tradeIds = new Set();
        this.tapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.aggregationTrades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.aggregationTradeIds = new Set();
        this.aggregationTapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.backgroundPaused = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.lastMessageAt = 0;
        this.lastRestartAt = 0;
        this.syncing = false;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.lastResyncAt = 0;
        this.tradeTransportName = "—";
        this.flowWindowStartedAt = Date.now();
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
        this.latestFlowHealth = null;
        this.tapeGuard = new self.InPulsTapeGuard({ rawWarmupTrades: 6, rawStaleMs: 1_500 });
        this.tradeLatency = new self.InPulsTapeLatency.RollingLatency({ windowMs: 2_000, maxSamples: 400, updateMs: 250 });
      }
    
      tradeBoundary() {
        let boundary = null;
        for (const trade of this.trades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeSnapshot(limit = MAX_TRADE_HISTORY) {
        return this.trades.toArray(limit);
      }
    
      aggregationBoundary() {
        let boundary = null;
        for (const trade of this.aggregationTrades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeKey(trade) {
        const firstTradeId = Number.isInteger(Number(trade?.firstTradeId))
          ? Number(trade.firstTradeId)
          : trade?.id;
        const lastTradeId = Number.isInteger(Number(trade?.lastTradeId))
          ? Number(trade.lastTradeId)
          : trade?.id;
        return `${firstTradeId}:${lastTradeId}:${trade?.time}:${trade?.price}:${trade?.quantity}`;
      }
    
      resetTapeGuard() {
        this.tapeGuard.reset({ lastOutputTradeId: this.aggregationBoundary() });
      }
    
      addSubscriber() {
        const wasZero = this.subscribers === 0;
        const wasCoolingDown = Boolean(this.closeTimer);
        this.subscribers += 1;
        clearTimeout(this.closeTimer);
        this.closeTimer = 0;
        if (wasZero && !wasCoolingDown) this.start();
        else this.refresh();
      }
    
      removeSubscriber() {
        this.subscribers = Math.max(0, this.subscribers - 1);
        if (this.subscribers > 0 || this.closeTimer) return;
        this.closeTimer = setTimeout(() => {
          this.closeTimer = 0;
          if (this.subscribers === 0) {
            this.stop();
            feeds.delete(this.symbol);
          }
        }, IDLE_CLOSE_MS);
      }
    
      start() {
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tradeLatency.reset();
        this.resetFlowWindow();
        this.syncing = false;
        this.backgroundPaused = false;
        this.resetTapeGuard();
        this.resetBook("start");
        this.setStatus("loading", "Подключение Worker");
        const generation = this.generation;
        diagnose(this.symbol, "feed.start", {
          state: "started",
          generation,
          subscribers: this.subscribers,
        });
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      stopSockets() {
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        try { this.socket?.close(); } catch {}
        try { this.tradeSocket?.close(); } catch {}
        this.socket = null;
        this.tradeSocket = null;
        this.tapeGuard.disconnect("socket-stop");
      }
    
      stop() {
        diagnose(this.symbol, "feed.stop", {
          state: "started",
          generation: this.generation,
        });
        this.generation += 1;
        this.stopSockets();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
      }
    
      resetBook(reason = "reset") {
        this.bids.clear();
        this.asks.clear();
        this.partialBidKeys.clear();
        this.partialAskKeys.clear();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.cachedSorted = null;
        this.dirty = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        const bookEpoch = this.bookEvents.reset(reason);
        this.densityLifecycle.reset({ bookEpoch, reason });
      }
    
      setStatus(state, text) {
        const key = `${state}:${text}`;
        if (key === this.statusKey) return;
        this.statusKey = key;
        post("status", this.symbol, { state, text });
      }
    
      latencyText() {
        const latency = this.tradeLatency.current();
        if (!Number.isFinite(latency)) return "";
        const value = latency < 1
          ? "<1"
          : latency < 10
            ? latency.toFixed(1)
            : String(Math.round(latency));
        return ` · RX ${value}ms`;
      }
    
      liveStatusText(tapeState = null) {
        const partial = this.mode === "partial" ? " · 20" : "";
        const reconnectingTape = tapeState === "reconnect" || (this.tradeLive && !this.tradeConnected);
        const tape = reconnectingTape
          ? " · TAPE RECONNECT"
          : (this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : "");
        return `LIVE 100ms · WORKER${partial}${tape}${this.latencyText()}`;
      }
    
      publishLiveStatus(tapeState = null) {
        if (this.syncing) {
          this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          return;
        }
        const depthAge = this.lastDepthAt ? Date.now() - this.lastDepthAt : Infinity;
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
          return;
        }
        this.setStatus("online", this.liveStatusText(tapeState));
      }
    
      markDirty(force = false) {
        this.dirty = true;
        if (force) this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      refresh() {
        this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      pauseForBackground() {
        if (this.subscribers <= 0 || this.backgroundPaused) return;
        this.backgroundPaused = true;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.generation += 1;
        this.stopSockets();
        this.tradeConnected = false;
        this.tradeLive = false;
        this.tradeLatency.reset();
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Пауза Worker",
        );
      }
    
      resume(delayMs = 0, epoch = visibilityEpoch) {
        clearTimeout(this.resumeTimer);
        this.resumeTimer = setTimeout(() => {
          this.resumeTimer = 0;
          if (!tabVisible || epoch !== visibilityEpoch || this.subscribers <= 0) return;
    
          const now = Date.now();
          const socketOpen = this.socket?.readyState === WebSocket.OPEN;
          const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
          const depthFresh = this.lastDepthAt > 0 && now - this.lastDepthAt <= RESUME_STALE_MS;
    
          if (!this.backgroundPaused && socketOpen && depthFresh) {
            post("tape", this.symbol, {
              replace: true,
              liveOnly: true,
              trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            }, null, { sourceKind: "live-reset" });
            if (!tradeOpen && !this.tradeReconnectTimer) {
              this.connectTrades(this.generation);
            }
            this.forceEmit = true;
            this.emit(now, MAX_RESUME_LEVELS_PER_SIDE, true);
            this.publishLiveStatus(tradeOpen ? null : "reconnect");
            return;
          }
    
          this.backgroundPaused = false;
          this.restartAfterBackground(true);
        }, Math.max(0, delayMs));
      }
    
      restartAfterBackground(force = false) {
        if (this.subscribers <= 0) return;
        const now = Date.now();
        if (!force && now - this.lastRestartAt < 2_500) return;
        this.lastRestartAt = now;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tapeGuard.disconnect("background-restart");
        this.resetFlowWindow();
        this.resetBook("background-restart");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Восстановление Worker",
        );
        const generation = this.generation;
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        }, null, { sourceKind: "live-reset" });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      resetFlowWindow(now = Date.now()) {
        this.flowWindowStartedAt = now;
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
      }
    
      recordFlow(kind, startedAt) {
        const durationMs = Math.max(0, performance.now() - startedAt);
        if (kind === "depth") {
          this.depthEventsInWindow += 1;
          this.depthProcessMsInWindow += durationMs;
          this.depthProcessMaxMsInWindow = Math.max(this.depthProcessMaxMsInWindow, durationMs);
          return;
        }
        this.tradeEventsInWindow += 1;
        this.tradeProcessMsInWindow += durationMs;
        this.tradeProcessMaxMsInWindow = Math.max(this.tradeProcessMaxMsInWindow, durationMs);
      }
    
      reportFlow(now = Date.now()) {
        const durationMs = Math.max(1, now - this.flowWindowStartedAt);
        const perSecond = 1_000 / durationMs;
        const snapshot = {
          durationMs,
          depthEventsPerSecond: this.depthEventsInWindow * perSecond,
          tradeEventsPerSecond: this.tradeEventsInWindow * perSecond,
          depthProcessMeanMs: this.depthEventsInWindow
            ? this.depthProcessMsInWindow / this.depthEventsInWindow
            : 0,
          tradeProcessMeanMs: this.tradeEventsInWindow
            ? this.tradeProcessMsInWindow / this.tradeEventsInWindow
            : 0,
          depthProcessMaxMs: this.depthProcessMaxMsInWindow,
          tradeProcessMaxMs: this.tradeProcessMaxMsInWindow,
          tapeQueue: this.tapeBatch.length,
          tapeDropped: this.tapeDroppedInWindow,
          depthSourceLagMs: this.lastDepthSourceLagMs,
          tradeSourceLagMs: this.lastTradeSourceLagMs,
        };
        this.latestFlowHealth = snapshot;
        diagnose(this.symbol, "worker.flow", {
          state: "sampled",
          generation: this.generation,
          ...snapshot,
        });
        this.resetFlowWindow(now);
        return snapshot;
      }
    
      ensureHealthy(now = Date.now()) {
        if (!tabVisible || this.subscribers <= 0) return;
        const recoveryDelayed = this.syncing
          && this.lastRestartAt > 0
          && now - this.lastRestartAt > RECOVERY_TIMEOUT_MS;
        if (recoveryDelayed) {
          this.setStatus("stale", "RECOVERY DELAY · повторное подключение");
          this.restartAfterBackground(true);
          return;
        }
        const socketOpen = this.socket?.readyState === WebSocket.OPEN;
        const socketConnecting = this.socket?.readyState === WebSocket.CONNECTING;
        const reconnectPending = Boolean(this.reconnectTimer || this.firstDepthTimer || this.snapshotLoading);
        const depthAge = this.lastDepthAt > 0 ? now - this.lastDepthAt : Infinity;
        const depthSourceStale = this.depthReady
          && Number.isFinite(this.lastDepthSourceLagMs)
          && this.lastDepthSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const stale = this.depthReady && (depthAge > ACTIVE_STALE_MS || depthSourceStale);
    
        if ((!socketOpen && !socketConnecting && !reconnectPending) || stale) {
          if (stale) {
            diagnose(this.symbol, "depth.freshness", {
              state: "stale",
              generation: this.generation,
              depthAgeMs: depthAge,
              sourceLagMs: this.lastDepthSourceLagMs,
              socketOpen,
            });
          }
          this.setStatus("offline", "RECONNECT · WORKER");
          this.restartAfterBackground();
          return;
        }
    
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
        } else if (this.depthReady && this.statusKey.startsWith("stale:")) {
          this.publishLiveStatus();
        }
    
        const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
        const tradeConnecting = this.tradeSocket?.readyState === WebSocket.CONNECTING;
        const tradeAge = this.lastTradeAt > 0 ? now - this.lastTradeAt : Infinity;
        const tradeSourceStale = Number.isFinite(this.lastTradeSourceLagMs)
          && this.lastTradeSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const tradeStale = this.tradeConnected
          && (tradeAge > TRADE_ACTIVE_STALE_MS || tradeSourceStale);
        if (tradeOpen && tradeStale) {
          diagnose(this.symbol, "tape.freshness", {
            state: "stale",
            generation: this.generation,
            tradeAgeMs: tradeAge,
            sourceLagMs: this.lastTradeSourceLagMs,
            socketOpen: true,
          });
          this.publishLiveStatus("reconnect");
          try { this.tradeSocket?.close(); } catch {}
          return;
        }
        if (!tradeOpen && !tradeConnecting && !this.tradeReconnectTimer) {
          if (this.tradeLive) this.publishLiveStatus("reconnect");
          this.connectTrades(this.generation);
        }
      }
    
      priorityRank() {
        const index = prioritySymbols.indexOf(this.symbol);
        return index < 0 ? Number.MAX_SAFE_INTEGER : index;
      }
    
      emittedLimit() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1) return MAX_EMITTED_LEVELS_PER_SIDE;
        if (this.priorityRank() === 0) return active <= 2 ? 1_800 : 900;
        if (active === 2) return 900;
        if (active <= 4) return 450;
        return 280;
      }
    
      emitIntervalMs() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1 || this.priorityRank() === 0) return 100;
        if (active <= 4) return 220;
        return 320;
      }
    
      bookStorageLimit() {
        // UI emission is throttled separately. Never destroy the deep local
        // book merely because several panels are open.
        return MAX_BOOK_LEVELS_PER_SIDE;
      }
    
      sortedDepth() {
        if (this.cachedSorted) return this.cachedSorted;
        const rawBids = [...this.bids.entries()];
        const rawAsks = [...this.asks.entries()];
        const bids = rawBids.sort((a, b) => b[0] - a[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const asks = rawAsks.sort((a, b) => a[0] - b[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const quoteScale = bookQuoteScale(rawBids, rawAsks);
        this.cachedSorted = {
          bids,
          asks,
          sizeScaleMaxQuote: quoteScale.maximum,
          sizeAnomalyThresholdQuote: quoteScale.anomalyThreshold,
          sizeAnomalyThresholdBidQuote: quoteScale.bidAnomalyThreshold,
          sizeAnomalyThresholdAskQuote: quoteScale.askAnomalyThreshold,
        };
        return this.cachedSorted;
      }
    
      emit(now = Date.now(), requestedLimit = this.emittedLimit(), force = false) {
        const processStartedAt = observabilityEnabled ? performance.now() : null;
        if (!tabVisible || this.subscribers <= 0 || (!force && !this.dirty && !this.forceEmit)) return;
        if (!force && !this.forceEmit && now - this.lastEmitAt < this.emitIntervalMs()) return;
        const fullView = this.sortedDepth();
        const limit = Math.max(100, Math.min(MAX_EMITTED_LEVELS_PER_SIDE, Math.floor(requestedLimit)));
        const view = self.InPulsOrderBookDepthProjection.compactDepthView(fullView, {
          exactLimit: limit,
          densityLimit: 96,
          bandCount: 128,
        });
        if (!view.bids.length || !view.asks.length) return;
        const bestBid = Number(fullView.bids[0][0]);
        const bestAsk = Number(fullView.asks[0][0]);
        const middle = (bestBid + bestAsk) / 2;
        const lowestBid = Number(view.bids.at(-1)?.[0]);
        const highestAsk = Number(view.asks.at(-1)?.[0]);
        this.densityLifecycle.refresh({
          bids: fullView.bids,
          asks: fullView.asks,
          now,
        });
        post("data", this.symbol, {
          data: {
            symbol: this.symbol,
            bids: view.bids,
            asks: view.asks,
            trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            bestBid,
            bestAsk,
            lastUpdateId: this.lastUpdateId,
            eventTime: now,
            depthReady: this.depthReady,
            coverage: {
              bidPercent: Number.isFinite(lowestBid) ? Math.max(0, ((middle - lowestBid) / middle) * 100) : 0,
              askPercent: Number.isFinite(highestAsk) ? Math.max(0, ((highestAsk - middle) / middle) * 100) : 0,
            },
            bookLevels: { bids: this.bids.size, asks: this.asks.size },
            sizeScaleMaxQuote: fullView.sizeScaleMaxQuote,
            sizeAnomalyThresholdQuote: fullView.sizeAnomalyThresholdQuote,
            sizeAnomalyThresholdBidQuote: fullView.sizeAnomalyThresholdBidQuote,
            sizeAnomalyThresholdAskQuote: fullView.sizeAnomalyThresholdAskQuote,
            resyncCount: this.resyncCount,
            orderBookEvents: this.bookEvents.summary(),
            densityLifecycle: this.densityLifecycle.summary(now),
            depthProjection: view.metadata,
            health: {
              mode: this.mode,
              depthAgeMs: this.lastDepthAt ? Math.max(0, now - this.lastDepthAt) : null,
              tradeAgeMs: this.lastTradeAt ? Math.max(0, now - this.lastTradeAt) : null,
              depthSourceLagMs: this.lastDepthSourceLagMs,
              tradeSourceLagMs: this.lastTradeSourceLagMs,
              depthBuffer: this.depthBuffer.length,
              tapeQueue: this.tapeBatch.length,
              subscribers: this.subscribers,
              depthTransport: this.transportIndex,
              tradeTransport: this.tradeTransportIndex,
              tradeTransportName: this.tradeTransportName,
              syncing: this.syncing,
              flow: this.latestFlowHealth,
              tape: this.tapeGuard.snapshot(now),
            },
            worker: true,
          },
        }, processStartedAt, {
          sourceEventTimeMs: this.lastDepthEventTime,
          sourceKind: this.lastDepthEventTime ? "live-depth" : "snapshot-depth",
        });
        this.lastEmitAt = now;
        this.dirty = false;
        this.forceEmit = false;
      }
    
      trimBook() {
        const limit = this.bookStorageLimit();
        trimSide(this.bids, "bid", limit);
        trimSide(this.asks, "ask", limit);
      }
    
      applyDepth(event, first = false) {
        const decision = sequenceDecision(this.lastUpdateId, event, first);
        if (decision === "ignore") return true;
        if (decision === "resync") {
          this.resync("Разрыв последовательности");
          return false;
        }
        const bookEvents = this.bookEvents.applyDiff({
          bids: this.bids,
          asks: this.asks,
          event,
          continuity: this.depthReady ? "live" : "recovered",
          receivedAt: Number(event?.__receivedAt) || Date.now(),
        });
        this.densityLifecycle.ingest(bookEvents);
        this.lastUpdateId = Number(event.u);
        this.cachedSorted = null;
        this.trimBook();
        this.markDirty();
        return true;
      }
    
      bufferDepth(event) {
        this.depthBuffer.push(event);
        if (this.depthBuffer.length > MAX_BUFFERED_DEPTH_EVENTS) {
          this.depthBuffer.splice(0, this.depthBuffer.length - MAX_BUFFERED_DEPTH_EVENTS);
          this.resync("Переполнение буфера");
        }
      }
    
      installSnapshot() {
        const snapshot = this.pendingSnapshot;
        if (!snapshot) return false;
        const snapshotId = Number(snapshot.lastUpdateId);
        const applicable = this.depthBuffer.filter((event) => Number(event?.u) > snapshotId);
        const bridgeIndex = applicable.findIndex(
          (event) => Number(event?.U) <= snapshotId + 1 && Number(event?.u) >= snapshotId + 1,
        );
        if (bridgeIndex < 0) {
          const firstU = Number(applicable[0]?.U);
          if (Number.isFinite(firstU) && firstU > snapshotId + 1) {
            diagnose(this.symbol, "depth.snapshot.bridge", {
              state: "gap",
              generation: this.generation,
              snapshotId,
              firstBufferedUpdateId: firstU,
            });
            this.pendingSnapshot = null;
            clearTimeout(this.snapshotTimer);
            this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), 250);
          }
          return false;
        }
        this.bids = new Map();
        this.asks = new Map();
        const snapshotReceivedAt = Date.now();
        const baseline = this.bookEvents.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          snapshot,
          receivedAt: snapshotReceivedAt,
        });
        this.densityLifecycle.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          bookEpoch: this.bookEvents.bookEpoch,
          receivedAt: snapshotReceivedAt,
        });
        this.lastUpdateId = baseline.snapshotId;
        this.cachedSorted = null;
        for (let index = bridgeIndex; index < applicable.length; index += 1) {
          if (!this.applyDepth(applicable[index], index === bridgeIndex)) return false;
        }
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.depthReady = true;
        this.syncing = false;
        this.bookEvents.markReady();
        this.densityLifecycle.markReady();
        diagnose(this.symbol, "depth.live", {
          state: "ready",
          generation: this.generation,
          mode: this.mode,
          snapshotId,
          bufferedEvents: applicable.length - bridgeIndex,
          bids: this.bids.size,
          asks: this.asks.size,
          orderBookEvents: this.bookEvents.summary(),
        });
        this.publishLiveStatus();
        this.markDirty(true);
        return true;
      }
    
      async loadSnapshot(generation) {
        if (generation !== this.generation || this.mode !== "deep" || this.snapshotLoading) return;
        this.snapshotLoading = true;
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        let snapshot = null;
        let winner = null;
        diagnose(this.symbol, "depth.snapshot", {
          state: "scheduled",
          generation,
          hosts: hosts.length,
        });
        try {
          winner = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const data = await fetchJson(
                `https://${host}/fapi/v1/depth?symbol=${encodeURIComponent(this.symbol)}&limit=1000`,
                SNAPSHOT_TIMEOUT_MS,
                signal,
              );
              if (!Array.isArray(data?.bids) || !Array.isArray(data?.asks) || !Number.isFinite(Number(data?.lastUpdateId))) {
                throw new Error("invalid snapshot");
              }
              return data;
            },
            {
              onAttempt: (event) => diagnose(this.symbol, "depth.snapshot.host", {
                ...event,
                host: event.target,
                target: undefined,
                generation,
              }),
            },
          );
          snapshot = winner.value;
        } catch {}
        this.snapshotLoading = false;
        if (generation !== this.generation || this.mode !== "deep") return;
        if (!snapshot) {
          diagnose(this.symbol, "depth.snapshot", {
            state: "failed",
            generation,
          });
          this.activatePartial(generation);
          return;
        }
        diagnose(this.symbol, "depth.snapshot", {
          state: "succeeded",
          generation,
          host: winner.target,
          durationMs: winner.durationMs,
          snapshotId: Number(snapshot.lastUpdateId),
        });
        this.pendingSnapshot = snapshot;
        this.installSnapshot();
      }
    
      activatePartial(generation) {
        if (generation !== this.generation || this.mode === "partial") return;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.mode = "partial";
        this.transportIndex = 0;
        this.syncing = preserveLastFrame;
        this.resetBook("partial-fallback");
        this.bookEvents.markUnavailable("partial-depth");
        this.densityLifecycle.markUnavailable("partial-depth");
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Резервный Worker-стакан",
        );
        diagnose(this.symbol, "depth.fallback", {
          state: "activated",
          generation,
          mode: "partial",
        });
        try { this.socket?.close(); } catch {}
        this.socket = null;
        this.reconnectTimer = setTimeout(() => this.connectDepth(generation), 0);
      }
    
      resync(text) {
        if (this.mode !== "deep") return;
        const now = Date.now();
        if (now - this.lastResyncAt < 350) return;
        this.lastResyncAt = now;
        this.resyncCount += 1;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.resetBook(text === "Переполнение буфера" ? "buffer-overflow" : "sequence-gap");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? `СИНХРОНИЗАЦИЯ · ${text}` : text,
        );
        clearTimeout(this.snapshotTimer);
        const delay = Math.min(2_000, 250 + this.resyncCount * 75);
        this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), delay);
      }
    
      replacePartial(target, previousKeys, rows) {
        const nextKeys = new Set();
        for (const row of rows ?? []) {
          const price = Number(row?.[0]);
          const quantity = Number(row?.[1]);
          if (!Number.isFinite(price) || !Number.isFinite(quantity)) continue;
          nextKeys.add(price);
          if (quantity > 0) target.set(price, quantity);
          else target.delete(price);
        }
        for (const price of previousKeys) if (!nextKeys.has(price)) target.delete(price);
        return nextKeys;
      }
    
      connectDepth(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.firstDepthTimer);
        const transports = depthTransports(this.symbol, this.mode);
        const transport = transports[this.transportIndex % transports.length];
        const transportIndex = this.transportIndex % transports.length;
        diagnose(this.symbol, "depth.ws.create", {
          state: "started",
          generation,
          mode: this.mode,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "depth.ws.create", {
            state: "failed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.transportIndex += 1;
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
          return;
        }
        this.socket = socket;
        this.firstDepthTimer = setTimeout(() => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.first-message", {
              state: "timeout",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              durationMs: 8_000,
            });
            try { socket.close(); } catch {}
          }
        }, 8_000);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.socket) return;
          diagnose(this.symbol, "depth.ws.open", {
            state: "succeeded",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
          });
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: [transport.stream],
              id: Date.now() % 2_147_483_647,
            }));
          }
          if (this.syncing) this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          else this.setStatus("loading", this.mode === "deep" ? "Синхронизация Worker" : "Подключаю резерв Worker");
          if (this.mode === "deep") this.loadSnapshot(generation);
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.socket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            this.lastMessageAt = Date.now();
    
            const bids = update?.b ?? update?.bids;
            const asks = update?.a ?? update?.asks;
            if (!Array.isArray(bids) || !Array.isArray(asks)) return;
            const receivedFirstDepth = this.lastDepthAt === 0;
            const receivedAt = Date.now();
            update.__receivedAt = receivedAt;
            this.lastDepthAt = receivedAt;
            const sourceEventTime = Number(update?.E);
            if (Number.isFinite(sourceEventTime)) {
              this.lastDepthEventTime = sourceEventTime;
              this.lastDepthSourceLagMs = calibratedSourceLag(sourceEventTime, receivedAt);
            }
            if (receivedFirstDepth) {
              diagnose(this.symbol, "depth.ws.first-message", {
                state: "received",
                generation,
                mode: this.mode,
                transport: this.transportIndex,
                transportName: transport.name,
              });
            }
            clearTimeout(this.firstDepthTimer);
            this.transportIndex = 0;
            this.depthReconnectAttempt = 0;
    
            if (this.mode === "partial") {
              this.partialBidKeys = this.replacePartial(this.bids, this.partialBidKeys, bids);
              this.partialAskKeys = this.replacePartial(this.asks, this.partialAskKeys, asks);
              this.lastUpdateId = Number(update.u ?? update.lastUpdateId) || this.lastUpdateId;
              this.depthReady = true;
              this.syncing = false;
              this.cachedSorted = null;
              this.publishLiveStatus();
              this.markDirty(true);
              return;
            }
    
            if (!Number.isFinite(Number(update?.U)) || !Number.isFinite(Number(update?.u))) return;
            if (!this.depthReady) {
              this.bufferDepth(update);
              if (!this.pendingSnapshot && !this.snapshotLoading) this.loadSnapshot(generation);
              this.installSnapshot();
              return;
            }
            this.applyDepth(update);
          } finally {
            this.recordFlow("depth", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.socket) return;
          clearTimeout(this.firstDepthTimer);
          this.socket = null;
          this.transportIndex += 1;
          diagnose(this.symbol, "depth.ws.close", {
            state: "closed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          const preserveLastFrame = this.depthReady || this.syncing;
          this.syncing = preserveLastFrame;
          this.resetBook("reconnect");
          this.setStatus(
            preserveLastFrame ? "stale" : "offline",
            preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "RECONNECT · WORKER",
          );
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.error", {
              state: "failed",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      connectTrades(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
    
        const streams = tradeStreams(this.symbol);
        const transports = tradeTransports(streams);
        const transport = transports[this.tradeTransportIndex % transports.length];
        const transportIndex = this.tradeTransportIndex % transports.length;
        diagnose(this.symbol, "tape.ws.create", {
          state: "started",
          generation,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "tape.ws.create", {
            state: "failed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.tradeTransportIndex += 1;
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
          return;
        }
        this.tradeSocket = socket;
        this.tradeTransportName = transport.name;
        let receivedTrade = false;
        this.tradeFirstMessageTimer = setTimeout(() => {
          if (generation !== this.generation || socket !== this.tradeSocket || receivedTrade) return;
          diagnose(this.symbol, "tape.ws.first-message", {
            state: "timeout",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            durationMs: TRADE_FIRST_MESSAGE_TIMEOUT_MS,
          });
          try { socket.close(); } catch {}
        }, TRADE_FIRST_MESSAGE_TIMEOUT_MS);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          diagnose(this.symbol, "tape.ws.open", {
            state: "succeeded",
            generation,
            transport: transportIndex,
            transportName: transport.name,
          });
          this.tapeGuard.connect();
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: transport.streams,
              id: Date.now() % 2_147_483_647,
            }));
          }
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            const eventType = String(update?.e ?? "").toLowerCase();
            const payloadStream = payload.stream.toLowerCase();
            const rawEvent = eventType === "trade" || payloadStream.endsWith("@trade");
            const aggregateEvent = eventType === "aggtrade" || payloadStream.endsWith("@aggtrade");
            if (!rawEvent && !aggregateEvent) return;
    
            const receivedAt = Date.now();
            const source = rawEvent ? "raw" : "agg";
            const trade = normalizeTrade(update, source, receivedAt);
            if (!trade) return;
            const firstTradeMessage = !receivedTrade;
            receivedTrade = true;
            clearTimeout(this.tradeFirstMessageTimer);
            this.tradeFirstMessageTimer = 0;
            this.lastTradeAt = receivedAt;
            this.lastTradeEventTime = trade.eventTime;
            this.lastTradeSourceLagMs = calibratedSourceLag(trade.eventTime, receivedAt);
            // RX in the header describes the stable production Tape feed. The parallel
            // experimental @trade channel must not double-weight or zero the rolling median.
            if (aggregateEvent) this.tradeLatency.record(trade.rxLatencyMs, receivedAt);
            this.tradeTransportIndex = 0;
            this.tradeReconnectAttempt = 0;
            this.tradeLive = true;
            this.tradeConnected = true;
            if (firstTradeMessage) {
              diagnose(this.symbol, "tape.ws.first-message", {
                state: "received",
                generation,
                transport: transportIndex,
                transportName: transport.name,
                source,
              });
            }
    
            const decision = this.tapeGuard.ingest(trade, this.lastTradeAt);
            let changed = false;
    
            // Preserve the visual RAW contract: it continues to receive only
            // Binance @aggTrade events, exactly as before this feature.
            if (aggregateEvent && this.insertTrade(trade, true)) {
              const matchedDensities = this.densityLifecycle.ingestTrades([trade]);
              if (matchedDensities.length) this.markDirty();
              this.queueTape(trade);
              changed = true;
            }
    
            // The guarded production path follows the same documented @aggTrade
            // stream and keeps continuity/dedup protection without mixing sources.
            if (decision.emit && this.insertAggregationTrade(trade, true)) {
              this.queueAggregationTape(trade);
              changed = true;
            }
    
            this.publishLiveStatus();
            if (!changed && rawEvent && decision.reason !== "raw-warmup") {
              diagnose(this.symbol, "tape.aggregation-source", {
                state: "skipped",
                source,
                reason: decision.reason,
                mode: decision.mode,
              });
            }
          } finally {
            this.recordFlow("trade", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          clearTimeout(this.tradeFirstMessageTimer);
          this.tradeFirstMessageTimer = 0;
          this.tradeSocket = null;
          this.tradeConnected = false;
          this.tapeGuard.disconnect("socket-close");
          this.tradeTransportIndex += 1;
          diagnose(this.symbol, "tape.ws.close", {
            state: "closed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          if (this.tradeLive && this.depthReady) this.publishLiveStatus("reconnect");
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.tradeSocket) {
            diagnose(this.symbol, "tape.ws.error", {
              state: "failed",
              generation,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      insertTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.tradeIds.has(key)) return false;
        this.tradeIds.add(key);
        const evicted = newestFirst
          ? this.trades.prepend(trade)
          : this.trades.append(trade);
        if (evicted === trade) {
          this.tradeIds.delete(key);
          return false;
        }
        if (evicted) this.tradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      insertAggregationTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.aggregationTradeIds.has(key)) return false;
        this.aggregationTradeIds.add(key);
        const evicted = newestFirst
          ? this.aggregationTrades.prepend(trade)
          : this.aggregationTrades.append(trade);
        if (evicted === trade) {
          this.aggregationTradeIds.delete(key);
          return false;
        }
        if (evicted) this.aggregationTradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      scheduleTapeFlush() {
        if (!this.tapeTimer) {
          this.tapeTimer = setTimeout(() => this.flushTapeBatch(), TAPE_FLUSH_MS);
        }
      }
    
      queueTape(trade) {
        if (!trade || !tabVisible) return;
        this.tapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      queueAggregationTape(trade) {
        if (!trade || !tabVisible) return;
        this.aggregationTapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      flushTapeBatch() {
        this.tapeTimer = 0;
        if (!tabVisible) {
          this.tapeBatch.clear();
          this.aggregationTapeBatch.clear();
          return;
        }
        const latest = this.tapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const aggregationLatest = this.aggregationTapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const trades = latest.items;
        const aggregationTrades = aggregationLatest.items;
        this.tapeDroppedInWindow += latest.dropped + aggregationLatest.dropped;
        if (trades.length || aggregationTrades.length) {
          const sourceEventTimeMs = [...trades, ...aggregationTrades].reduce(
            (latestTime, trade) => Math.max(latestTime, Number(trade?.eventTime) || 0),
            0,
          );
          const guard = this.tapeGuard.snapshot();
          post(
            "tape",
            this.symbol,
            {
              replace: false,
              live: true,
              liveOnly: true,
              trades,
              aggregationTrades,
              aggregationSource: guard.mode,
              aggregationHealth: guard,
              backpressure: {
                dropped: latest.dropped + aggregationLatest.dropped,
                pending: this.tapeBatch.length + this.aggregationTapeBatch.length,
              },
            },
            observabilityEnabled ? performance.now() : null,
            { sourceEventTimeMs, sourceKind: "live-trade-dual" },
          );
        }
      }
    
    }
    
    function scheduleEmit() {
      if (!tabVisible || emitTimer) return;
      emitTimer = setTimeout(() => {
        emitTimer = 0;
        if (!tabVisible) return;
    
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0);
        if (!active.length) return;
    
        // Не отправляем несколько тяжёлых книг в UI одним залпом.
        // При 3+ стаканах один тик обслуживает один символ по кругу.
        const budget = active.length <= 2 ? active.length : 1;
        const now = Date.now();
        for (let index = 0; index < budget; index += 1) {
          const feed = active[emitCursor % active.length];
          emitCursor = (emitCursor + 1) % active.length;
          feed.emit(now);
        }
    
        if (active.some((feed) => feed.dirty || feed.forceEmit)) scheduleEmit();
      }, 25);
    }
    
    function scheduleWatchdog() {
      clearTimeout(watchdogTimer);
      watchdogTimer = setTimeout(() => {
        watchdogTimer = 0;
        const now = Date.now();
        if (tabVisible) {
          for (const feed of feeds.values()) {
            if (feed.subscribers <= 0) continue;
            feed.reportFlow(now);
            feed.ensureHealthy(now);
          }
        }
        self.postMessage({
          type: "heartbeat",
          time: now,
          visible: tabVisible,
          activeFeeds: [...feeds.values()].filter((feed) => feed.subscribers > 0).length,
          totalFeeds: feeds.size,
        });
        scheduleWatchdog();
      }, WORKER_HEARTBEAT_MS);
    }
    
    function getFeed(symbol) {
      let feed = feeds.get(symbol);
      if (!feed) {
        feed = new SymbolFeed(symbol);
        feeds.set(symbol, feed);
      }
      return feed;
    }
    
    function cancelBackgroundPause() {
      clearTimeout(backgroundPauseTimer);
      backgroundPauseTimer = 0;
    }
    
    function scheduleBackgroundPause(epoch) {
      cancelBackgroundPause();
      backgroundPauseTimer = setTimeout(() => {
        backgroundPauseTimer = 0;
        if (tabVisible || epoch !== visibilityEpoch) return;
        for (const feed of feeds.values()) feed.pauseForBackground();
      }, BACKGROUND_GRACE_MS);
    }
    
    self.addEventListener("message", (event) => {
      const message = event.data;
      if (!message || typeof message !== "object") return;
    
      if (message.type === "observability") {
        observabilityEnabled = Boolean(message.enabled);
        return;
      }
    
      if (message.type === "visibility") {
        const nextVisible = Boolean(message.visible);
        if (nextVisible === tabVisible) return;
    
        tabVisible = nextVisible;
        visibilityEpoch += 1;
        const epoch = visibilityEpoch;
    
        if (!tabVisible) {
          clearTimeout(emitTimer);
          emitTimer = 0;
          scheduleBackgroundPause(epoch);
          return;
        }
    
        cancelBackgroundPause();
        syncServerClock(true).catch(() => {});
    
        const resumePrioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        prioritySymbols = resumePrioritySymbols;
        const priorityRank = new Map(
          resumePrioritySymbols.map((symbol, index) => [symbol, index]),
        );
        const active = [...feeds.values()]
          .filter((feed) => feed.subscribers > 0)
          .sort((left, right) => (
            (priorityRank.get(left.symbol) ?? Number.MAX_SAFE_INTEGER)
            - (priorityRank.get(right.symbol) ?? Number.MAX_SAFE_INTEGER)
          ));
        active.forEach((feed, index) => feed.resume(index * RESUME_STAGGER_MS, epoch));
        return;
      }
    
      if (message.type === "priority") {
        prioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        return;
      }
    
      const symbol = String(message.symbol ?? "").toUpperCase();
      if (!symbol.endsWith("USDT")) return;
      if (message.type === "subscribe") {
        getFeed(symbol).addSubscriber();
        return;
      }
      if (message.type === "unsubscribe") {
        feeds.get(symbol)?.removeSubscriber();
        return;
      }
      if (message.type === "refresh") {
        feeds.get(symbol)?.refresh();
      }
    });
    
    syncServerClock(true).catch(() => {});
    scheduleWatchdog();
    post("ready", "");
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/InPuls/InPuls/test-orderbook-guarded-raw-tape.mjs:109:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: manual orderbook scroll remains authoritative
ok 38 - manual orderbook scroll remains authoritative
  ---
  duration_ms: 3.526684
  type: 'test'
  ...
# Subtest: Ctrl+wheel changes a fixed 1/2/5 price-step multiplier
ok 39 - Ctrl+wheel changes a fixed 1/2/5 price-step multiplier
  ---
  duration_ms: 1.028635
  type: 'test'
  ...
# Subtest: short background switches use a grace period
ok 40 - short background switches use a grace period
  ---
  duration_ms: 1.456499
  type: 'test'
  ...
# Subtest: healthy sockets resume without REST backfill
ok 41 - healthy sockets resume without REST backfill
  ---
  duration_ms: 0.856381
  type: 'test'
  ...
# Subtest: long background keeps a frozen Tape frame
ok 42 - long background keeps a frozen Tape frame
  ---
  duration_ms: 0.495187
  type: 'test'
  ...
# Subtest: Tape clock advances continuously while the live stream is healthy
ok 43 - Tape clock advances continuously while the live stream is healthy
  ---
  duration_ms: 0.394089
  type: 'test'
  ...
# Subtest: footprint preserves its canvas while the feed recovers
ok 44 - footprint preserves its canvas while the feed recovers
  ---
  duration_ms: 1.824147
  type: 'test'
  ...
# Subtest: a delayed feed retries independently
ok 45 - a delayed feed retries independently
  ---
  duration_ms: 0.371913
  type: 'test'
  ...
# Subtest: Resume v2 ships one consistent runtime
ok 46 - Resume v2 ships one consistent runtime
  ---
  duration_ms: 0.249598
  type: 'test'
  ...
# Subtest: manager hard restart preserves frozen symbol frames
ok 47 - manager hard restart preserves frozen symbol frames
  ---
  duration_ms: 0.399509
  type: 'test'
  ...
# Subtest: normal reload keeps one consistent runtime build
ok 48 - normal reload keeps one consistent runtime build
  ---
  duration_ms: 1.426251
  type: 'test'
  ...
# Subtest: worker-unavailable fallback does not leave a health interval open
ok 49 - worker-unavailable fallback does not leave a health interval open
  ---
  duration_ms: 0.387279
  type: 'test'
  ...
# Subtest: hidden tabs close sockets instead of accumulating a stale queue
ok 50 - hidden tabs close sockets instead of accumulating a stale queue
  ---
  duration_ms: 0.432261
  type: 'test'
  ...
# Subtest: workspace order is footprint then Tape then book
ok 51 - workspace order is footprint then Tape then book
  ---
  duration_ms: 0.318595
  type: 'test'
  ...
# Subtest: Tape and footprint visibility controls stay independent
ok 52 - Tape and footprint visibility controls stay independent
  ---
  duration_ms: 0.466953
  type: 'test'
  ...
# Subtest: RX uses calibrated Binance server time
ok 53 - RX uses calibrated Binance server time
  ---
  duration_ms: 0.336029
  type: 'test'
  ...
# Subtest: multi-book worker prioritizes UI without truncating local depth
ok 54 - multi-book worker prioritizes UI without truncating local depth
  ---
  duration_ms: 0.634509
  type: 'test'
  ...
# Subtest: background return reuses the existing worker before a guarded restart
ok 55 - background return reuses the existing worker before a guarded restart
  ---
  duration_ms: 1.516098
  type: 'test'
  ...
# Subtest: last rendered book remains visible while strict resync happens
ok 56 - last rendered book remains visible while strict resync happens
  ---
  duration_ms: 0.567765
  type: 'test'
  ...
# Subtest: resume prioritizes the last selected symbol and staggers other feeds
ok 57 - resume prioritizes the last selected symbol and staggers other feeds
  ---
  duration_ms: 0.268082
  type: 'test'
  ...
# Subtest: every resume starts a clean live-only Tape without REST history
ok 58 - every resume starts a clean live-only Tape without REST history
  ---
  duration_ms: 0.695556
  type: 'test'
  ...
# Subtest: cache versions keep seamless resume while shipping Resume v2
ok 59 - cache versions keep seamless resume while shipping Resume v2
  ---
  duration_ms: 0.400601
  type: 'test'
  ...
# Subtest: latency helper keeps exchange and browser timestamps separate
ok 60 - latency helper keeps exchange and browser timestamps separate
  ---
  duration_ms: 1.729366
  type: 'test'
  ...
# Subtest: REST timing stays without a fake receive timestamp
ok 61 - REST timing stays without a fake receive timestamp
  ---
  duration_ms: 0.380616
  type: 'test'
  ...
# Subtest: latency helper rejects clock outliers
ok 62 - latency helper rejects clock outliers
  ---
  duration_ms: 0.171407
  type: 'test'
  ...
# Subtest: RX display is a rolling short-window median
ok 63 - RX display is a rolling short-window median
  ---
  duration_ms: 0.505258
  type: 'test'
  ...
# Subtest: worker records receive time and exposes RX in live status
ok 64 - worker records receive time and exposes RX in live status
  ---
  duration_ms: 0.557937
  type: 'test'
  ...
# Subtest: UI preserves timing fields for future hover and replay
ok 65 - UI preserves timing fields for future hover and replay
  ---
  duration_ms: 0.469149
  type: 'test'
  ...
# Subtest: Tape keeps RAW default and AGG/SERIES explicit
ok 66 - Tape keeps RAW default and AGG/SERIES explicit
  ---
  duration_ms: 1.379802
  type: 'test'
  ...
# Subtest: water renderer owns stable event geometry
ok 67 - water renderer owns stable event geometry
  ---
  duration_ms: 1.121002
  type: 'test'
  ...
# Subtest: Flow Workspace cache and reset page point to water runtime
ok 68 - Flow Workspace cache and reset page point to water runtime
  ---
  duration_ms: 0.256191
  type: 'test'
  ...
# Subtest: production TAPE accepts only live packets and starts from an empty frame
ok 69 - production TAPE accepts only live packets and starts from an empty frame
  ---
  duration_ms: 1.136672
  type: 'test'
  ...
# Subtest: sparse trades stay on real time
ok 70 - sparse trades stay on real time
  ---
  duration_ms: 1.626878
  type: 'test'
  ...
# Subtest: same-time packet remains local and chronological
ok 71 - same-time packet remains local and chronological
  ---
  duration_ms: 5.706469
  type: 'test'
  ...
# Subtest: continuous flow is split into local groups
ok 72 - continuous flow is split into local groups
  ---
  duration_ms: 1.112772
  type: 'test'
  ...
# Subtest: past trades never receive density-dependent vertical offsets
ok 73 - past trades never receive density-dependent vertical offsets
  ---
  duration_ms: 0.28004
  type: 'test'
  ...
# Subtest: neighboring dense groups never cross chronological order
ok 74 - neighboring dense groups never cross chronological order
  ---
  duration_ms: 0.37835
  type: 'test'
  ...
# Subtest: dense raw dots shrink while large sparse dots stay visible
ok 75 - dense raw dots shrink while large sparse dots stay visible
  ---
  duration_ms: 0.208648
  type: 'test'
  ...
# Subtest: agg labels are selective and non-overlapping
ok 76 - agg labels are selective and non-overlapping
  ---
  duration_ms: 0.495909
  type: 'test'
  ...
# Subtest: AGG label uses the price-range midpoint
ok 77 - AGG label uses the price-range midpoint
  ---
  duration_ms: 1.179445
  type: 'test'
  ...
# Subtest: same-millisecond AGG groups receive stable non-overlapping ordinals
ok 78 - same-millisecond AGG groups receive stable non-overlapping ordinals
  ---
  duration_ms: 3.028903
  type: 'test'
  ...
# Subtest: tape time scale supports close flow and long history
ok 79 - tape time scale supports close flow and long history
  ---
  duration_ms: 0.290509
  type: 'test'
  ...
# Subtest: settings expose 80-200 font scale and verified shortcut sections only
ok 80 - settings expose 80-200 font scale and verified shortcut sections only
  ---
  duration_ms: 1.584671
  type: 'test'
  ...
# Subtest: clusters use the brighter dominance fill
ok 81 - clusters use the brighter dominance fill
  ---
  duration_ms: 0.413694
  type: 'test'
  ...
# Subtest: narrow books give unused price space back to sizes and mark the full current level
ok 82 - narrow books give unused price space back to sizes and mark the full current level
  ---
  duration_ms: 1.585509
  type: 'test'
  ...
# Subtest: ordinary sizes are neutral and full-book anomalies have three tiers
ok 83 - ordinary sizes are neutral and full-book anomalies have three tiers
  ---
  duration_ms: 1.579444
  type: 'test'
  ...
# Subtest: size labels stay readable and hover shows distance from current price
ok 84 - size labels stay readable and hover shows distance from current price
  ---
  duration_ms: 1.443565
  type: 'test'
  ...
# Subtest: footprint uses one proportional dominance cell and interval candles
ok 85 - footprint uses one proportional dominance cell and interval candles
  ---
  duration_ms: 0.631654
  type: 'test'
  ...
# Subtest: cluster history is compact, pannable and follows the brightness palette
ok 86 - cluster history is compact, pannable and follows the brightness palette
  ---
  duration_ms: 0.58231
  type: 'test'
  ...
# Subtest: Tape paints the active panel theme instead of a black canvas
ok 87 - Tape paints the active panel theme instead of a black canvas
  ---
  duration_ms: 0.279539
  type: 'test'
  ...
# Subtest: only full round prices affect text and the liquidity meter stays readable
ok 88 - only full round prices affect text and the liquidity meter stays readable
  ---
  duration_ms: 0.908255
  type: 'test'
  ...
# Subtest: live flow invalidates footprint while Tape keeps one coherent viewport
ok 89 - live flow invalidates footprint while Tape keeps one coherent viewport
  ---
  duration_ms: 0.770297
  type: 'test'
  ...
# Subtest: trade count is not shown above Tape
ok 90 - trade count is not shown above Tape
  ---
  duration_ms: 0.477054
  type: 'test'
  ...
# Subtest: visual priority ships one consistent runtime
ok 91 - visual priority ships one consistent runtime
  ---
  duration_ms: 1.482893
  type: 'test'
  ...
# Subtest: parallel chart phases spread canvas work across the interval
ok 92 - parallel chart phases spread canvas work across the interval
  ---
  duration_ms: 0.793579
  type: 'test'
  ...
# Subtest: extra charts use phased updates while the primary chart remains unrestricted
ok 93 - extra charts use phased updates while the primary chart remains unrestricted
  ---
  duration_ms: 1.667943
  type: 'test'
  ...
# Subtest: symbol lists are normalized, deduplicated and capped
ok 94 - symbol lists are normalized, deduplicated and capped
  ---
  duration_ms: 1.578939
  type: 'test'
  ...
# Subtest: RAW sequence gaps are counted only after the segment anchor
ok 95 - RAW sequence gaps are counted only after the segment anchor
  ---
  duration_ms: 0.302579
  type: 'test'
  ...
# Subtest: rejected trade payloads keep a usable sequence identity and an exact reason
ok 96 - rejected trade payloads keep a usable sequence identity and an exact reason
  ---
  duration_ms: 0.255267
  type: 'test'
  ...
# Subtest: zero-price zero-quantity RAW events are accepted only as sequence markers
ok 97 - zero-price zero-quantity RAW events are accepted only as sequence markers
  ---
  duration_ms: 0.327953
  type: 'test'
  ...
# Subtest: sequence markers complete aggregate ID coverage without adding executed volume
ok 98 - sequence markers complete aggregate ID coverage without adding executed volume
  ---
  duration_ms: 0.259741
  type: 'test'
  ...
# Subtest: invalid payload diagnostics are bounded to public market fields
ok 99 - invalid payload diagnostics are bounded to public market fields
  ---
  duration_ms: 0.194035
  type: 'test'
  ...
# Subtest: source-only stalls require continued counterpart activity after market silence
ok 100 - source-only stalls require continued counterpart activity after market silence
  ---
  duration_ms: 0.255501
  type: 'test'
  ...
# Subtest: aggregate sequence uses the complete f-l coverage range
ok 101 - aggregate sequence uses the complete f-l coverage range
  ---
  duration_ms: 0.178225
  type: 'test'
  ...
# Subtest: reconnect backoff is bounded and includes controlled jitter
ok 102 - reconnect backoff is bounded and includes controlled jitter
  ---
  duration_ms: 0.277477
  type: 'test'
  ...
# Subtest: reservoir keeps exact seen count while bounding retained values
ok 103 - reservoir keeps exact seen count while bounding retained values
  ---
  duration_ms: 0.400148
  type: 'test'
  ...
# Subtest: assessment never promotes an incomplete or short run
ok 104 - assessment never promotes an incomplete or short run
  ---
  duration_ms: 0.425099
  type: 'test'
  ...
# Subtest: assessment blocks RAW gaps, stalls and incomplete group coverage
ok 105 - assessment blocks RAW gaps, stalls and incomplete group coverage
  ---
  duration_ms: 0.124501
  type: 'test'
  ...
# Subtest: assessment blocks fast unplanned reconnects and unfinished recovery
ok 106 - assessment blocks fast unplanned reconnects and unfinished recovery
  ---
  duration_ms: 0.111048
  type: 'test'
  ...
# Subtest: clean assessment is explicitly one-run evidence, not production promotion
ok 107 - clean assessment is explicitly one-run evidence, not production promotion
  ---
  duration_ms: 0.169712
  type: 'test'
  ...
# Subtest: matching summary keeps exact totals and sampled distributions
ok 108 - matching summary keeps exact totals and sampled distributions
  ---
  duration_ms: 0.437175
  type: 'test'
  ...
# Subtest: browser lab stays isolated while production adds a guarded raw AGG channel
not ok 109 - browser lab stays isolated while production adds a guarded raw AGG channel
  ---
  duration_ms: 2.914946
  type: 'test'
  location: '/home/runner/work/InPuls/InPuls/test-raw-stability-core.mjs:304:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /return \[`\$\{name\}@aggTrade`, `\$\{name\}@trade`\];/. Input:
    
    'importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-network.js?v=obs-pr1-1");\n' +
      'importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-depth-projection.js?v=deep-book-v1");\n' +
      'importScripts("./orderbook-events.js?v=orderbook-events-core-v1");\n' +
      'importScripts("./orderbook-density.js?v=density-trades-correlation-v1");\n' +
      '\n' +
      'const MAX_BOOK_LEVELS_PER_SIDE = 20_000;\n' +
      'const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;\n' +
      'const MAX_BUFFERED_DEPTH_EVENTS = 4_000;\n' +
      'const MAX_TRADE_HISTORY = 12_000;\n' +
      'const MAX_RESUME_LEVELS_PER_SIDE = 700;\n' +
      'const RESUME_STAGGER_MS = 180;\n' +
      'const BACKGROUND_GRACE_MS = 2_000;\n' +
      'const RECOVERY_TIMEOUT_MS = 8_000;\n' +
      'const RESUME_STALE_MS = 3_500;\n' +
      'const DEPTH_STALE_NOTICE_MS = 3_000;\n' +
      'const ACTIVE_STALE_MS = 9_000;\n' +
      'const SNAPSHOT_TIMEOUT_MS = 2_800;\n' +
      'const IDLE_CLOSE_MS = 10_000;\n' +
      'const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;\n' +
      'const MAX_TAPE_BATCH_PER_POST = 500;\n' +
      'const MAX_PENDING_TAPE_TRADES = 800;\n' +
      'const TAPE_FLUSH_MS = 25;\n' +
      'const ACTIVE_SOURCE_LAG_MS = 4_000;\n' +
      'const TRADE_ACTIVE_STALE_MS = 30_000;\n' +
      'const RECONNECT_BASE_MS = 400;\n' +
      'const RECONNECT_MAX_MS = 8_000;\n' +
      'const WORKER_HEARTBEAT_MS = 2_000;\n' +
      'const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;\n' +
      '\n' +
      'const feeds = new Map();\n' +
      'let tabVisible = true;\n' +
      'let emitTimer = 0;\n' +
      'let emitCursor = 0;\n' +
      'let visibilityEpoch = 0;\n' +
      'let backgroundPauseTimer = 0;\n' +
      'let watchdogTimer = 0;\n' +
      'let prioritySymbols = [];\n' +
      'let serverClockOffsetMs = null;\n' +
      'let serverClockRttMs = null;\n' +
      'let serverClockSyncAt = 0;\n' +
      'let serverClockSyncPromise = null;\n' +
      'let observabilityEnabled = false;\n' +
      'let observabilityPostCount = 0;\n' +
      '\n' +
      'function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {\n' +
      '  if (!observabilityEnabled) {\n' +
      '    self.postMessage({ type, symbol, ...payload });\n' +
      '    return;\n' +
      '  }\n' +
      '  const observerStartedAt = performance.now();\n' +
      '  const message = { type, symbol, ...payload };\n' +
      '  const samplePayload = observabilityPostCount++ % 50 === 0;\n' +
      '  let payloadBytes = null;\n' +
      '  if (samplePayload) {\n' +
      '    try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}\n' +
      '  }\n' +
      '  const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);\n' +
      '  message.__obs = {\n' +
      '    sentAtEpochMs: Date.now(),\n' +
      '    processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,\n' +
      '    observerOverheadMs: performance.now() - observerStartedAt,\n' +
      '    payloadBytes,\n' +
      '    payloadSampleRate: 50,\n' +
      '    sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,\n' +
      '    sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,\n' +
      '    sourceKind: obsContext.sourceKind ?? null,\n' +
      '  };\n' +
      '  self.postMessage(message);\n' +
      '}\n' +
      '\n' +
      'function diagnose(symbol, phase, details = {}) {\n' +
      '  if (!observabilityEnabled) return;\n' +
      '  post("diagnostic", symbol, {\n' +
      '    diagnostic: {\n' +
      '      phase,\n' +
      '      atEpochMs: Date.now(),\n' +
      '      ...details,\n' +
      '    },\n' +
      '  });\n' +
      '}\n' +
      '\n' +
      'function reconnectDelay(attempt = 0) {\n' +
      '  const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));\n' +
      '  const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));\n' +
      '  return exponential + jitter;\n' +
      '}\n' +
      '\n' +
      'function bookSideQuoteScale(levels, sampleLimit = 1_024) {\n' +
      '  const totalLevels = Number.isFinite(levels?.size)\n' +
      '    ? levels.size\n' +
      '    : (Number(levels?.length) || 0);\n' +
      '  const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));\n' +
      '  const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));\n' +
      '  const sample = [];\n' +
      '  let maximum = 1;\n' +
      '  let validIndex = 0;\n' +
      '  for (const [priceValue, quantityValue] of levels ?? []) {\n' +
      '    const quote = Number(priceValue) * Number(quantityValue);\n' +
      '    if (!Number.isFinite(quote) || quote <= 0) continue;\n' +
      '    if (quote > maximum) maximum = quote;\n' +
      '    if (validIndex % sampleStride === 0) sample.push(quote);\n' +
      '    validIndex += 1;\n' +
      '  }\n' +
      '  sample.sort((left, right) => left - right);\n' +
      '  const quantile = (ratio) => sample.length\n' +
      '    ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]\n' +
      '    : 0;\n' +
      '  const median = quantile(.5);\n' +
      '  const upper = quantile(.95);\n' +
      '  const extreme = quantile(.99);\n' +
      '  return {\n' +
      '    maximum,\n' +
      '    anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),\n' +
      '    sampledLevels: sample.length,\n' +
      '    totalLevels: validIndex,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function bookQuoteScale(bids, asks, sampleLimit = 2_048) {\n' +
      '  const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));\n' +
      '  const bid = bookSideQuoteScale(bids ?? [], sideLimit);\n' +
      '  const ask = bookSideQuoteScale(asks ?? [], sideLimit);\n' +
      '  return {\n' +
      '    maximum: Math.max(bid.maximum, ask.maximum),\n' +
      '    anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),\n' +
      '    bidAnomalyThreshold: bid.anomalyThreshold,\n' +
      '    askAnomalyThreshold: ask.anomalyThreshold,\n' +
      '    sampledLevels: bid.sampledLevels + ask.sampledLevels,\n' +
      '    totalLevels: bid.totalLevels + ask.totalLevels,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function parsePayload(raw) {\n' +
      '  let payload;\n' +
      '  try { payload = JSON.parse(raw); } catch { return null; }\n' +
      '  if (payload?.result === null || payload?.id) return null;\n' +
      '  return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };\n' +
      '}\n' +
      '\n' +
      'function normalizeTrade(event, sourceHint = null, receivedAt = null) {\n' +
      '  const eventType = String(event?.e ?? "").toLowerCase();\n' +
      '  const inferredRaw = eventType === "trade"\n' +
      '    || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));\n' +
      '  const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";\n' +
      '  const price = Number(event?.p);\n' +
      '  const quantity = Number(event?.q);\n' +
      '  const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);\n' +
      '  const time = timing.tradeTime;\n' +
      '  const id = source === "raw" ? Number(event?.t) : Number(event?.a);\n' +
      '  const firstTradeId = source === "raw" ? id : Number(event?.f);\n' +
      '  const lastTradeId = source === "raw" ? id : Number(event?.l);\n' +
      '  if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;\n' +
      '  if (price <= 0 || quantity <= 0 || time <= 0) return null;\n' +
      '  if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;\n' +
      '  return {\n' +
      '    id,\n' +
      '    firstTradeId,\n' +
      '    lastTradeId,\n' +
      '    source,\n' +
      '    price,\n' +
      '    quantity,\n' +
      '    quote: price * quantity,\n' +
      '    time,\n' +
      '    tradeTime: timing.tradeTime,\n' +
      '    eventTime: timing.eventTime,\n' +
      '    receivedAt: timing.receivedAt,\n' +
      '    rxLatencyMs: timing.rxLatencyMs,\n' +
      '    side: event?.m ? "sell" : "buy",\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function calibratedSourceLag(eventTime, receivedAt = Date.now()) {\n' +
      '  const sourceTime = Number(eventTime);\n' +
      '  const localTime = Number(receivedAt);\n' +
      '  if (\n' +
      '    !Number.isFinite(sourceTime)\n' +
      '    || !Number.isFinite(localTime)\n' +
      '    || !Number.isFinite(serverClockOffsetMs)\n' +
      '  ) return null;\n' +
      '  const lag = localTime + serverClockOffsetMs - sourceTime;\n' +
      '  if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;\n' +
      '  return Math.max(0, lag);\n' +
      '}\n' +
      '\n' +
      'function sequenceDecision(lastUpdateId, event, firstEvent = false) {\n' +
      '  const first = Number(event?.U);\n' +
      '  const final = Number(event?.u);\n' +
      '  const previous = Number(event?.pu);\n' +
      '  const local = Number(lastUpdateId);\n' +
      '  if (![first, final, local].every(Number.isFinite)) return "resync";\n' +
      '  if (final <= local) return "ignore";\n' +
      '  if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";\n' +
      '  if (Number.isFinite(previous) && previous !== local) return "resync";\n' +
      '  if (!Number.isFinite(previous) && first > local + 1) return "resync";\n' +
      '  return "apply";\n' +
      '}\n' +
      '\n' +
      'async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {\n' +
      '  const controller = typeof AbortController === "function" ? new AbortController() : null;\n' +
      '  let timer;\n' +
      '  const abortFromExternal = () => controller?.abort();\n' +
      '  if (externalSignal?.aborted) abortFromExternal();\n' +
      '  else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });\n' +
      '  try {\n' +
      '    const timeout = new Promise((_, reject) => {\n' +
      '      timer = setTimeout(() => {\n' +
      '        controller?.abort();\n' +
      '        reject(new Error("timeout"));\n' +
      '      }, timeoutMs);\n' +
      '    });\n' +
      '    const request = fetch(url, {\n' +
      '      cache: "no-store",\n' +
      '      ...(controller ? { signal: controller.signal } : {}),\n' +
      '    }).then(async (response) => {\n' +
      '      if (!response.ok) throw new Error(`HTTP ${response.status}`);\n' +
      '      return response.json();\n' +
      '    });\n' +
      '    return await Promise.race([request, timeout]);\n' +
      '  } finally {\n' +
      '    clearTimeout(timer);\n' +
      '    externalSignal?.removeEventListener?.("abort", abortFromExternal);\n' +
      '  }\n' +
      '}\n' +
      '\n' +
      'async function syncServerClock(force = false) {\n' +
      '  const now = Date.now();\n' +
      '  if (\n' +
      '    !force\n' +
      '    && Number.isFinite(serverClockOffsetMs)\n' +
      '    && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS\n' +
      '  ) return serverClockOffsetMs;\n' +
      '  if (serverClockSyncPromise) return serverClockSyncPromise;\n' +
      '\n' +
      '  serverClockSyncPromise = (async () => {\n' +
      '    const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];\n' +
      '    diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });\n' +
      '    let result = null;\n' +
      '    try {\n' +
      '      result = await self.InPulsOrderBookNetwork.firstSuccessful(\n' +
      '        hosts,\n' +
      '        async (host, { signal }) => {\n' +
      '          const startedAt = Date.now();\n' +
      '          const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);\n' +
      '          const finishedAt = Date.now();\n' +
      '          const serverTime = Number(data?.serverTime);\n' +
      '          if (!Number.isFinite(serverTime)) throw new Error("invalid server time");\n' +
      '          return {\n' +
      '            rtt: Math.max(0, finishedAt - startedAt),\n' +
      '            offset: serverTime - (startedAt + finishedAt) / 2,\n' +
      '          };\n' +
      '        },\n' +
      '        {\n' +
      '          onAttempt: (event) => diagnose("", "clock.rest.host", {\n' +
      '            ...event,\n' +
      '            host: event.target,\n' +
      '            target: undefined,\n' +
      '          }),\n' +
      '        },\n' +
      '      );\n' +
      '    } catch {}\n' +
      '    const best = result?.value;\n' +
      '    if (best) {\n' +
      '      serverClockOffsetMs = best.offset;\n' +
      '      serverClockRttMs = best.rtt;\n' +
      '      serverClockSyncAt = Date.now();\n' +
      '      '... 50929 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");
    importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");
    importScripts("./orderbook-network.js?v=obs-pr1-1");
    importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");
    importScripts("./orderbook-depth-projection.js?v=deep-book-v1");
    importScripts("./orderbook-events.js?v=orderbook-events-core-v1");
    importScripts("./orderbook-density.js?v=density-trades-correlation-v1");
    
    const MAX_BOOK_LEVELS_PER_SIDE = 20_000;
    const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;
    const MAX_BUFFERED_DEPTH_EVENTS = 4_000;
    const MAX_TRADE_HISTORY = 12_000;
    const MAX_RESUME_LEVELS_PER_SIDE = 700;
    const RESUME_STAGGER_MS = 180;
    const BACKGROUND_GRACE_MS = 2_000;
    const RECOVERY_TIMEOUT_MS = 8_000;
    const RESUME_STALE_MS = 3_500;
    const DEPTH_STALE_NOTICE_MS = 3_000;
    const ACTIVE_STALE_MS = 9_000;
    const SNAPSHOT_TIMEOUT_MS = 2_800;
    const IDLE_CLOSE_MS = 10_000;
    const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;
    const MAX_TAPE_BATCH_PER_POST = 500;
    const MAX_PENDING_TAPE_TRADES = 800;
    const TAPE_FLUSH_MS = 25;
    const ACTIVE_SOURCE_LAG_MS = 4_000;
    const TRADE_ACTIVE_STALE_MS = 30_000;
    const RECONNECT_BASE_MS = 400;
    const RECONNECT_MAX_MS = 8_000;
    const WORKER_HEARTBEAT_MS = 2_000;
    const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;
    
    const feeds = new Map();
    let tabVisible = true;
    let emitTimer = 0;
    let emitCursor = 0;
    let visibilityEpoch = 0;
    let backgroundPauseTimer = 0;
    let watchdogTimer = 0;
    let prioritySymbols = [];
    let serverClockOffsetMs = null;
    let serverClockRttMs = null;
    let serverClockSyncAt = 0;
    let serverClockSyncPromise = null;
    let observabilityEnabled = false;
    let observabilityPostCount = 0;
    
    function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {
      if (!observabilityEnabled) {
        self.postMessage({ type, symbol, ...payload });
        return;
      }
      const observerStartedAt = performance.now();
      const message = { type, symbol, ...payload };
      const samplePayload = observabilityPostCount++ % 50 === 0;
      let payloadBytes = null;
      if (samplePayload) {
        try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}
      }
      const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);
      message.__obs = {
        sentAtEpochMs: Date.now(),
        processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,
        observerOverheadMs: performance.now() - observerStartedAt,
        payloadBytes,
        payloadSampleRate: 50,
        sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,
        sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,
        sourceKind: obsContext.sourceKind ?? null,
      };
      self.postMessage(message);
    }
    
    function diagnose(symbol, phase, details = {}) {
      if (!observabilityEnabled) return;
      post("diagnostic", symbol, {
        diagnostic: {
          phase,
          atEpochMs: Date.now(),
          ...details,
        },
      });
    }
    
    function reconnectDelay(attempt = 0) {
      const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));
      const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));
      return exponential + jitter;
    }
    
    function bookSideQuoteScale(levels, sampleLimit = 1_024) {
      const totalLevels = Number.isFinite(levels?.size)
        ? levels.size
        : (Number(levels?.length) || 0);
      const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));
      const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));
      const sample = [];
      let maximum = 1;
      let validIndex = 0;
      for (const [priceValue, quantityValue] of levels ?? []) {
        const quote = Number(priceValue) * Number(quantityValue);
        if (!Number.isFinite(quote) || quote <= 0) continue;
        if (quote > maximum) maximum = quote;
        if (validIndex % sampleStride === 0) sample.push(quote);
        validIndex += 1;
      }
      sample.sort((left, right) => left - right);
      const quantile = (ratio) => sample.length
        ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]
        : 0;
      const median = quantile(.5);
      const upper = quantile(.95);
      const extreme = quantile(.99);
      return {
        maximum,
        anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),
        sampledLevels: sample.length,
        totalLevels: validIndex,
      };
    }
    
    function bookQuoteScale(bids, asks, sampleLimit = 2_048) {
      const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));
      const bid = bookSideQuoteScale(bids ?? [], sideLimit);
      const ask = bookSideQuoteScale(asks ?? [], sideLimit);
      return {
        maximum: Math.max(bid.maximum, ask.maximum),
        anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),
        bidAnomalyThreshold: bid.anomalyThreshold,
        askAnomalyThreshold: ask.anomalyThreshold,
        sampledLevels: bid.sampledLevels + ask.sampledLevels,
        totalLevels: bid.totalLevels + ask.totalLevels,
      };
    }
    
    function parsePayload(raw) {
      let payload;
      try { payload = JSON.parse(raw); } catch { return null; }
      if (payload?.result === null || payload?.id) return null;
      return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };
    }
    
    function normalizeTrade(event, sourceHint = null, receivedAt = null) {
      const eventType = String(event?.e ?? "").toLowerCase();
      const inferredRaw = eventType === "trade"
        || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));
      const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";
      const price = Number(event?.p);
      const quantity = Number(event?.q);
      const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);
      const time = timing.tradeTime;
      const id = source === "raw" ? Number(event?.t) : Number(event?.a);
      const firstTradeId = source === "raw" ? id : Number(event?.f);
      const lastTradeId = source === "raw" ? id : Number(event?.l);
      if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;
      if (price <= 0 || quantity <= 0 || time <= 0) return null;
      if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;
      return {
        id,
        firstTradeId,
        lastTradeId,
        source,
        price,
        quantity,
        quote: price * quantity,
        time,
        tradeTime: timing.tradeTime,
        eventTime: timing.eventTime,
        receivedAt: timing.receivedAt,
        rxLatencyMs: timing.rxLatencyMs,
        side: event?.m ? "sell" : "buy",
      };
    }
    
    function calibratedSourceLag(eventTime, receivedAt = Date.now()) {
      const sourceTime = Number(eventTime);
      const localTime = Number(receivedAt);
      if (
        !Number.isFinite(sourceTime)
        || !Number.isFinite(localTime)
        || !Number.isFinite(serverClockOffsetMs)
      ) return null;
      const lag = localTime + serverClockOffsetMs - sourceTime;
      if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;
      return Math.max(0, lag);
    }
    
    function sequenceDecision(lastUpdateId, event, firstEvent = false) {
      const first = Number(event?.U);
      const final = Number(event?.u);
      const previous = Number(event?.pu);
      const local = Number(lastUpdateId);
      if (![first, final, local].every(Number.isFinite)) return "resync";
      if (final <= local) return "ignore";
      if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";
      if (Number.isFinite(previous) && previous !== local) return "resync";
      if (!Number.isFinite(previous) && first > local + 1) return "resync";
      return "apply";
    }
    
    async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {
      const controller = typeof AbortController === "function" ? new AbortController() : null;
      let timer;
      const abortFromExternal = () => controller?.abort();
      if (externalSignal?.aborted) abortFromExternal();
      else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });
      try {
        const timeout = new Promise((_, reject) => {
          timer = setTimeout(() => {
            controller?.abort();
            reject(new Error("timeout"));
          }, timeoutMs);
        });
        const request = fetch(url, {
          cache: "no-store",
          ...(controller ? { signal: controller.signal } : {}),
        }).then(async (response) => {
          if (!response.ok) throw new Error(`HTTP ${response.status}`);
          return response.json();
        });
        return await Promise.race([request, timeout]);
      } finally {
        clearTimeout(timer);
        externalSignal?.removeEventListener?.("abort", abortFromExternal);
      }
    }
    
    async function syncServerClock(force = false) {
      const now = Date.now();
      if (
        !force
        && Number.isFinite(serverClockOffsetMs)
        && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS
      ) return serverClockOffsetMs;
      if (serverClockSyncPromise) return serverClockSyncPromise;
    
      serverClockSyncPromise = (async () => {
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });
        let result = null;
        try {
          result = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const startedAt = Date.now();
              const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);
              const finishedAt = Date.now();
              const serverTime = Number(data?.serverTime);
              if (!Number.isFinite(serverTime)) throw new Error("invalid server time");
              return {
                rtt: Math.max(0, finishedAt - startedAt),
                offset: serverTime - (startedAt + finishedAt) / 2,
              };
            },
            {
              onAttempt: (event) => diagnose("", "clock.rest.host", {
                ...event,
                host: event.target,
                target: undefined,
              }),
            },
          );
        } catch {}
        const best = result?.value;
        if (best) {
          serverClockOffsetMs = best.offset;
          serverClockRttMs = best.rtt;
          serverClockSyncAt = Date.now();
          diagnose("", "clock.rest", {
            state: "succeeded",
            host: result.target,
            durationMs: result.durationMs,
            rttMs: best.rtt,
          });
        } else {
          diagnose("", "clock.rest", { state: "failed" });
        }
        return serverClockOffsetMs;
      })().finally(() => {
        serverClockSyncPromise = null;
      });
      return serverClockSyncPromise;
    }
    
    function depthTransports(symbol, mode) {
      const stream = `${symbol.toLowerCase()}@${mode === "partial" ? "depth20" : "depth"}@100ms`;
      return [
        { name: "public · combined", url: `wss://fstream.binance.com/public/stream?streams=${stream}`, subscribe: false, stream },
        { name: "public · raw", url: `wss://fstream.binance.com/public/ws/${stream}`, subscribe: false, stream },
      ];
    }
    
    function tradeStreams(symbol) {
      const name = symbol.toLowerCase();
      // Production Tape uses the documented USD-M Futures @aggTrade stream.
      // Undocumented @trade remains excluded until a separate shadow study proves
      // continuity, duplication and latency behaviour over a long observation.
      return [`${name}@aggTrade`];
    }
    
    function tradeTransports(streams) {
      const stream = String(streams?.[0] ?? "");
      return [
        { name: "market · combined", url: `wss://fstream.binance.com/market/stream?streams=${stream}`, subscribe: false, streams: [stream] },
        { name: "market · raw", url: `wss://fstream.binance.com/market/ws/${stream}`, subscribe: false, streams: [stream] },
      ];
    }
    
    function trimSide(levels, side, limit) {
      if (levels.size <= limit) return;
      const keys = [...levels.keys()].sort(side === "bid" ? (a, b) => b - a : (a, b) => a - b);
      for (const price of keys.slice(limit)) levels.delete(price);
    }
    
    class SymbolFeed {
      constructor(symbol) {
        this.symbol = symbol;
        this.subscribers = 0;
        this.closeTimer = 0;
        this.socket = null;
        this.tradeSocket = null;
        this.reconnectTimer = 0;
        this.tradeReconnectTimer = 0;
        this.tradeFirstMessageTimer = 0;
        this.firstDepthTimer = 0;
        this.snapshotTimer = 0;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.generation = 0;
        this.bids = new Map();
        this.asks = new Map();
        this.partialBidKeys = new Set();
        this.partialAskKeys = new Set();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.resyncCount = 0;
        this.dirty = false;
        this.forceEmit = false;
        this.lastEmitAt = 0;
        this.cachedSorted = null;
        this.bookEvents = new self.InPulsOrderBookEvents.DepthEventJournal({ symbol });
        this.densityLifecycle = new self.InPulsOrderBookDensity.DensityLifecycleTracker({ symbol });
        this.statusKey = "";
        this.trades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.tradeIds = new Set();
        this.tapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.aggregationTrades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.aggregationTradeIds = new Set();
        this.aggregationTapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.backgroundPaused = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.lastMessageAt = 0;
        this.lastRestartAt = 0;
        this.syncing = false;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.lastResyncAt = 0;
        this.tradeTransportName = "—";
        this.flowWindowStartedAt = Date.now();
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
        this.latestFlowHealth = null;
        this.tapeGuard = new self.InPulsTapeGuard({ rawWarmupTrades: 6, rawStaleMs: 1_500 });
        this.tradeLatency = new self.InPulsTapeLatency.RollingLatency({ windowMs: 2_000, maxSamples: 400, updateMs: 250 });
      }
    
      tradeBoundary() {
        let boundary = null;
        for (const trade of this.trades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeSnapshot(limit = MAX_TRADE_HISTORY) {
        return this.trades.toArray(limit);
      }
    
      aggregationBoundary() {
        let boundary = null;
        for (const trade of this.aggregationTrades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeKey(trade) {
        const firstTradeId = Number.isInteger(Number(trade?.firstTradeId))
          ? Number(trade.firstTradeId)
          : trade?.id;
        const lastTradeId = Number.isInteger(Number(trade?.lastTradeId))
          ? Number(trade.lastTradeId)
          : trade?.id;
        return `${firstTradeId}:${lastTradeId}:${trade?.time}:${trade?.price}:${trade?.quantity}`;
      }
    
      resetTapeGuard() {
        this.tapeGuard.reset({ lastOutputTradeId: this.aggregationBoundary() });
      }
    
      addSubscriber() {
        const wasZero = this.subscribers === 0;
        const wasCoolingDown = Boolean(this.closeTimer);
        this.subscribers += 1;
        clearTimeout(this.closeTimer);
        this.closeTimer = 0;
        if (wasZero && !wasCoolingDown) this.start();
        else this.refresh();
      }
    
      removeSubscriber() {
        this.subscribers = Math.max(0, this.subscribers - 1);
        if (this.subscribers > 0 || this.closeTimer) return;
        this.closeTimer = setTimeout(() => {
          this.closeTimer = 0;
          if (this.subscribers === 0) {
            this.stop();
            feeds.delete(this.symbol);
          }
        }, IDLE_CLOSE_MS);
      }
    
      start() {
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tradeLatency.reset();
        this.resetFlowWindow();
        this.syncing = false;
        this.backgroundPaused = false;
        this.resetTapeGuard();
        this.resetBook("start");
        this.setStatus("loading", "Подключение Worker");
        const generation = this.generation;
        diagnose(this.symbol, "feed.start", {
          state: "started",
          generation,
          subscribers: this.subscribers,
        });
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      stopSockets() {
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        try { this.socket?.close(); } catch {}
        try { this.tradeSocket?.close(); } catch {}
        this.socket = null;
        this.tradeSocket = null;
        this.tapeGuard.disconnect("socket-stop");
      }
    
      stop() {
        diagnose(this.symbol, "feed.stop", {
          state: "started",
          generation: this.generation,
        });
        this.generation += 1;
        this.stopSockets();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
      }
    
      resetBook(reason = "reset") {
        this.bids.clear();
        this.asks.clear();
        this.partialBidKeys.clear();
        this.partialAskKeys.clear();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.cachedSorted = null;
        this.dirty = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        const bookEpoch = this.bookEvents.reset(reason);
        this.densityLifecycle.reset({ bookEpoch, reason });
      }
    
      setStatus(state, text) {
        const key = `${state}:${text}`;
        if (key === this.statusKey) return;
        this.statusKey = key;
        post("status", this.symbol, { state, text });
      }
    
      latencyText() {
        const latency = this.tradeLatency.current();
        if (!Number.isFinite(latency)) return "";
        const value = latency < 1
          ? "<1"
          : latency < 10
            ? latency.toFixed(1)
            : String(Math.round(latency));
        return ` · RX ${value}ms`;
      }
    
      liveStatusText(tapeState = null) {
        const partial = this.mode === "partial" ? " · 20" : "";
        const reconnectingTape = tapeState === "reconnect" || (this.tradeLive && !this.tradeConnected);
        const tape = reconnectingTape
          ? " · TAPE RECONNECT"
          : (this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : "");
        return `LIVE 100ms · WORKER${partial}${tape}${this.latencyText()}`;
      }
    
      publishLiveStatus(tapeState = null) {
        if (this.syncing) {
          this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          return;
        }
        const depthAge = this.lastDepthAt ? Date.now() - this.lastDepthAt : Infinity;
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
          return;
        }
        this.setStatus("online", this.liveStatusText(tapeState));
      }
    
      markDirty(force = false) {
        this.dirty = true;
        if (force) this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      refresh() {
        this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      pauseForBackground() {
        if (this.subscribers <= 0 || this.backgroundPaused) return;
        this.backgroundPaused = true;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.generation += 1;
        this.stopSockets();
        this.tradeConnected = false;
        this.tradeLive = false;
        this.tradeLatency.reset();
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Пауза Worker",
        );
      }
    
      resume(delayMs = 0, epoch = visibilityEpoch) {
        clearTimeout(this.resumeTimer);
        this.resumeTimer = setTimeout(() => {
          this.resumeTimer = 0;
          if (!tabVisible || epoch !== visibilityEpoch || this.subscribers <= 0) return;
    
          const now = Date.now();
          const socketOpen = this.socket?.readyState === WebSocket.OPEN;
          const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
          const depthFresh = this.lastDepthAt > 0 && now - this.lastDepthAt <= RESUME_STALE_MS;
    
          if (!this.backgroundPaused && socketOpen && depthFresh) {
            post("tape", this.symbol, {
              replace: true,
              liveOnly: true,
              trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            }, null, { sourceKind: "live-reset" });
            if (!tradeOpen && !this.tradeReconnectTimer) {
              this.connectTrades(this.generation);
            }
            this.forceEmit = true;
            this.emit(now, MAX_RESUME_LEVELS_PER_SIDE, true);
            this.publishLiveStatus(tradeOpen ? null : "reconnect");
            return;
          }
    
          this.backgroundPaused = false;
          this.restartAfterBackground(true);
        }, Math.max(0, delayMs));
      }
    
      restartAfterBackground(force = false) {
        if (this.subscribers <= 0) return;
        const now = Date.now();
        if (!force && now - this.lastRestartAt < 2_500) return;
        this.lastRestartAt = now;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tapeGuard.disconnect("background-restart");
        this.resetFlowWindow();
        this.resetBook("background-restart");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Восстановление Worker",
        );
        const generation = this.generation;
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        }, null, { sourceKind: "live-reset" });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      resetFlowWindow(now = Date.now()) {
        this.flowWindowStartedAt = now;
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
      }
    
      recordFlow(kind, startedAt) {
        const durationMs = Math.max(0, performance.now() - startedAt);
        if (kind === "depth") {
          this.depthEventsInWindow += 1;
          this.depthProcessMsInWindow += durationMs;
          this.depthProcessMaxMsInWindow = Math.max(this.depthProcessMaxMsInWindow, durationMs);
          return;
        }
        this.tradeEventsInWindow += 1;
        this.tradeProcessMsInWindow += durationMs;
        this.tradeProcessMaxMsInWindow = Math.max(this.tradeProcessMaxMsInWindow, durationMs);
      }
    
      reportFlow(now = Date.now()) {
        const durationMs = Math.max(1, now - this.flowWindowStartedAt);
        const perSecond = 1_000 / durationMs;
        const snapshot = {
          durationMs,
          depthEventsPerSecond: this.depthEventsInWindow * perSecond,
          tradeEventsPerSecond: this.tradeEventsInWindow * perSecond,
          depthProcessMeanMs: this.depthEventsInWindow
            ? this.depthProcessMsInWindow / this.depthEventsInWindow
            : 0,
          tradeProcessMeanMs: this.tradeEventsInWindow
            ? this.tradeProcessMsInWindow / this.tradeEventsInWindow
            : 0,
          depthProcessMaxMs: this.depthProcessMaxMsInWindow,
          tradeProcessMaxMs: this.tradeProcessMaxMsInWindow,
          tapeQueue: this.tapeBatch.length,
          tapeDropped: this.tapeDroppedInWindow,
          depthSourceLagMs: this.lastDepthSourceLagMs,
          tradeSourceLagMs: this.lastTradeSourceLagMs,
        };
        this.latestFlowHealth = snapshot;
        diagnose(this.symbol, "worker.flow", {
          state: "sampled",
          generation: this.generation,
          ...snapshot,
        });
        this.resetFlowWindow(now);
        return snapshot;
      }
    
      ensureHealthy(now = Date.now()) {
        if (!tabVisible || this.subscribers <= 0) return;
        const recoveryDelayed = this.syncing
          && this.lastRestartAt > 0
          && now - this.lastRestartAt > RECOVERY_TIMEOUT_MS;
        if (recoveryDelayed) {
          this.setStatus("stale", "RECOVERY DELAY · повторное подключение");
          this.restartAfterBackground(true);
          return;
        }
        const socketOpen = this.socket?.readyState === WebSocket.OPEN;
        const socketConnecting = this.socket?.readyState === WebSocket.CONNECTING;
        const reconnectPending = Boolean(this.reconnectTimer || this.firstDepthTimer || this.snapshotLoading);
        const depthAge = this.lastDepthAt > 0 ? now - this.lastDepthAt : Infinity;
        const depthSourceStale = this.depthReady
          && Number.isFinite(this.lastDepthSourceLagMs)
          && this.lastDepthSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const stale = this.depthReady && (depthAge > ACTIVE_STALE_MS || depthSourceStale);
    
        if ((!socketOpen && !socketConnecting && !reconnectPending) || stale) {
          if (stale) {
            diagnose(this.symbol, "depth.freshness", {
              state: "stale",
              generation: this.generation,
              depthAgeMs: depthAge,
              sourceLagMs: this.lastDepthSourceLagMs,
              socketOpen,
            });
          }
          this.setStatus("offline", "RECONNECT · WORKER");
          this.restartAfterBackground();
          return;
        }
    
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
        } else if (this.depthReady && this.statusKey.startsWith("stale:")) {
          this.publishLiveStatus();
        }
    
        const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
        const tradeConnecting = this.tradeSocket?.readyState === WebSocket.CONNECTING;
        const tradeAge = this.lastTradeAt > 0 ? now - this.lastTradeAt : Infinity;
        const tradeSourceStale = Number.isFinite(this.lastTradeSourceLagMs)
          && this.lastTradeSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const tradeStale = this.tradeConnected
          && (tradeAge > TRADE_ACTIVE_STALE_MS || tradeSourceStale);
        if (tradeOpen && tradeStale) {
          diagnose(this.symbol, "tape.freshness", {
            state: "stale",
            generation: this.generation,
            tradeAgeMs: tradeAge,
            sourceLagMs: this.lastTradeSourceLagMs,
            socketOpen: true,
          });
          this.publishLiveStatus("reconnect");
          try { this.tradeSocket?.close(); } catch {}
          return;
        }
        if (!tradeOpen && !tradeConnecting && !this.tradeReconnectTimer) {
          if (this.tradeLive) this.publishLiveStatus("reconnect");
          this.connectTrades(this.generation);
        }
      }
    
      priorityRank() {
        const index = prioritySymbols.indexOf(this.symbol);
        return index < 0 ? Number.MAX_SAFE_INTEGER : index;
      }
    
      emittedLimit() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1) return MAX_EMITTED_LEVELS_PER_SIDE;
        if (this.priorityRank() === 0) return active <= 2 ? 1_800 : 900;
        if (active === 2) return 900;
        if (active <= 4) return 450;
        return 280;
      }
    
      emitIntervalMs() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1 || this.priorityRank() === 0) return 100;
        if (active <= 4) return 220;
        return 320;
      }
    
      bookStorageLimit() {
        // UI emission is throttled separately. Never destroy the deep local
        // book merely because several panels are open.
        return MAX_BOOK_LEVELS_PER_SIDE;
      }
    
      sortedDepth() {
        if (this.cachedSorted) return this.cachedSorted;
        const rawBids = [...this.bids.entries()];
        const rawAsks = [...this.asks.entries()];
        const bids = rawBids.sort((a, b) => b[0] - a[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const asks = rawAsks.sort((a, b) => a[0] - b[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const quoteScale = bookQuoteScale(rawBids, rawAsks);
        this.cachedSorted = {
          bids,
          asks,
          sizeScaleMaxQuote: quoteScale.maximum,
          sizeAnomalyThresholdQuote: quoteScale.anomalyThreshold,
          sizeAnomalyThresholdBidQuote: quoteScale.bidAnomalyThreshold,
          sizeAnomalyThresholdAskQuote: quoteScale.askAnomalyThreshold,
        };
        return this.cachedSorted;
      }
    
      emit(now = Date.now(), requestedLimit = this.emittedLimit(), force = false) {
        const processStartedAt = observabilityEnabled ? performance.now() : null;
        if (!tabVisible || this.subscribers <= 0 || (!force && !this.dirty && !this.forceEmit)) return;
        if (!force && !this.forceEmit && now - this.lastEmitAt < this.emitIntervalMs()) return;
        const fullView = this.sortedDepth();
        const limit = Math.max(100, Math.min(MAX_EMITTED_LEVELS_PER_SIDE, Math.floor(requestedLimit)));
        const view = self.InPulsOrderBookDepthProjection.compactDepthView(fullView, {
          exactLimit: limit,
          densityLimit: 96,
          bandCount: 128,
        });
        if (!view.bids.length || !view.asks.length) return;
        const bestBid = Number(fullView.bids[0][0]);
        const bestAsk = Number(fullView.asks[0][0]);
        const middle = (bestBid + bestAsk) / 2;
        const lowestBid = Number(view.bids.at(-1)?.[0]);
        const highestAsk = Number(view.asks.at(-1)?.[0]);
        this.densityLifecycle.refresh({
          bids: fullView.bids,
          asks: fullView.asks,
          now,
        });
        post("data", this.symbol, {
          data: {
            symbol: this.symbol,
            bids: view.bids,
            asks: view.asks,
            trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            bestBid,
            bestAsk,
            lastUpdateId: this.lastUpdateId,
            eventTime: now,
            depthReady: this.depthReady,
            coverage: {
              bidPercent: Number.isFinite(lowestBid) ? Math.max(0, ((middle - lowestBid) / middle) * 100) : 0,
              askPercent: Number.isFinite(highestAsk) ? Math.max(0, ((highestAsk - middle) / middle) * 100) : 0,
            },
            bookLevels: { bids: this.bids.size, asks: this.asks.size },
            sizeScaleMaxQuote: fullView.sizeScaleMaxQuote,
            sizeAnomalyThresholdQuote: fullView.sizeAnomalyThresholdQuote,
            sizeAnomalyThresholdBidQuote: fullView.sizeAnomalyThresholdBidQuote,
            sizeAnomalyThresholdAskQuote: fullView.sizeAnomalyThresholdAskQuote,
            resyncCount: this.resyncCount,
            orderBookEvents: this.bookEvents.summary(),
            densityLifecycle: this.densityLifecycle.summary(now),
            depthProjection: view.metadata,
            health: {
              mode: this.mode,
              depthAgeMs: this.lastDepthAt ? Math.max(0, now - this.lastDepthAt) : null,
              tradeAgeMs: this.lastTradeAt ? Math.max(0, now - this.lastTradeAt) : null,
              depthSourceLagMs: this.lastDepthSourceLagMs,
              tradeSourceLagMs: this.lastTradeSourceLagMs,
              depthBuffer: this.depthBuffer.length,
              tapeQueue: this.tapeBatch.length,
              subscribers: this.subscribers,
              depthTransport: this.transportIndex,
              tradeTransport: this.tradeTransportIndex,
              tradeTransportName: this.tradeTransportName,
              syncing: this.syncing,
              flow: this.latestFlowHealth,
              tape: this.tapeGuard.snapshot(now),
            },
            worker: true,
          },
        }, processStartedAt, {
          sourceEventTimeMs: this.lastDepthEventTime,
          sourceKind: this.lastDepthEventTime ? "live-depth" : "snapshot-depth",
        });
        this.lastEmitAt = now;
        this.dirty = false;
        this.forceEmit = false;
      }
    
      trimBook() {
        const limit = this.bookStorageLimit();
        trimSide(this.bids, "bid", limit);
        trimSide(this.asks, "ask", limit);
      }
    
      applyDepth(event, first = false) {
        const decision = sequenceDecision(this.lastUpdateId, event, first);
        if (decision === "ignore") return true;
        if (decision === "resync") {
          this.resync("Разрыв последовательности");
          return false;
        }
        const bookEvents = this.bookEvents.applyDiff({
          bids: this.bids,
          asks: this.asks,
          event,
          continuity: this.depthReady ? "live" : "recovered",
          receivedAt: Number(event?.__receivedAt) || Date.now(),
        });
        this.densityLifecycle.ingest(bookEvents);
        this.lastUpdateId = Number(event.u);
        this.cachedSorted = null;
        this.trimBook();
        this.markDirty();
        return true;
      }
    
      bufferDepth(event) {
        this.depthBuffer.push(event);
        if (this.depthBuffer.length > MAX_BUFFERED_DEPTH_EVENTS) {
          this.depthBuffer.splice(0, this.depthBuffer.length - MAX_BUFFERED_DEPTH_EVENTS);
          this.resync("Переполнение буфера");
        }
      }
    
      installSnapshot() {
        const snapshot = this.pendingSnapshot;
        if (!snapshot) return false;
        const snapshotId = Number(snapshot.lastUpdateId);
        const applicable = this.depthBuffer.filter((event) => Number(event?.u) > snapshotId);
        const bridgeIndex = applicable.findIndex(
          (event) => Number(event?.U) <= snapshotId + 1 && Number(event?.u) >= snapshotId + 1,
        );
        if (bridgeIndex < 0) {
          const firstU = Number(applicable[0]?.U);
          if (Number.isFinite(firstU) && firstU > snapshotId + 1) {
            diagnose(this.symbol, "depth.snapshot.bridge", {
              state: "gap",
              generation: this.generation,
              snapshotId,
              firstBufferedUpdateId: firstU,
            });
            this.pendingSnapshot = null;
            clearTimeout(this.snapshotTimer);
            this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), 250);
          }
          return false;
        }
        this.bids = new Map();
        this.asks = new Map();
        const snapshotReceivedAt = Date.now();
        const baseline = this.bookEvents.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          snapshot,
          receivedAt: snapshotReceivedAt,
        });
        this.densityLifecycle.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          bookEpoch: this.bookEvents.bookEpoch,
          receivedAt: snapshotReceivedAt,
        });
        this.lastUpdateId = baseline.snapshotId;
        this.cachedSorted = null;
        for (let index = bridgeIndex; index < applicable.length; index += 1) {
          if (!this.applyDepth(applicable[index], index === bridgeIndex)) return false;
        }
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.depthReady = true;
        this.syncing = false;
        this.bookEvents.markReady();
        this.densityLifecycle.markReady();
        diagnose(this.symbol, "depth.live", {
          state: "ready",
          generation: this.generation,
          mode: this.mode,
          snapshotId,
          bufferedEvents: applicable.length - bridgeIndex,
          bids: this.bids.size,
          asks: this.asks.size,
          orderBookEvents: this.bookEvents.summary(),
        });
        this.publishLiveStatus();
        this.markDirty(true);
        return true;
      }
    
      async loadSnapshot(generation) {
        if (generation !== this.generation || this.mode !== "deep" || this.snapshotLoading) return;
        this.snapshotLoading = true;
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        let snapshot = null;
        let winner = null;
        diagnose(this.symbol, "depth.snapshot", {
          state: "scheduled",
          generation,
          hosts: hosts.length,
        });
        try {
          winner = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const data = await fetchJson(
                `https://${host}/fapi/v1/depth?symbol=${encodeURIComponent(this.symbol)}&limit=1000`,
                SNAPSHOT_TIMEOUT_MS,
                signal,
              );
              if (!Array.isArray(data?.bids) || !Array.isArray(data?.asks) || !Number.isFinite(Number(data?.lastUpdateId))) {
                throw new Error("invalid snapshot");
              }
              return data;
            },
            {
              onAttempt: (event) => diagnose(this.symbol, "depth.snapshot.host", {
                ...event,
                host: event.target,
                target: undefined,
                generation,
              }),
            },
          );
          snapshot = winner.value;
        } catch {}
        this.snapshotLoading = false;
        if (generation !== this.generation || this.mode !== "deep") return;
        if (!snapshot) {
          diagnose(this.symbol, "depth.snapshot", {
            state: "failed",
            generation,
          });
          this.activatePartial(generation);
          return;
        }
        diagnose(this.symbol, "depth.snapshot", {
          state: "succeeded",
          generation,
          host: winner.target,
          durationMs: winner.durationMs,
          snapshotId: Number(snapshot.lastUpdateId),
        });
        this.pendingSnapshot = snapshot;
        this.installSnapshot();
      }
    
      activatePartial(generation) {
        if (generation !== this.generation || this.mode === "partial") return;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.mode = "partial";
        this.transportIndex = 0;
        this.syncing = preserveLastFrame;
        this.resetBook("partial-fallback");
        this.bookEvents.markUnavailable("partial-depth");
        this.densityLifecycle.markUnavailable("partial-depth");
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Резервный Worker-стакан",
        );
        diagnose(this.symbol, "depth.fallback", {
          state: "activated",
          generation,
          mode: "partial",
        });
        try { this.socket?.close(); } catch {}
        this.socket = null;
        this.reconnectTimer = setTimeout(() => this.connectDepth(generation), 0);
      }
    
      resync(text) {
        if (this.mode !== "deep") return;
        const now = Date.now();
        if (now - this.lastResyncAt < 350) return;
        this.lastResyncAt = now;
        this.resyncCount += 1;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.resetBook(text === "Переполнение буфера" ? "buffer-overflow" : "sequence-gap");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? `СИНХРОНИЗАЦИЯ · ${text}` : text,
        );
        clearTimeout(this.snapshotTimer);
        const delay = Math.min(2_000, 250 + this.resyncCount * 75);
        this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), delay);
      }
    
      replacePartial(target, previousKeys, rows) {
        const nextKeys = new Set();
        for (const row of rows ?? []) {
          const price = Number(row?.[0]);
          const quantity = Number(row?.[1]);
          if (!Number.isFinite(price) || !Number.isFinite(quantity)) continue;
          nextKeys.add(price);
          if (quantity > 0) target.set(price, quantity);
          else target.delete(price);
        }
        for (const price of previousKeys) if (!nextKeys.has(price)) target.delete(price);
        return nextKeys;
      }
    
      connectDepth(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.firstDepthTimer);
        const transports = depthTransports(this.symbol, this.mode);
        const transport = transports[this.transportIndex % transports.length];
        const transportIndex = this.transportIndex % transports.length;
        diagnose(this.symbol, "depth.ws.create", {
          state: "started",
          generation,
          mode: this.mode,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "depth.ws.create", {
            state: "failed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.transportIndex += 1;
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
          return;
        }
        this.socket = socket;
        this.firstDepthTimer = setTimeout(() => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.first-message", {
              state: "timeout",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              durationMs: 8_000,
            });
            try { socket.close(); } catch {}
          }
        }, 8_000);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.socket) return;
          diagnose(this.symbol, "depth.ws.open", {
            state: "succeeded",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
          });
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: [transport.stream],
              id: Date.now() % 2_147_483_647,
            }));
          }
          if (this.syncing) this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          else this.setStatus("loading", this.mode === "deep" ? "Синхронизация Worker" : "Подключаю резерв Worker");
          if (this.mode === "deep") this.loadSnapshot(generation);
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.socket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            this.lastMessageAt = Date.now();
    
            const bids = update?.b ?? update?.bids;
            const asks = update?.a ?? update?.asks;
            if (!Array.isArray(bids) || !Array.isArray(asks)) return;
            const receivedFirstDepth = this.lastDepthAt === 0;
            const receivedAt = Date.now();
            update.__receivedAt = receivedAt;
            this.lastDepthAt = receivedAt;
            const sourceEventTime = Number(update?.E);
            if (Number.isFinite(sourceEventTime)) {
              this.lastDepthEventTime = sourceEventTime;
              this.lastDepthSourceLagMs = calibratedSourceLag(sourceEventTime, receivedAt);
            }
            if (receivedFirstDepth) {
              diagnose(this.symbol, "depth.ws.first-message", {
                state: "received",
                generation,
                mode: this.mode,
                transport: this.transportIndex,
                transportName: transport.name,
              });
            }
            clearTimeout(this.firstDepthTimer);
            this.transportIndex = 0;
            this.depthReconnectAttempt = 0;
    
            if (this.mode === "partial") {
              this.partialBidKeys = this.replacePartial(this.bids, this.partialBidKeys, bids);
              this.partialAskKeys = this.replacePartial(this.asks, this.partialAskKeys, asks);
              this.lastUpdateId = Number(update.u ?? update.lastUpdateId) || this.lastUpdateId;
              this.depthReady = true;
              this.syncing = false;
              this.cachedSorted = null;
              this.publishLiveStatus();
              this.markDirty(true);
              return;
            }
    
            if (!Number.isFinite(Number(update?.U)) || !Number.isFinite(Number(update?.u))) return;
            if (!this.depthReady) {
              this.bufferDepth(update);
              if (!this.pendingSnapshot && !this.snapshotLoading) this.loadSnapshot(generation);
              this.installSnapshot();
              return;
            }
            this.applyDepth(update);
          } finally {
            this.recordFlow("depth", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.socket) return;
          clearTimeout(this.firstDepthTimer);
          this.socket = null;
          this.transportIndex += 1;
          diagnose(this.symbol, "depth.ws.close", {
            state: "closed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          const preserveLastFrame = this.depthReady || this.syncing;
          this.syncing = preserveLastFrame;
          this.resetBook("reconnect");
          this.setStatus(
            preserveLastFrame ? "stale" : "offline",
            preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "RECONNECT · WORKER",
          );
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.error", {
              state: "failed",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      connectTrades(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
    
        const streams = tradeStreams(this.symbol);
        const transports = tradeTransports(streams);
        const transport = transports[this.tradeTransportIndex % transports.length];
        const transportIndex = this.tradeTransportIndex % transports.length;
        diagnose(this.symbol, "tape.ws.create", {
          state: "started",
          generation,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "tape.ws.create", {
            state: "failed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.tradeTransportIndex += 1;
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
          return;
        }
        this.tradeSocket = socket;
        this.tradeTransportName = transport.name;
        let receivedTrade = false;
        this.tradeFirstMessageTimer = setTimeout(() => {
          if (generation !== this.generation || socket !== this.tradeSocket || receivedTrade) return;
          diagnose(this.symbol, "tape.ws.first-message", {
            state: "timeout",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            durationMs: TRADE_FIRST_MESSAGE_TIMEOUT_MS,
          });
          try { socket.close(); } catch {}
        }, TRADE_FIRST_MESSAGE_TIMEOUT_MS);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          diagnose(this.symbol, "tape.ws.open", {
            state: "succeeded",
            generation,
            transport: transportIndex,
            transportName: transport.name,
          });
          this.tapeGuard.connect();
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: transport.streams,
              id: Date.now() % 2_147_483_647,
            }));
          }
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            const eventType = String(update?.e ?? "").toLowerCase();
            const payloadStream = payload.stream.toLowerCase();
            const rawEvent = eventType === "trade" || payloadStream.endsWith("@trade");
            const aggregateEvent = eventType === "aggtrade" || payloadStream.endsWith("@aggtrade");
            if (!rawEvent && !aggregateEvent) return;
    
            const receivedAt = Date.now();
            const source = rawEvent ? "raw" : "agg";
            const trade = normalizeTrade(update, source, receivedAt);
            if (!trade) return;
            const firstTradeMessage = !receivedTrade;
            receivedTrade = true;
            clearTimeout(this.tradeFirstMessageTimer);
            this.tradeFirstMessageTimer = 0;
            this.lastTradeAt = receivedAt;
            this.lastTradeEventTime = trade.eventTime;
            this.lastTradeSourceLagMs = calibratedSourceLag(trade.eventTime, receivedAt);
            // RX in the header describes the stable production Tape feed. The parallel
            // experimental @trade channel must not double-weight or zero the rolling median.
            if (aggregateEvent) this.tradeLatency.record(trade.rxLatencyMs, receivedAt);
            this.tradeTransportIndex = 0;
            this.tradeReconnectAttempt = 0;
            this.tradeLive = true;
            this.tradeConnected = true;
            if (firstTradeMessage) {
              diagnose(this.symbol, "tape.ws.first-message", {
                state: "received",
                generation,
                transport: transportIndex,
                transportName: transport.name,
                source,
              });
            }
    
            const decision = this.tapeGuard.ingest(trade, this.lastTradeAt);
            let changed = false;
    
            // Preserve the visual RAW contract: it continues to receive only
            // Binance @aggTrade events, exactly as before this feature.
            if (aggregateEvent && this.insertTrade(trade, true)) {
              const matchedDensities = this.densityLifecycle.ingestTrades([trade]);
              if (matchedDensities.length) this.markDirty();
              this.queueTape(trade);
              changed = true;
            }
    
            // The guarded production path follows the same documented @aggTrade
            // stream and keeps continuity/dedup protection without mixing sources.
            if (decision.emit && this.insertAggregationTrade(trade, true)) {
              this.queueAggregationTape(trade);
              changed = true;
            }
    
            this.publishLiveStatus();
            if (!changed && rawEvent && decision.reason !== "raw-warmup") {
              diagnose(this.symbol, "tape.aggregation-source", {
                state: "skipped",
                source,
                reason: decision.reason,
                mode: decision.mode,
              });
            }
          } finally {
            this.recordFlow("trade", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          clearTimeout(this.tradeFirstMessageTimer);
          this.tradeFirstMessageTimer = 0;
          this.tradeSocket = null;
          this.tradeConnected = false;
          this.tapeGuard.disconnect("socket-close");
          this.tradeTransportIndex += 1;
          diagnose(this.symbol, "tape.ws.close", {
            state: "closed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          if (this.tradeLive && this.depthReady) this.publishLiveStatus("reconnect");
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.tradeSocket) {
            diagnose(this.symbol, "tape.ws.error", {
              state: "failed",
              generation,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      insertTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.tradeIds.has(key)) return false;
        this.tradeIds.add(key);
        const evicted = newestFirst
          ? this.trades.prepend(trade)
          : this.trades.append(trade);
        if (evicted === trade) {
          this.tradeIds.delete(key);
          return false;
        }
        if (evicted) this.tradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      insertAggregationTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.aggregationTradeIds.has(key)) return false;
        this.aggregationTradeIds.add(key);
        const evicted = newestFirst
          ? this.aggregationTrades.prepend(trade)
          : this.aggregationTrades.append(trade);
        if (evicted === trade) {
          this.aggregationTradeIds.delete(key);
          return false;
        }
        if (evicted) this.aggregationTradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      scheduleTapeFlush() {
        if (!this.tapeTimer) {
          this.tapeTimer = setTimeout(() => this.flushTapeBatch(), TAPE_FLUSH_MS);
        }
      }
    
      queueTape(trade) {
        if (!trade || !tabVisible) return;
        this.tapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      queueAggregationTape(trade) {
        if (!trade || !tabVisible) return;
        this.aggregationTapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      flushTapeBatch() {
        this.tapeTimer = 0;
        if (!tabVisible) {
          this.tapeBatch.clear();
          this.aggregationTapeBatch.clear();
          return;
        }
        const latest = this.tapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const aggregationLatest = this.aggregationTapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const trades = latest.items;
        const aggregationTrades = aggregationLatest.items;
        this.tapeDroppedInWindow += latest.dropped + aggregationLatest.dropped;
        if (trades.length || aggregationTrades.length) {
          const sourceEventTimeMs = [...trades, ...aggregationTrades].reduce(
            (latestTime, trade) => Math.max(latestTime, Number(trade?.eventTime) || 0),
            0,
          );
          const guard = this.tapeGuard.snapshot();
          post(
            "tape",
            this.symbol,
            {
              replace: false,
              live: true,
              liveOnly: true,
              trades,
              aggregationTrades,
              aggregationSource: guard.mode,
              aggregationHealth: guard,
              backpressure: {
                dropped: latest.dropped + aggregationLatest.dropped,
                pending: this.tapeBatch.length + this.aggregationTapeBatch.length,
              },
            },
            observabilityEnabled ? performance.now() : null,
            { sourceEventTimeMs, sourceKind: "live-trade-dual" },
          );
        }
      }
    
    }
    
    function scheduleEmit() {
      if (!tabVisible || emitTimer) return;
      emitTimer = setTimeout(() => {
        emitTimer = 0;
        if (!tabVisible) return;
    
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0);
        if (!active.length) return;
    
        // Не отправляем несколько тяжёлых книг в UI одним залпом.
        // При 3+ стаканах один тик обслуживает один символ по кругу.
        const budget = active.length <= 2 ? active.length : 1;
        const now = Date.now();
        for (let index = 0; index < budget; index += 1) {
          const feed = active[emitCursor % active.length];
          emitCursor = (emitCursor + 1) % active.length;
          feed.emit(now);
        }
    
        if (active.some((feed) => feed.dirty || feed.forceEmit)) scheduleEmit();
      }, 25);
    }
    
    function scheduleWatchdog() {
      clearTimeout(watchdogTimer);
      watchdogTimer = setTimeout(() => {
        watchdogTimer = 0;
        const now = Date.now();
        if (tabVisible) {
          for (const feed of feeds.values()) {
            if (feed.subscribers <= 0) continue;
            feed.reportFlow(now);
            feed.ensureHealthy(now);
          }
        }
        self.postMessage({
          type: "heartbeat",
          time: now,
          visible: tabVisible,
          activeFeeds: [...feeds.values()].filter((feed) => feed.subscribers > 0).length,
          totalFeeds: feeds.size,
        });
        scheduleWatchdog();
      }, WORKER_HEARTBEAT_MS);
    }
    
    function getFeed(symbol) {
      let feed = feeds.get(symbol);
      if (!feed) {
        feed = new SymbolFeed(symbol);
        feeds.set(symbol, feed);
      }
      return feed;
    }
    
    function cancelBackgroundPause() {
      clearTimeout(backgroundPauseTimer);
      backgroundPauseTimer = 0;
    }
    
    function scheduleBackgroundPause(epoch) {
      cancelBackgroundPause();
      backgroundPauseTimer = setTimeout(() => {
        backgroundPauseTimer = 0;
        if (tabVisible || epoch !== visibilityEpoch) return;
        for (const feed of feeds.values()) feed.pauseForBackground();
      }, BACKGROUND_GRACE_MS);
    }
    
    self.addEventListener("message", (event) => {
      const message = event.data;
      if (!message || typeof message !== "object") return;
    
      if (message.type === "observability") {
        observabilityEnabled = Boolean(message.enabled);
        return;
      }
    
      if (message.type === "visibility") {
        const nextVisible = Boolean(message.visible);
        if (nextVisible === tabVisible) return;
    
        tabVisible = nextVisible;
        visibilityEpoch += 1;
        const epoch = visibilityEpoch;
    
        if (!tabVisible) {
          clearTimeout(emitTimer);
          emitTimer = 0;
          scheduleBackgroundPause(epoch);
          return;
        }
    
        cancelBackgroundPause();
        syncServerClock(true).catch(() => {});
    
        const resumePrioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        prioritySymbols = resumePrioritySymbols;
        const priorityRank = new Map(
          resumePrioritySymbols.map((symbol, index) => [symbol, index]),
        );
        const active = [...feeds.values()]
          .filter((feed) => feed.subscribers > 0)
          .sort((left, right) => (
            (priorityRank.get(left.symbol) ?? Number.MAX_SAFE_INTEGER)
            - (priorityRank.get(right.symbol) ?? Number.MAX_SAFE_INTEGER)
          ));
        active.forEach((feed, index) => feed.resume(index * RESUME_STAGGER_MS, epoch));
        return;
      }
    
      if (message.type === "priority") {
        prioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        return;
      }
    
      const symbol = String(message.symbol ?? "").toUpperCase();
      if (!symbol.endsWith("USDT")) return;
      if (message.type === "subscribe") {
        getFeed(symbol).addSubscriber();
        return;
      }
      if (message.type === "unsubscribe") {
        feeds.get(symbol)?.removeSubscriber();
        return;
      }
      if (message.type === "refresh") {
        feeds.get(symbol)?.refresh();
      }
    });
    
    syncServerClock(true).catch(() => {});
    scheduleWatchdog();
    post("ready", "");
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/InPuls/InPuls/test-raw-stability-core.mjs:322:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: SERIES displays a sum but keeps the visual size of its largest child AGG
ok 110 - SERIES displays a sum but keeps the visual size of its largest child AGG
  ---
  duration_ms: 2.101459
  type: 'test'
  ...
# Subtest: dense AGG windows keep markers but bound overlapping text labels
ok 111 - dense AGG windows keep markers but bound overlapping text labels
  ---
  duration_ms: 1.594883
  type: 'test'
  ...
# Subtest: live chart candles update in place without copying the full history
ok 112 - live chart candles update in place without copying the full history
  ---
  duration_ms: 0.414422
  type: 'test'
  ...
# Subtest: hidden timezone map no longer performs exact-second marker scans
ok 113 - hidden timezone map no longer performs exact-second marker scans
  ---
  duration_ms: 1.672693
  type: 'test'
  ...
# Subtest: KlineFeed coalesces live data and throttles full-array cache copies
ok 114 - KlineFeed coalesces live data and throttles full-array cache copies
  ---
  duration_ms: 0.581529
  type: 'test'
  ...
# Subtest: zero-ms AGG groups only consecutive executions with equal event time and side
ok 115 - zero-ms AGG groups only consecutive executions with equal event time and side
  ---
  duration_ms: 3.893845
  type: 'test'
  ...
# Subtest: only the right-most aggregate is OPEN and sealed history keeps object identity
ok 116 - only the right-most aggregate is OPEN and sealed history keeps object identity
  ---
  duration_ms: 7.203921
  type: 'test'
  ...
# Subtest: Tape UI keeps zero-ms AGG, adds SERIES and restores the marker threshold
ok 117 - Tape UI keeps zero-ms AGG, adds SERIES and restores the marker threshold
  ---
  duration_ms: 4.854755
  type: 'test'
  ...
# Subtest: Series groups same-side raw executions for a fixed 100 ms window
ok 118 - Series groups same-side raw executions for a fixed 100 ms window
  ---
  duration_ms: 3.152853
  type: 'test'
  ...
# Subtest: the first opposite execution closes the current Series immediately
ok 119 - the first opposite execution closes the current Series immediately
  ---
  duration_ms: 0.413903
  type: 'test'
  ...
# Subtest: sealed Series history keeps identity while only the open Series grows
ok 120 - sealed Series history keeps identity while only the open Series grows
  ---
  duration_ms: 0.723487
  type: 'test'
  ...
# Subtest: Aggregate labels are clipped and Series labels use the ending price
ok 121 - Aggregate labels are clipped and Series labels use the ending price
  ---
  duration_ms: 0.363865
  type: 'test'
  ...
# Subtest: Tape live edge follows the site clock even when the market is silent
ok 122 - Tape live edge follows the site clock even when the market is silent
  ---
  duration_ms: 11.046903
  type: 'test'
  ...
# Subtest: Series labels keep the larger volume when visual boxes collide
ok 123 - Series labels keep the larger volume when visual boxes collide
  ---
  duration_ms: 1.5175
  type: 'test'
  ...
# Subtest: Current open Series label wins a collision so the live event remains readable
ok 124 - Current open Series label wins a collision so the live event remains readable
  ---
  duration_ms: 0.263484
  type: 'test'
  ...
# Subtest: Burst traffic cannot create an unbounded wall of Series labels
ok 125 - Burst traffic cannot create an unbounded wall of Series labels
  ---
  duration_ms: 0.910466
  type: 'test'
  ...
# Subtest: Runtime exposes compact Series and avoids per-second card rescans
ok 126 - Runtime exposes compact Series and avoids per-second card rescans
  ---
  duration_ms: 2.803293
  type: 'test'
  ...
# Subtest: Footprint volume labels reuse order-book size typography
ok 127 - Footprint volume labels reuse order-book size typography
  ---
  duration_ms: 0.811855
  type: 'test'
  ...
# Subtest: historical Tape X coordinates do not depend on newly appended neighbours
ok 128 - historical Tape X coordinates do not depend on newly appended neighbours
  ---
  duration_ms: 1.3976
  type: 'test'
  ...
# Subtest: zero-ms AGG keeps sealed identity while only the newest group stays open
ok 129 - zero-ms AGG keeps sealed identity while only the newest group stays open
  ---
  duration_ms: 0.954073
  type: 'test'
  ...
# Subtest: distance badge is unsigned and fixed to tenths
ok 130 - distance badge is unsigned and fixed to tenths
  ---
  duration_ms: 0.284642
  type: 'test'
  ...
# Subtest: footprint exposes the same timeframe set and exchange-aligned boundaries
ok 131 - footprint exposes the same timeframe set and exchange-aligned boundaries
  ---
  duration_ms: 1.142982
  type: 'test'
  ...
# Subtest: first cluster candle is aligned but explicitly marked session-partial
ok 132 - first cluster candle is aligned but explicitly marked session-partial
  ---
  duration_ms: 1.582938
  type: 'test'
  ...
# Subtest: runtime ships zero-ms RAW/AGG/SERIES control, synchronized canvas and density age toggle
ok 133 - runtime ships zero-ms RAW/AGG/SERIES control, synchronized canvas and density age toggle
  ---
  duration_ms: 0.606948
  type: 'test'
  ...
# Subtest: psychological levels keep one anchored unit per symbol
ok 134 - psychological levels keep one anchored unit per symbol
  ---
  duration_ms: 2.22965
  type: 'test'
  ...
# Subtest: water clock moves continuously between WebSocket packets
ok 135 - water clock moves continuously between WebSocket packets
  ---
  duration_ms: 0.501703
  type: 'test'
  ...
# Subtest: all trades share one coherent affine price viewport
ok 136 - all trades share one coherent affine price viewport
  ---
  duration_ms: 0.819723
  type: 'test'
  ...
# Subtest: zero-ms AGG groups exact event time and freezes completed history
ok 137 - zero-ms AGG groups exact event time and freezes completed history
  ---
  duration_ms: 1.036521
  type: 'test'
  ...
# Subtest: marker geometry is absolute and independent of visible neighbours
ok 138 - marker geometry is absolute and independent of visible neighbours
  ---
  duration_ms: 0.411522
  type: 'test'
  ...
# Subtest: footprint and chart visual requests stay applied
ok 139 - footprint and chart visual requests stay applied
  ---
  duration_ms: 0.314573
  type: 'test'
  ...
# Subtest: Tape threshold is restored without restoring aggregation levels
ok 140 - Tape threshold is restored without restoring aggregation levels
  ---
  duration_ms: 1.24172
  type: 'test'
  ...
# Subtest: threshold filters markers while the path still uses every RAW trade
ok 141 - threshold filters markers while the path still uses every RAW trade
  ---
  duration_ms: 0.428169
  type: 'test'
  ...
# Subtest: zero threshold no longer writes a label on every aggregate
ok 142 - zero threshold no longer writes a label on every aggregate
  ---
  duration_ms: 0.245132
  type: 'test'
  ...
# Subtest: Worker keeps visual RAW on aggTrade and exposes guarded trade input for AGG
not ok 143 - Worker keeps visual RAW on aggTrade and exposes guarded trade input for AGG
  ---
  duration_ms: 4.116657
  type: 'test'
  location: '/home/runner/work/InPuls/InPuls/test-tiger-zero-ms-agg-source-v1.mjs:23:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /return \[`\$\{name\}@aggTrade`, `\$\{name\}@trade`\]/. Input:
    
    'importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-network.js?v=obs-pr1-1");\n' +
      'importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-depth-projection.js?v=deep-book-v1");\n' +
      'importScripts("./orderbook-events.js?v=orderbook-events-core-v1");\n' +
      'importScripts("./orderbook-density.js?v=density-trades-correlation-v1");\n' +
      '\n' +
      'const MAX_BOOK_LEVELS_PER_SIDE = 20_000;\n' +
      'const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;\n' +
      'const MAX_BUFFERED_DEPTH_EVENTS = 4_000;\n' +
      'const MAX_TRADE_HISTORY = 12_000;\n' +
      'const MAX_RESUME_LEVELS_PER_SIDE = 700;\n' +
      'const RESUME_STAGGER_MS = 180;\n' +
      'const BACKGROUND_GRACE_MS = 2_000;\n' +
      'const RECOVERY_TIMEOUT_MS = 8_000;\n' +
      'const RESUME_STALE_MS = 3_500;\n' +
      'const DEPTH_STALE_NOTICE_MS = 3_000;\n' +
      'const ACTIVE_STALE_MS = 9_000;\n' +
      'const SNAPSHOT_TIMEOUT_MS = 2_800;\n' +
      'const IDLE_CLOSE_MS = 10_000;\n' +
      'const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;\n' +
      'const MAX_TAPE_BATCH_PER_POST = 500;\n' +
      'const MAX_PENDING_TAPE_TRADES = 800;\n' +
      'const TAPE_FLUSH_MS = 25;\n' +
      'const ACTIVE_SOURCE_LAG_MS = 4_000;\n' +
      'const TRADE_ACTIVE_STALE_MS = 30_000;\n' +
      'const RECONNECT_BASE_MS = 400;\n' +
      'const RECONNECT_MAX_MS = 8_000;\n' +
      'const WORKER_HEARTBEAT_MS = 2_000;\n' +
      'const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;\n' +
      '\n' +
      'const feeds = new Map();\n' +
      'let tabVisible = true;\n' +
      'let emitTimer = 0;\n' +
      'let emitCursor = 0;\n' +
      'let visibilityEpoch = 0;\n' +
      'let backgroundPauseTimer = 0;\n' +
      'let watchdogTimer = 0;\n' +
      'let prioritySymbols = [];\n' +
      'let serverClockOffsetMs = null;\n' +
      'let serverClockRttMs = null;\n' +
      'let serverClockSyncAt = 0;\n' +
      'let serverClockSyncPromise = null;\n' +
      'let observabilityEnabled = false;\n' +
      'let observabilityPostCount = 0;\n' +
      '\n' +
      'function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {\n' +
      '  if (!observabilityEnabled) {\n' +
      '    self.postMessage({ type, symbol, ...payload });\n' +
      '    return;\n' +
      '  }\n' +
      '  const observerStartedAt = performance.now();\n' +
      '  const message = { type, symbol, ...payload };\n' +
      '  const samplePayload = observabilityPostCount++ % 50 === 0;\n' +
      '  let payloadBytes = null;\n' +
      '  if (samplePayload) {\n' +
      '    try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}\n' +
      '  }\n' +
      '  const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);\n' +
      '  message.__obs = {\n' +
      '    sentAtEpochMs: Date.now(),\n' +
      '    processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,\n' +
      '    observerOverheadMs: performance.now() - observerStartedAt,\n' +
      '    payloadBytes,\n' +
      '    payloadSampleRate: 50,\n' +
      '    sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,\n' +
      '    sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,\n' +
      '    sourceKind: obsContext.sourceKind ?? null,\n' +
      '  };\n' +
      '  self.postMessage(message);\n' +
      '}\n' +
      '\n' +
      'function diagnose(symbol, phase, details = {}) {\n' +
      '  if (!observabilityEnabled) return;\n' +
      '  post("diagnostic", symbol, {\n' +
      '    diagnostic: {\n' +
      '      phase,\n' +
      '      atEpochMs: Date.now(),\n' +
      '      ...details,\n' +
      '    },\n' +
      '  });\n' +
      '}\n' +
      '\n' +
      'function reconnectDelay(attempt = 0) {\n' +
      '  const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));\n' +
      '  const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));\n' +
      '  return exponential + jitter;\n' +
      '}\n' +
      '\n' +
      'function bookSideQuoteScale(levels, sampleLimit = 1_024) {\n' +
      '  const totalLevels = Number.isFinite(levels?.size)\n' +
      '    ? levels.size\n' +
      '    : (Number(levels?.length) || 0);\n' +
      '  const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));\n' +
      '  const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));\n' +
      '  const sample = [];\n' +
      '  let maximum = 1;\n' +
      '  let validIndex = 0;\n' +
      '  for (const [priceValue, quantityValue] of levels ?? []) {\n' +
      '    const quote = Number(priceValue) * Number(quantityValue);\n' +
      '    if (!Number.isFinite(quote) || quote <= 0) continue;\n' +
      '    if (quote > maximum) maximum = quote;\n' +
      '    if (validIndex % sampleStride === 0) sample.push(quote);\n' +
      '    validIndex += 1;\n' +
      '  }\n' +
      '  sample.sort((left, right) => left - right);\n' +
      '  const quantile = (ratio) => sample.length\n' +
      '    ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]\n' +
      '    : 0;\n' +
      '  const median = quantile(.5);\n' +
      '  const upper = quantile(.95);\n' +
      '  const extreme = quantile(.99);\n' +
      '  return {\n' +
      '    maximum,\n' +
      '    anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),\n' +
      '    sampledLevels: sample.length,\n' +
      '    totalLevels: validIndex,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function bookQuoteScale(bids, asks, sampleLimit = 2_048) {\n' +
      '  const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));\n' +
      '  const bid = bookSideQuoteScale(bids ?? [], sideLimit);\n' +
      '  const ask = bookSideQuoteScale(asks ?? [], sideLimit);\n' +
      '  return {\n' +
      '    maximum: Math.max(bid.maximum, ask.maximum),\n' +
      '    anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),\n' +
      '    bidAnomalyThreshold: bid.anomalyThreshold,\n' +
      '    askAnomalyThreshold: ask.anomalyThreshold,\n' +
      '    sampledLevels: bid.sampledLevels + ask.sampledLevels,\n' +
      '    totalLevels: bid.totalLevels + ask.totalLevels,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function parsePayload(raw) {\n' +
      '  let payload;\n' +
      '  try { payload = JSON.parse(raw); } catch { return null; }\n' +
      '  if (payload?.result === null || payload?.id) return null;\n' +
      '  return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };\n' +
      '}\n' +
      '\n' +
      'function normalizeTrade(event, sourceHint = null, receivedAt = null) {\n' +
      '  const eventType = String(event?.e ?? "").toLowerCase();\n' +
      '  const inferredRaw = eventType === "trade"\n' +
      '    || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));\n' +
      '  const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";\n' +
      '  const price = Number(event?.p);\n' +
      '  const quantity = Number(event?.q);\n' +
      '  const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);\n' +
      '  const time = timing.tradeTime;\n' +
      '  const id = source === "raw" ? Number(event?.t) : Number(event?.a);\n' +
      '  const firstTradeId = source === "raw" ? id : Number(event?.f);\n' +
      '  const lastTradeId = source === "raw" ? id : Number(event?.l);\n' +
      '  if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;\n' +
      '  if (price <= 0 || quantity <= 0 || time <= 0) return null;\n' +
      '  if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;\n' +
      '  return {\n' +
      '    id,\n' +
      '    firstTradeId,\n' +
      '    lastTradeId,\n' +
      '    source,\n' +
      '    price,\n' +
      '    quantity,\n' +
      '    quote: price * quantity,\n' +
      '    time,\n' +
      '    tradeTime: timing.tradeTime,\n' +
      '    eventTime: timing.eventTime,\n' +
      '    receivedAt: timing.receivedAt,\n' +
      '    rxLatencyMs: timing.rxLatencyMs,\n' +
      '    side: event?.m ? "sell" : "buy",\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function calibratedSourceLag(eventTime, receivedAt = Date.now()) {\n' +
      '  const sourceTime = Number(eventTime);\n' +
      '  const localTime = Number(receivedAt);\n' +
      '  if (\n' +
      '    !Number.isFinite(sourceTime)\n' +
      '    || !Number.isFinite(localTime)\n' +
      '    || !Number.isFinite(serverClockOffsetMs)\n' +
      '  ) return null;\n' +
      '  const lag = localTime + serverClockOffsetMs - sourceTime;\n' +
      '  if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;\n' +
      '  return Math.max(0, lag);\n' +
      '}\n' +
      '\n' +
      'function sequenceDecision(lastUpdateId, event, firstEvent = false) {\n' +
      '  const first = Number(event?.U);\n' +
      '  const final = Number(event?.u);\n' +
      '  const previous = Number(event?.pu);\n' +
      '  const local = Number(lastUpdateId);\n' +
      '  if (![first, final, local].every(Number.isFinite)) return "resync";\n' +
      '  if (final <= local) return "ignore";\n' +
      '  if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";\n' +
      '  if (Number.isFinite(previous) && previous !== local) return "resync";\n' +
      '  if (!Number.isFinite(previous) && first > local + 1) return "resync";\n' +
      '  return "apply";\n' +
      '}\n' +
      '\n' +
      'async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {\n' +
      '  const controller = typeof AbortController === "function" ? new AbortController() : null;\n' +
      '  let timer;\n' +
      '  const abortFromExternal = () => controller?.abort();\n' +
      '  if (externalSignal?.aborted) abortFromExternal();\n' +
      '  else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });\n' +
      '  try {\n' +
      '    const timeout = new Promise((_, reject) => {\n' +
      '      timer = setTimeout(() => {\n' +
      '        controller?.abort();\n' +
      '        reject(new Error("timeout"));\n' +
      '      }, timeoutMs);\n' +
      '    });\n' +
      '    const request = fetch(url, {\n' +
      '      cache: "no-store",\n' +
      '      ...(controller ? { signal: controller.signal } : {}),\n' +
      '    }).then(async (response) => {\n' +
      '      if (!response.ok) throw new Error(`HTTP ${response.status}`);\n' +
      '      return response.json();\n' +
      '    });\n' +
      '    return await Promise.race([request, timeout]);\n' +
      '  } finally {\n' +
      '    clearTimeout(timer);\n' +
      '    externalSignal?.removeEventListener?.("abort", abortFromExternal);\n' +
      '  }\n' +
      '}\n' +
      '\n' +
      'async function syncServerClock(force = false) {\n' +
      '  const now = Date.now();\n' +
      '  if (\n' +
      '    !force\n' +
      '    && Number.isFinite(serverClockOffsetMs)\n' +
      '    && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS\n' +
      '  ) return serverClockOffsetMs;\n' +
      '  if (serverClockSyncPromise) return serverClockSyncPromise;\n' +
      '\n' +
      '  serverClockSyncPromise = (async () => {\n' +
      '    const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];\n' +
      '    diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });\n' +
      '    let result = null;\n' +
      '    try {\n' +
      '      result = await self.InPulsOrderBookNetwork.firstSuccessful(\n' +
      '        hosts,\n' +
      '        async (host, { signal }) => {\n' +
      '          const startedAt = Date.now();\n' +
      '          const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);\n' +
      '          const finishedAt = Date.now();\n' +
      '          const serverTime = Number(data?.serverTime);\n' +
      '          if (!Number.isFinite(serverTime)) throw new Error("invalid server time");\n' +
      '          return {\n' +
      '            rtt: Math.max(0, finishedAt - startedAt),\n' +
      '            offset: serverTime - (startedAt + finishedAt) / 2,\n' +
      '          };\n' +
      '        },\n' +
      '        {\n' +
      '          onAttempt: (event) => diagnose("", "clock.rest.host", {\n' +
      '            ...event,\n' +
      '            host: event.target,\n' +
      '            target: undefined,\n' +
      '          }),\n' +
      '        },\n' +
      '      );\n' +
      '    } catch {}\n' +
      '    const best = result?.value;\n' +
      '    if (best) {\n' +
      '      serverClockOffsetMs = best.offset;\n' +
      '      serverClockRttMs = best.rtt;\n' +
      '      serverClockSyncAt = Date.now();\n' +
      '      '... 50929 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");
    importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");
    importScripts("./orderbook-network.js?v=obs-pr1-1");
    importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");
    importScripts("./orderbook-depth-projection.js?v=deep-book-v1");
    importScripts("./orderbook-events.js?v=orderbook-events-core-v1");
    importScripts("./orderbook-density.js?v=density-trades-correlation-v1");
    
    const MAX_BOOK_LEVELS_PER_SIDE = 20_000;
    const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;
    const MAX_BUFFERED_DEPTH_EVENTS = 4_000;
    const MAX_TRADE_HISTORY = 12_000;
    const MAX_RESUME_LEVELS_PER_SIDE = 700;
    const RESUME_STAGGER_MS = 180;
    const BACKGROUND_GRACE_MS = 2_000;
    const RECOVERY_TIMEOUT_MS = 8_000;
    const RESUME_STALE_MS = 3_500;
    const DEPTH_STALE_NOTICE_MS = 3_000;
    const ACTIVE_STALE_MS = 9_000;
    const SNAPSHOT_TIMEOUT_MS = 2_800;
    const IDLE_CLOSE_MS = 10_000;
    const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;
    const MAX_TAPE_BATCH_PER_POST = 500;
    const MAX_PENDING_TAPE_TRADES = 800;
    const TAPE_FLUSH_MS = 25;
    const ACTIVE_SOURCE_LAG_MS = 4_000;
    const TRADE_ACTIVE_STALE_MS = 30_000;
    const RECONNECT_BASE_MS = 400;
    const RECONNECT_MAX_MS = 8_000;
    const WORKER_HEARTBEAT_MS = 2_000;
    const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;
    
    const feeds = new Map();
    let tabVisible = true;
    let emitTimer = 0;
    let emitCursor = 0;
    let visibilityEpoch = 0;
    let backgroundPauseTimer = 0;
    let watchdogTimer = 0;
    let prioritySymbols = [];
    let serverClockOffsetMs = null;
    let serverClockRttMs = null;
    let serverClockSyncAt = 0;
    let serverClockSyncPromise = null;
    let observabilityEnabled = false;
    let observabilityPostCount = 0;
    
    function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {
      if (!observabilityEnabled) {
        self.postMessage({ type, symbol, ...payload });
        return;
      }
      const observerStartedAt = performance.now();
      const message = { type, symbol, ...payload };
      const samplePayload = observabilityPostCount++ % 50 === 0;
      let payloadBytes = null;
      if (samplePayload) {
        try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}
      }
      const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);
      message.__obs = {
        sentAtEpochMs: Date.now(),
        processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,
        observerOverheadMs: performance.now() - observerStartedAt,
        payloadBytes,
        payloadSampleRate: 50,
        sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,
        sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,
        sourceKind: obsContext.sourceKind ?? null,
      };
      self.postMessage(message);
    }
    
    function diagnose(symbol, phase, details = {}) {
      if (!observabilityEnabled) return;
      post("diagnostic", symbol, {
        diagnostic: {
          phase,
          atEpochMs: Date.now(),
          ...details,
        },
      });
    }
    
    function reconnectDelay(attempt = 0) {
      const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));
      const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));
      return exponential + jitter;
    }
    
    function bookSideQuoteScale(levels, sampleLimit = 1_024) {
      const totalLevels = Number.isFinite(levels?.size)
        ? levels.size
        : (Number(levels?.length) || 0);
      const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));
      const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));
      const sample = [];
      let maximum = 1;
      let validIndex = 0;
      for (const [priceValue, quantityValue] of levels ?? []) {
        const quote = Number(priceValue) * Number(quantityValue);
        if (!Number.isFinite(quote) || quote <= 0) continue;
        if (quote > maximum) maximum = quote;
        if (validIndex % sampleStride === 0) sample.push(quote);
        validIndex += 1;
      }
      sample.sort((left, right) => left - right);
      const quantile = (ratio) => sample.length
        ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]
        : 0;
      const median = quantile(.5);
      const upper = quantile(.95);
      const extreme = quantile(.99);
      return {
        maximum,
        anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),
        sampledLevels: sample.length,
        totalLevels: validIndex,
      };
    }
    
    function bookQuoteScale(bids, asks, sampleLimit = 2_048) {
      const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));
      const bid = bookSideQuoteScale(bids ?? [], sideLimit);
      const ask = bookSideQuoteScale(asks ?? [], sideLimit);
      return {
        maximum: Math.max(bid.maximum, ask.maximum),
        anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),
        bidAnomalyThreshold: bid.anomalyThreshold,
        askAnomalyThreshold: ask.anomalyThreshold,
        sampledLevels: bid.sampledLevels + ask.sampledLevels,
        totalLevels: bid.totalLevels + ask.totalLevels,
      };
    }
    
    function parsePayload(raw) {
      let payload;
      try { payload = JSON.parse(raw); } catch { return null; }
      if (payload?.result === null || payload?.id) return null;
      return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };
    }
    
    function normalizeTrade(event, sourceHint = null, receivedAt = null) {
      const eventType = String(event?.e ?? "").toLowerCase();
      const inferredRaw = eventType === "trade"
        || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));
      const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";
      const price = Number(event?.p);
      const quantity = Number(event?.q);
      const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);
      const time = timing.tradeTime;
      const id = source === "raw" ? Number(event?.t) : Number(event?.a);
      const firstTradeId = source === "raw" ? id : Number(event?.f);
      const lastTradeId = source === "raw" ? id : Number(event?.l);
      if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;
      if (price <= 0 || quantity <= 0 || time <= 0) return null;
      if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;
      return {
        id,
        firstTradeId,
        lastTradeId,
        source,
        price,
        quantity,
        quote: price * quantity,
        time,
        tradeTime: timing.tradeTime,
        eventTime: timing.eventTime,
        receivedAt: timing.receivedAt,
        rxLatencyMs: timing.rxLatencyMs,
        side: event?.m ? "sell" : "buy",
      };
    }
    
    function calibratedSourceLag(eventTime, receivedAt = Date.now()) {
      const sourceTime = Number(eventTime);
      const localTime = Number(receivedAt);
      if (
        !Number.isFinite(sourceTime)
        || !Number.isFinite(localTime)
        || !Number.isFinite(serverClockOffsetMs)
      ) return null;
      const lag = localTime + serverClockOffsetMs - sourceTime;
      if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;
      return Math.max(0, lag);
    }
    
    function sequenceDecision(lastUpdateId, event, firstEvent = false) {
      const first = Number(event?.U);
      const final = Number(event?.u);
      const previous = Number(event?.pu);
      const local = Number(lastUpdateId);
      if (![first, final, local].every(Number.isFinite)) return "resync";
      if (final <= local) return "ignore";
      if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";
      if (Number.isFinite(previous) && previous !== local) return "resync";
      if (!Number.isFinite(previous) && first > local + 1) return "resync";
      return "apply";
    }
    
    async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {
      const controller = typeof AbortController === "function" ? new AbortController() : null;
      let timer;
      const abortFromExternal = () => controller?.abort();
      if (externalSignal?.aborted) abortFromExternal();
      else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });
      try {
        const timeout = new Promise((_, reject) => {
          timer = setTimeout(() => {
            controller?.abort();
            reject(new Error("timeout"));
          }, timeoutMs);
        });
        const request = fetch(url, {
          cache: "no-store",
          ...(controller ? { signal: controller.signal } : {}),
        }).then(async (response) => {
          if (!response.ok) throw new Error(`HTTP ${response.status}`);
          return response.json();
        });
        return await Promise.race([request, timeout]);
      } finally {
        clearTimeout(timer);
        externalSignal?.removeEventListener?.("abort", abortFromExternal);
      }
    }
    
    async function syncServerClock(force = false) {
      const now = Date.now();
      if (
        !force
        && Number.isFinite(serverClockOffsetMs)
        && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS
      ) return serverClockOffsetMs;
      if (serverClockSyncPromise) return serverClockSyncPromise;
    
      serverClockSyncPromise = (async () => {
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });
        let result = null;
        try {
          result = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const startedAt = Date.now();
              const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);
              const finishedAt = Date.now();
              const serverTime = Number(data?.serverTime);
              if (!Number.isFinite(serverTime)) throw new Error("invalid server time");
              return {
                rtt: Math.max(0, finishedAt - startedAt),
                offset: serverTime - (startedAt + finishedAt) / 2,
              };
            },
            {
              onAttempt: (event) => diagnose("", "clock.rest.host", {
                ...event,
                host: event.target,
                target: undefined,
              }),
            },
          );
        } catch {}
        const best = result?.value;
        if (best) {
          serverClockOffsetMs = best.offset;
          serverClockRttMs = best.rtt;
          serverClockSyncAt = Date.now();
          diagnose("", "clock.rest", {
            state: "succeeded",
            host: result.target,
            durationMs: result.durationMs,
            rttMs: best.rtt,
          });
        } else {
          diagnose("", "clock.rest", { state: "failed" });
        }
        return serverClockOffsetMs;
      })().finally(() => {
        serverClockSyncPromise = null;
      });
      return serverClockSyncPromise;
    }
    
    function depthTransports(symbol, mode) {
      const stream = `${symbol.toLowerCase()}@${mode === "partial" ? "depth20" : "depth"}@100ms`;
      return [
        { name: "public · combined", url: `wss://fstream.binance.com/public/stream?streams=${stream}`, subscribe: false, stream },
        { name: "public · raw", url: `wss://fstream.binance.com/public/ws/${stream}`, subscribe: false, stream },
      ];
    }
    
    function tradeStreams(symbol) {
      const name = symbol.toLowerCase();
      // Production Tape uses the documented USD-M Futures @aggTrade stream.
      // Undocumented @trade remains excluded until a separate shadow study proves
      // continuity, duplication and latency behaviour over a long observation.
      return [`${name}@aggTrade`];
    }
    
    function tradeTransports(streams) {
      const stream = String(streams?.[0] ?? "");
      return [
        { name: "market · combined", url: `wss://fstream.binance.com/market/stream?streams=${stream}`, subscribe: false, streams: [stream] },
        { name: "market · raw", url: `wss://fstream.binance.com/market/ws/${stream}`, subscribe: false, streams: [stream] },
      ];
    }
    
    function trimSide(levels, side, limit) {
      if (levels.size <= limit) return;
      const keys = [...levels.keys()].sort(side === "bid" ? (a, b) => b - a : (a, b) => a - b);
      for (const price of keys.slice(limit)) levels.delete(price);
    }
    
    class SymbolFeed {
      constructor(symbol) {
        this.symbol = symbol;
        this.subscribers = 0;
        this.closeTimer = 0;
        this.socket = null;
        this.tradeSocket = null;
        this.reconnectTimer = 0;
        this.tradeReconnectTimer = 0;
        this.tradeFirstMessageTimer = 0;
        this.firstDepthTimer = 0;
        this.snapshotTimer = 0;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.generation = 0;
        this.bids = new Map();
        this.asks = new Map();
        this.partialBidKeys = new Set();
        this.partialAskKeys = new Set();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.resyncCount = 0;
        this.dirty = false;
        this.forceEmit = false;
        this.lastEmitAt = 0;
        this.cachedSorted = null;
        this.bookEvents = new self.InPulsOrderBookEvents.DepthEventJournal({ symbol });
        this.densityLifecycle = new self.InPulsOrderBookDensity.DensityLifecycleTracker({ symbol });
        this.statusKey = "";
        this.trades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.tradeIds = new Set();
        this.tapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.aggregationTrades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.aggregationTradeIds = new Set();
        this.aggregationTapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.backgroundPaused = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.lastMessageAt = 0;
        this.lastRestartAt = 0;
        this.syncing = false;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.lastResyncAt = 0;
        this.tradeTransportName = "—";
        this.flowWindowStartedAt = Date.now();
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
        this.latestFlowHealth = null;
        this.tapeGuard = new self.InPulsTapeGuard({ rawWarmupTrades: 6, rawStaleMs: 1_500 });
        this.tradeLatency = new self.InPulsTapeLatency.RollingLatency({ windowMs: 2_000, maxSamples: 400, updateMs: 250 });
      }
    
      tradeBoundary() {
        let boundary = null;
        for (const trade of this.trades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeSnapshot(limit = MAX_TRADE_HISTORY) {
        return this.trades.toArray(limit);
      }
    
      aggregationBoundary() {
        let boundary = null;
        for (const trade of this.aggregationTrades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeKey(trade) {
        const firstTradeId = Number.isInteger(Number(trade?.firstTradeId))
          ? Number(trade.firstTradeId)
          : trade?.id;
        const lastTradeId = Number.isInteger(Number(trade?.lastTradeId))
          ? Number(trade.lastTradeId)
          : trade?.id;
        return `${firstTradeId}:${lastTradeId}:${trade?.time}:${trade?.price}:${trade?.quantity}`;
      }
    
      resetTapeGuard() {
        this.tapeGuard.reset({ lastOutputTradeId: this.aggregationBoundary() });
      }
    
      addSubscriber() {
        const wasZero = this.subscribers === 0;
        const wasCoolingDown = Boolean(this.closeTimer);
        this.subscribers += 1;
        clearTimeout(this.closeTimer);
        this.closeTimer = 0;
        if (wasZero && !wasCoolingDown) this.start();
        else this.refresh();
      }
    
      removeSubscriber() {
        this.subscribers = Math.max(0, this.subscribers - 1);
        if (this.subscribers > 0 || this.closeTimer) return;
        this.closeTimer = setTimeout(() => {
          this.closeTimer = 0;
          if (this.subscribers === 0) {
            this.stop();
            feeds.delete(this.symbol);
          }
        }, IDLE_CLOSE_MS);
      }
    
      start() {
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tradeLatency.reset();
        this.resetFlowWindow();
        this.syncing = false;
        this.backgroundPaused = false;
        this.resetTapeGuard();
        this.resetBook("start");
        this.setStatus("loading", "Подключение Worker");
        const generation = this.generation;
        diagnose(this.symbol, "feed.start", {
          state: "started",
          generation,
          subscribers: this.subscribers,
        });
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      stopSockets() {
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        try { this.socket?.close(); } catch {}
        try { this.tradeSocket?.close(); } catch {}
        this.socket = null;
        this.tradeSocket = null;
        this.tapeGuard.disconnect("socket-stop");
      }
    
      stop() {
        diagnose(this.symbol, "feed.stop", {
          state: "started",
          generation: this.generation,
        });
        this.generation += 1;
        this.stopSockets();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
      }
    
      resetBook(reason = "reset") {
        this.bids.clear();
        this.asks.clear();
        this.partialBidKeys.clear();
        this.partialAskKeys.clear();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.cachedSorted = null;
        this.dirty = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        const bookEpoch = this.bookEvents.reset(reason);
        this.densityLifecycle.reset({ bookEpoch, reason });
      }
    
      setStatus(state, text) {
        const key = `${state}:${text}`;
        if (key === this.statusKey) return;
        this.statusKey = key;
        post("status", this.symbol, { state, text });
      }
    
      latencyText() {
        const latency = this.tradeLatency.current();
        if (!Number.isFinite(latency)) return "";
        const value = latency < 1
          ? "<1"
          : latency < 10
            ? latency.toFixed(1)
            : String(Math.round(latency));
        return ` · RX ${value}ms`;
      }
    
      liveStatusText(tapeState = null) {
        const partial = this.mode === "partial" ? " · 20" : "";
        const reconnectingTape = tapeState === "reconnect" || (this.tradeLive && !this.tradeConnected);
        const tape = reconnectingTape
          ? " · TAPE RECONNECT"
          : (this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : "");
        return `LIVE 100ms · WORKER${partial}${tape}${this.latencyText()}`;
      }
    
      publishLiveStatus(tapeState = null) {
        if (this.syncing) {
          this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          return;
        }
        const depthAge = this.lastDepthAt ? Date.now() - this.lastDepthAt : Infinity;
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
          return;
        }
        this.setStatus("online", this.liveStatusText(tapeState));
      }
    
      markDirty(force = false) {
        this.dirty = true;
        if (force) this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      refresh() {
        this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      pauseForBackground() {
        if (this.subscribers <= 0 || this.backgroundPaused) return;
        this.backgroundPaused = true;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.generation += 1;
        this.stopSockets();
        this.tradeConnected = false;
        this.tradeLive = false;
        this.tradeLatency.reset();
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Пауза Worker",
        );
      }
    
      resume(delayMs = 0, epoch = visibilityEpoch) {
        clearTimeout(this.resumeTimer);
        this.resumeTimer = setTimeout(() => {
          this.resumeTimer = 0;
          if (!tabVisible || epoch !== visibilityEpoch || this.subscribers <= 0) return;
    
          const now = Date.now();
          const socketOpen = this.socket?.readyState === WebSocket.OPEN;
          const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
          const depthFresh = this.lastDepthAt > 0 && now - this.lastDepthAt <= RESUME_STALE_MS;
    
          if (!this.backgroundPaused && socketOpen && depthFresh) {
            post("tape", this.symbol, {
              replace: true,
              liveOnly: true,
              trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            }, null, { sourceKind: "live-reset" });
            if (!tradeOpen && !this.tradeReconnectTimer) {
              this.connectTrades(this.generation);
            }
            this.forceEmit = true;
            this.emit(now, MAX_RESUME_LEVELS_PER_SIDE, true);
            this.publishLiveStatus(tradeOpen ? null : "reconnect");
            return;
          }
    
          this.backgroundPaused = false;
          this.restartAfterBackground(true);
        }, Math.max(0, delayMs));
      }
    
      restartAfterBackground(force = false) {
        if (this.subscribers <= 0) return;
        const now = Date.now();
        if (!force && now - this.lastRestartAt < 2_500) return;
        this.lastRestartAt = now;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tapeGuard.disconnect("background-restart");
        this.resetFlowWindow();
        this.resetBook("background-restart");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Восстановление Worker",
        );
        const generation = this.generation;
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        }, null, { sourceKind: "live-reset" });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      resetFlowWindow(now = Date.now()) {
        this.flowWindowStartedAt = now;
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
      }
    
      recordFlow(kind, startedAt) {
        const durationMs = Math.max(0, performance.now() - startedAt);
        if (kind === "depth") {
          this.depthEventsInWindow += 1;
          this.depthProcessMsInWindow += durationMs;
          this.depthProcessMaxMsInWindow = Math.max(this.depthProcessMaxMsInWindow, durationMs);
          return;
        }
        this.tradeEventsInWindow += 1;
        this.tradeProcessMsInWindow += durationMs;
        this.tradeProcessMaxMsInWindow = Math.max(this.tradeProcessMaxMsInWindow, durationMs);
      }
    
      reportFlow(now = Date.now()) {
        const durationMs = Math.max(1, now - this.flowWindowStartedAt);
        const perSecond = 1_000 / durationMs;
        const snapshot = {
          durationMs,
          depthEventsPerSecond: this.depthEventsInWindow * perSecond,
          tradeEventsPerSecond: this.tradeEventsInWindow * perSecond,
          depthProcessMeanMs: this.depthEventsInWindow
            ? this.depthProcessMsInWindow / this.depthEventsInWindow
            : 0,
          tradeProcessMeanMs: this.tradeEventsInWindow
            ? this.tradeProcessMsInWindow / this.tradeEventsInWindow
            : 0,
          depthProcessMaxMs: this.depthProcessMaxMsInWindow,
          tradeProcessMaxMs: this.tradeProcessMaxMsInWindow,
          tapeQueue: this.tapeBatch.length,
          tapeDropped: this.tapeDroppedInWindow,
          depthSourceLagMs: this.lastDepthSourceLagMs,
          tradeSourceLagMs: this.lastTradeSourceLagMs,
        };
        this.latestFlowHealth = snapshot;
        diagnose(this.symbol, "worker.flow", {
          state: "sampled",
          generation: this.generation,
          ...snapshot,
        });
        this.resetFlowWindow(now);
        return snapshot;
      }
    
      ensureHealthy(now = Date.now()) {
        if (!tabVisible || this.subscribers <= 0) return;
        const recoveryDelayed = this.syncing
          && this.lastRestartAt > 0
          && now - this.lastRestartAt > RECOVERY_TIMEOUT_MS;
        if (recoveryDelayed) {
          this.setStatus("stale", "RECOVERY DELAY · повторное подключение");
          this.restartAfterBackground(true);
          return;
        }
        const socketOpen = this.socket?.readyState === WebSocket.OPEN;
        const socketConnecting = this.socket?.readyState === WebSocket.CONNECTING;
        const reconnectPending = Boolean(this.reconnectTimer || this.firstDepthTimer || this.snapshotLoading);
        const depthAge = this.lastDepthAt > 0 ? now - this.lastDepthAt : Infinity;
        const depthSourceStale = this.depthReady
          && Number.isFinite(this.lastDepthSourceLagMs)
          && this.lastDepthSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const stale = this.depthReady && (depthAge > ACTIVE_STALE_MS || depthSourceStale);
    
        if ((!socketOpen && !socketConnecting && !reconnectPending) || stale) {
          if (stale) {
            diagnose(this.symbol, "depth.freshness", {
              state: "stale",
              generation: this.generation,
              depthAgeMs: depthAge,
              sourceLagMs: this.lastDepthSourceLagMs,
              socketOpen,
            });
          }
          this.setStatus("offline", "RECONNECT · WORKER");
          this.restartAfterBackground();
          return;
        }
    
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
        } else if (this.depthReady && this.statusKey.startsWith("stale:")) {
          this.publishLiveStatus();
        }
    
        const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
        const tradeConnecting = this.tradeSocket?.readyState === WebSocket.CONNECTING;
        const tradeAge = this.lastTradeAt > 0 ? now - this.lastTradeAt : Infinity;
        const tradeSourceStale = Number.isFinite(this.lastTradeSourceLagMs)
          && this.lastTradeSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const tradeStale = this.tradeConnected
          && (tradeAge > TRADE_ACTIVE_STALE_MS || tradeSourceStale);
        if (tradeOpen && tradeStale) {
          diagnose(this.symbol, "tape.freshness", {
            state: "stale",
            generation: this.generation,
            tradeAgeMs: tradeAge,
            sourceLagMs: this.lastTradeSourceLagMs,
            socketOpen: true,
          });
          this.publishLiveStatus("reconnect");
          try { this.tradeSocket?.close(); } catch {}
          return;
        }
        if (!tradeOpen && !tradeConnecting && !this.tradeReconnectTimer) {
          if (this.tradeLive) this.publishLiveStatus("reconnect");
          this.connectTrades(this.generation);
        }
      }
    
      priorityRank() {
        const index = prioritySymbols.indexOf(this.symbol);
        return index < 0 ? Number.MAX_SAFE_INTEGER : index;
      }
    
      emittedLimit() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1) return MAX_EMITTED_LEVELS_PER_SIDE;
        if (this.priorityRank() === 0) return active <= 2 ? 1_800 : 900;
        if (active === 2) return 900;
        if (active <= 4) return 450;
        return 280;
      }
    
      emitIntervalMs() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1 || this.priorityRank() === 0) return 100;
        if (active <= 4) return 220;
        return 320;
      }
    
      bookStorageLimit() {
        // UI emission is throttled separately. Never destroy the deep local
        // book merely because several panels are open.
        return MAX_BOOK_LEVELS_PER_SIDE;
      }
    
      sortedDepth() {
        if (this.cachedSorted) return this.cachedSorted;
        const rawBids = [...this.bids.entries()];
        const rawAsks = [...this.asks.entries()];
        const bids = rawBids.sort((a, b) => b[0] - a[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const asks = rawAsks.sort((a, b) => a[0] - b[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const quoteScale = bookQuoteScale(rawBids, rawAsks);
        this.cachedSorted = {
          bids,
          asks,
          sizeScaleMaxQuote: quoteScale.maximum,
          sizeAnomalyThresholdQuote: quoteScale.anomalyThreshold,
          sizeAnomalyThresholdBidQuote: quoteScale.bidAnomalyThreshold,
          sizeAnomalyThresholdAskQuote: quoteScale.askAnomalyThreshold,
        };
        return this.cachedSorted;
      }
    
      emit(now = Date.now(), requestedLimit = this.emittedLimit(), force = false) {
        const processStartedAt = observabilityEnabled ? performance.now() : null;
        if (!tabVisible || this.subscribers <= 0 || (!force && !this.dirty && !this.forceEmit)) return;
        if (!force && !this.forceEmit && now - this.lastEmitAt < this.emitIntervalMs()) return;
        const fullView = this.sortedDepth();
        const limit = Math.max(100, Math.min(MAX_EMITTED_LEVELS_PER_SIDE, Math.floor(requestedLimit)));
        const view = self.InPulsOrderBookDepthProjection.compactDepthView(fullView, {
          exactLimit: limit,
          densityLimit: 96,
          bandCount: 128,
        });
        if (!view.bids.length || !view.asks.length) return;
        const bestBid = Number(fullView.bids[0][0]);
        const bestAsk = Number(fullView.asks[0][0]);
        const middle = (bestBid + bestAsk) / 2;
        const lowestBid = Number(view.bids.at(-1)?.[0]);
        const highestAsk = Number(view.asks.at(-1)?.[0]);
        this.densityLifecycle.refresh({
          bids: fullView.bids,
          asks: fullView.asks,
          now,
        });
        post("data", this.symbol, {
          data: {
            symbol: this.symbol,
            bids: view.bids,
            asks: view.asks,
            trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            bestBid,
            bestAsk,
            lastUpdateId: this.lastUpdateId,
            eventTime: now,
            depthReady: this.depthReady,
            coverage: {
              bidPercent: Number.isFinite(lowestBid) ? Math.max(0, ((middle - lowestBid) / middle) * 100) : 0,
              askPercent: Number.isFinite(highestAsk) ? Math.max(0, ((highestAsk - middle) / middle) * 100) : 0,
            },
            bookLevels: { bids: this.bids.size, asks: this.asks.size },
            sizeScaleMaxQuote: fullView.sizeScaleMaxQuote,
            sizeAnomalyThresholdQuote: fullView.sizeAnomalyThresholdQuote,
            sizeAnomalyThresholdBidQuote: fullView.sizeAnomalyThresholdBidQuote,
            sizeAnomalyThresholdAskQuote: fullView.sizeAnomalyThresholdAskQuote,
            resyncCount: this.resyncCount,
            orderBookEvents: this.bookEvents.summary(),
            densityLifecycle: this.densityLifecycle.summary(now),
            depthProjection: view.metadata,
            health: {
              mode: this.mode,
              depthAgeMs: this.lastDepthAt ? Math.max(0, now - this.lastDepthAt) : null,
              tradeAgeMs: this.lastTradeAt ? Math.max(0, now - this.lastTradeAt) : null,
              depthSourceLagMs: this.lastDepthSourceLagMs,
              tradeSourceLagMs: this.lastTradeSourceLagMs,
              depthBuffer: this.depthBuffer.length,
              tapeQueue: this.tapeBatch.length,
              subscribers: this.subscribers,
              depthTransport: this.transportIndex,
              tradeTransport: this.tradeTransportIndex,
              tradeTransportName: this.tradeTransportName,
              syncing: this.syncing,
              flow: this.latestFlowHealth,
              tape: this.tapeGuard.snapshot(now),
            },
            worker: true,
          },
        }, processStartedAt, {
          sourceEventTimeMs: this.lastDepthEventTime,
          sourceKind: this.lastDepthEventTime ? "live-depth" : "snapshot-depth",
        });
        this.lastEmitAt = now;
        this.dirty = false;
        this.forceEmit = false;
      }
    
      trimBook() {
        const limit = this.bookStorageLimit();
        trimSide(this.bids, "bid", limit);
        trimSide(this.asks, "ask", limit);
      }
    
      applyDepth(event, first = false) {
        const decision = sequenceDecision(this.lastUpdateId, event, first);
        if (decision === "ignore") return true;
        if (decision === "resync") {
          this.resync("Разрыв последовательности");
          return false;
        }
        const bookEvents = this.bookEvents.applyDiff({
          bids: this.bids,
          asks: this.asks,
          event,
          continuity: this.depthReady ? "live" : "recovered",
          receivedAt: Number(event?.__receivedAt) || Date.now(),
        });
        this.densityLifecycle.ingest(bookEvents);
        this.lastUpdateId = Number(event.u);
        this.cachedSorted = null;
        this.trimBook();
        this.markDirty();
        return true;
      }
    
      bufferDepth(event) {
        this.depthBuffer.push(event);
        if (this.depthBuffer.length > MAX_BUFFERED_DEPTH_EVENTS) {
          this.depthBuffer.splice(0, this.depthBuffer.length - MAX_BUFFERED_DEPTH_EVENTS);
          this.resync("Переполнение буфера");
        }
      }
    
      installSnapshot() {
        const snapshot = this.pendingSnapshot;
        if (!snapshot) return false;
        const snapshotId = Number(snapshot.lastUpdateId);
        const applicable = this.depthBuffer.filter((event) => Number(event?.u) > snapshotId);
        const bridgeIndex = applicable.findIndex(
          (event) => Number(event?.U) <= snapshotId + 1 && Number(event?.u) >= snapshotId + 1,
        );
        if (bridgeIndex < 0) {
          const firstU = Number(applicable[0]?.U);
          if (Number.isFinite(firstU) && firstU > snapshotId + 1) {
            diagnose(this.symbol, "depth.snapshot.bridge", {
              state: "gap",
              generation: this.generation,
              snapshotId,
              firstBufferedUpdateId: firstU,
            });
            this.pendingSnapshot = null;
            clearTimeout(this.snapshotTimer);
            this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), 250);
          }
          return false;
        }
        this.bids = new Map();
        this.asks = new Map();
        const snapshotReceivedAt = Date.now();
        const baseline = this.bookEvents.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          snapshot,
          receivedAt: snapshotReceivedAt,
        });
        this.densityLifecycle.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          bookEpoch: this.bookEvents.bookEpoch,
          receivedAt: snapshotReceivedAt,
        });
        this.lastUpdateId = baseline.snapshotId;
        this.cachedSorted = null;
        for (let index = bridgeIndex; index < applicable.length; index += 1) {
          if (!this.applyDepth(applicable[index], index === bridgeIndex)) return false;
        }
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.depthReady = true;
        this.syncing = false;
        this.bookEvents.markReady();
        this.densityLifecycle.markReady();
        diagnose(this.symbol, "depth.live", {
          state: "ready",
          generation: this.generation,
          mode: this.mode,
          snapshotId,
          bufferedEvents: applicable.length - bridgeIndex,
          bids: this.bids.size,
          asks: this.asks.size,
          orderBookEvents: this.bookEvents.summary(),
        });
        this.publishLiveStatus();
        this.markDirty(true);
        return true;
      }
    
      async loadSnapshot(generation) {
        if (generation !== this.generation || this.mode !== "deep" || this.snapshotLoading) return;
        this.snapshotLoading = true;
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        let snapshot = null;
        let winner = null;
        diagnose(this.symbol, "depth.snapshot", {
          state: "scheduled",
          generation,
          hosts: hosts.length,
        });
        try {
          winner = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const data = await fetchJson(
                `https://${host}/fapi/v1/depth?symbol=${encodeURIComponent(this.symbol)}&limit=1000`,
                SNAPSHOT_TIMEOUT_MS,
                signal,
              );
              if (!Array.isArray(data?.bids) || !Array.isArray(data?.asks) || !Number.isFinite(Number(data?.lastUpdateId))) {
                throw new Error("invalid snapshot");
              }
              return data;
            },
            {
              onAttempt: (event) => diagnose(this.symbol, "depth.snapshot.host", {
                ...event,
                host: event.target,
                target: undefined,
                generation,
              }),
            },
          );
          snapshot = winner.value;
        } catch {}
        this.snapshotLoading = false;
        if (generation !== this.generation || this.mode !== "deep") return;
        if (!snapshot) {
          diagnose(this.symbol, "depth.snapshot", {
            state: "failed",
            generation,
          });
          this.activatePartial(generation);
          return;
        }
        diagnose(this.symbol, "depth.snapshot", {
          state: "succeeded",
          generation,
          host: winner.target,
          durationMs: winner.durationMs,
          snapshotId: Number(snapshot.lastUpdateId),
        });
        this.pendingSnapshot = snapshot;
        this.installSnapshot();
      }
    
      activatePartial(generation) {
        if (generation !== this.generation || this.mode === "partial") return;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.mode = "partial";
        this.transportIndex = 0;
        this.syncing = preserveLastFrame;
        this.resetBook("partial-fallback");
        this.bookEvents.markUnavailable("partial-depth");
        this.densityLifecycle.markUnavailable("partial-depth");
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Резервный Worker-стакан",
        );
        diagnose(this.symbol, "depth.fallback", {
          state: "activated",
          generation,
          mode: "partial",
        });
        try { this.socket?.close(); } catch {}
        this.socket = null;
        this.reconnectTimer = setTimeout(() => this.connectDepth(generation), 0);
      }
    
      resync(text) {
        if (this.mode !== "deep") return;
        const now = Date.now();
        if (now - this.lastResyncAt < 350) return;
        this.lastResyncAt = now;
        this.resyncCount += 1;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.resetBook(text === "Переполнение буфера" ? "buffer-overflow" : "sequence-gap");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? `СИНХРОНИЗАЦИЯ · ${text}` : text,
        );
        clearTimeout(this.snapshotTimer);
        const delay = Math.min(2_000, 250 + this.resyncCount * 75);
        this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), delay);
      }
    
      replacePartial(target, previousKeys, rows) {
        const nextKeys = new Set();
        for (const row of rows ?? []) {
          const price = Number(row?.[0]);
          const quantity = Number(row?.[1]);
          if (!Number.isFinite(price) || !Number.isFinite(quantity)) continue;
          nextKeys.add(price);
          if (quantity > 0) target.set(price, quantity);
          else target.delete(price);
        }
        for (const price of previousKeys) if (!nextKeys.has(price)) target.delete(price);
        return nextKeys;
      }
    
      connectDepth(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.firstDepthTimer);
        const transports = depthTransports(this.symbol, this.mode);
        const transport = transports[this.transportIndex % transports.length];
        const transportIndex = this.transportIndex % transports.length;
        diagnose(this.symbol, "depth.ws.create", {
          state: "started",
          generation,
          mode: this.mode,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "depth.ws.create", {
            state: "failed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.transportIndex += 1;
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
          return;
        }
        this.socket = socket;
        this.firstDepthTimer = setTimeout(() => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.first-message", {
              state: "timeout",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              durationMs: 8_000,
            });
            try { socket.close(); } catch {}
          }
        }, 8_000);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.socket) return;
          diagnose(this.symbol, "depth.ws.open", {
            state: "succeeded",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
          });
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: [transport.stream],
              id: Date.now() % 2_147_483_647,
            }));
          }
          if (this.syncing) this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          else this.setStatus("loading", this.mode === "deep" ? "Синхронизация Worker" : "Подключаю резерв Worker");
          if (this.mode === "deep") this.loadSnapshot(generation);
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.socket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            this.lastMessageAt = Date.now();
    
            const bids = update?.b ?? update?.bids;
            const asks = update?.a ?? update?.asks;
            if (!Array.isArray(bids) || !Array.isArray(asks)) return;
            const receivedFirstDepth = this.lastDepthAt === 0;
            const receivedAt = Date.now();
            update.__receivedAt = receivedAt;
            this.lastDepthAt = receivedAt;
            const sourceEventTime = Number(update?.E);
            if (Number.isFinite(sourceEventTime)) {
              this.lastDepthEventTime = sourceEventTime;
              this.lastDepthSourceLagMs = calibratedSourceLag(sourceEventTime, receivedAt);
            }
            if (receivedFirstDepth) {
              diagnose(this.symbol, "depth.ws.first-message", {
                state: "received",
                generation,
                mode: this.mode,
                transport: this.transportIndex,
                transportName: transport.name,
              });
            }
            clearTimeout(this.firstDepthTimer);
            this.transportIndex = 0;
            this.depthReconnectAttempt = 0;
    
            if (this.mode === "partial") {
              this.partialBidKeys = this.replacePartial(this.bids, this.partialBidKeys, bids);
              this.partialAskKeys = this.replacePartial(this.asks, this.partialAskKeys, asks);
              this.lastUpdateId = Number(update.u ?? update.lastUpdateId) || this.lastUpdateId;
              this.depthReady = true;
              this.syncing = false;
              this.cachedSorted = null;
              this.publishLiveStatus();
              this.markDirty(true);
              return;
            }
    
            if (!Number.isFinite(Number(update?.U)) || !Number.isFinite(Number(update?.u))) return;
            if (!this.depthReady) {
              this.bufferDepth(update);
              if (!this.pendingSnapshot && !this.snapshotLoading) this.loadSnapshot(generation);
              this.installSnapshot();
              return;
            }
            this.applyDepth(update);
          } finally {
            this.recordFlow("depth", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.socket) return;
          clearTimeout(this.firstDepthTimer);
          this.socket = null;
          this.transportIndex += 1;
          diagnose(this.symbol, "depth.ws.close", {
            state: "closed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          const preserveLastFrame = this.depthReady || this.syncing;
          this.syncing = preserveLastFrame;
          this.resetBook("reconnect");
          this.setStatus(
            preserveLastFrame ? "stale" : "offline",
            preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "RECONNECT · WORKER",
          );
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.error", {
              state: "failed",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      connectTrades(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
    
        const streams = tradeStreams(this.symbol);
        const transports = tradeTransports(streams);
        const transport = transports[this.tradeTransportIndex % transports.length];
        const transportIndex = this.tradeTransportIndex % transports.length;
        diagnose(this.symbol, "tape.ws.create", {
          state: "started",
          generation,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "tape.ws.create", {
            state: "failed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.tradeTransportIndex += 1;
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
          return;
        }
        this.tradeSocket = socket;
        this.tradeTransportName = transport.name;
        let receivedTrade = false;
        this.tradeFirstMessageTimer = setTimeout(() => {
          if (generation !== this.generation || socket !== this.tradeSocket || receivedTrade) return;
          diagnose(this.symbol, "tape.ws.first-message", {
            state: "timeout",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            durationMs: TRADE_FIRST_MESSAGE_TIMEOUT_MS,
          });
          try { socket.close(); } catch {}
        }, TRADE_FIRST_MESSAGE_TIMEOUT_MS);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          diagnose(this.symbol, "tape.ws.open", {
            state: "succeeded",
            generation,
            transport: transportIndex,
            transportName: transport.name,
          });
          this.tapeGuard.connect();
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: transport.streams,
              id: Date.now() % 2_147_483_647,
            }));
          }
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            const eventType = String(update?.e ?? "").toLowerCase();
            const payloadStream = payload.stream.toLowerCase();
            const rawEvent = eventType === "trade" || payloadStream.endsWith("@trade");
            const aggregateEvent = eventType === "aggtrade" || payloadStream.endsWith("@aggtrade");
            if (!rawEvent && !aggregateEvent) return;
    
            const receivedAt = Date.now();
            const source = rawEvent ? "raw" : "agg";
            const trade = normalizeTrade(update, source, receivedAt);
            if (!trade) return;
            const firstTradeMessage = !receivedTrade;
            receivedTrade = true;
            clearTimeout(this.tradeFirstMessageTimer);
            this.tradeFirstMessageTimer = 0;
            this.lastTradeAt = receivedAt;
            this.lastTradeEventTime = trade.eventTime;
            this.lastTradeSourceLagMs = calibratedSourceLag(trade.eventTime, receivedAt);
            // RX in the header describes the stable production Tape feed. The parallel
            // experimental @trade channel must not double-weight or zero the rolling median.
            if (aggregateEvent) this.tradeLatency.record(trade.rxLatencyMs, receivedAt);
            this.tradeTransportIndex = 0;
            this.tradeReconnectAttempt = 0;
            this.tradeLive = true;
            this.tradeConnected = true;
            if (firstTradeMessage) {
              diagnose(this.symbol, "tape.ws.first-message", {
                state: "received",
                generation,
                transport: transportIndex,
                transportName: transport.name,
                source,
              });
            }
    
            const decision = this.tapeGuard.ingest(trade, this.lastTradeAt);
            let changed = false;
    
            // Preserve the visual RAW contract: it continues to receive only
            // Binance @aggTrade events, exactly as before this feature.
            if (aggregateEvent && this.insertTrade(trade, true)) {
              const matchedDensities = this.densityLifecycle.ingestTrades([trade]);
              if (matchedDensities.length) this.markDirty();
              this.queueTape(trade);
              changed = true;
            }
    
            // The guarded production path follows the same documented @aggTrade
            // stream and keeps continuity/dedup protection without mixing sources.
            if (decision.emit && this.insertAggregationTrade(trade, true)) {
              this.queueAggregationTape(trade);
              changed = true;
            }
    
            this.publishLiveStatus();
            if (!changed && rawEvent && decision.reason !== "raw-warmup") {
              diagnose(this.symbol, "tape.aggregation-source", {
                state: "skipped",
                source,
                reason: decision.reason,
                mode: decision.mode,
              });
            }
          } finally {
            this.recordFlow("trade", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          clearTimeout(this.tradeFirstMessageTimer);
          this.tradeFirstMessageTimer = 0;
          this.tradeSocket = null;
          this.tradeConnected = false;
          this.tapeGuard.disconnect("socket-close");
          this.tradeTransportIndex += 1;
          diagnose(this.symbol, "tape.ws.close", {
            state: "closed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          if (this.tradeLive && this.depthReady) this.publishLiveStatus("reconnect");
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.tradeSocket) {
            diagnose(this.symbol, "tape.ws.error", {
              state: "failed",
              generation,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      insertTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.tradeIds.has(key)) return false;
        this.tradeIds.add(key);
        const evicted = newestFirst
          ? this.trades.prepend(trade)
          : this.trades.append(trade);
        if (evicted === trade) {
          this.tradeIds.delete(key);
          return false;
        }
        if (evicted) this.tradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      insertAggregationTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.aggregationTradeIds.has(key)) return false;
        this.aggregationTradeIds.add(key);
        const evicted = newestFirst
          ? this.aggregationTrades.prepend(trade)
          : this.aggregationTrades.append(trade);
        if (evicted === trade) {
          this.aggregationTradeIds.delete(key);
          return false;
        }
        if (evicted) this.aggregationTradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      scheduleTapeFlush() {
        if (!this.tapeTimer) {
          this.tapeTimer = setTimeout(() => this.flushTapeBatch(), TAPE_FLUSH_MS);
        }
      }
    
      queueTape(trade) {
        if (!trade || !tabVisible) return;
        this.tapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      queueAggregationTape(trade) {
        if (!trade || !tabVisible) return;
        this.aggregationTapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      flushTapeBatch() {
        this.tapeTimer = 0;
        if (!tabVisible) {
          this.tapeBatch.clear();
          this.aggregationTapeBatch.clear();
          return;
        }
        const latest = this.tapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const aggregationLatest = this.aggregationTapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const trades = latest.items;
        const aggregationTrades = aggregationLatest.items;
        this.tapeDroppedInWindow += latest.dropped + aggregationLatest.dropped;
        if (trades.length || aggregationTrades.length) {
          const sourceEventTimeMs = [...trades, ...aggregationTrades].reduce(
            (latestTime, trade) => Math.max(latestTime, Number(trade?.eventTime) || 0),
            0,
          );
          const guard = this.tapeGuard.snapshot();
          post(
            "tape",
            this.symbol,
            {
              replace: false,
              live: true,
              liveOnly: true,
              trades,
              aggregationTrades,
              aggregationSource: guard.mode,
              aggregationHealth: guard,
              backpressure: {
                dropped: latest.dropped + aggregationLatest.dropped,
                pending: this.tapeBatch.length + this.aggregationTapeBatch.length,
              },
            },
            observabilityEnabled ? performance.now() : null,
            { sourceEventTimeMs, sourceKind: "live-trade-dual" },
          );
        }
      }
    
    }
    
    function scheduleEmit() {
      if (!tabVisible || emitTimer) return;
      emitTimer = setTimeout(() => {
        emitTimer = 0;
        if (!tabVisible) return;
    
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0);
        if (!active.length) return;
    
        // Не отправляем несколько тяжёлых книг в UI одним залпом.
        // При 3+ стаканах один тик обслуживает один символ по кругу.
        const budget = active.length <= 2 ? active.length : 1;
        const now = Date.now();
        for (let index = 0; index < budget; index += 1) {
          const feed = active[emitCursor % active.length];
          emitCursor = (emitCursor + 1) % active.length;
          feed.emit(now);
        }
    
        if (active.some((feed) => feed.dirty || feed.forceEmit)) scheduleEmit();
      }, 25);
    }
    
    function scheduleWatchdog() {
      clearTimeout(watchdogTimer);
      watchdogTimer = setTimeout(() => {
        watchdogTimer = 0;
        const now = Date.now();
        if (tabVisible) {
          for (const feed of feeds.values()) {
            if (feed.subscribers <= 0) continue;
            feed.reportFlow(now);
            feed.ensureHealthy(now);
          }
        }
        self.postMessage({
          type: "heartbeat",
          time: now,
          visible: tabVisible,
          activeFeeds: [...feeds.values()].filter((feed) => feed.subscribers > 0).length,
          totalFeeds: feeds.size,
        });
        scheduleWatchdog();
      }, WORKER_HEARTBEAT_MS);
    }
    
    function getFeed(symbol) {
      let feed = feeds.get(symbol);
      if (!feed) {
        feed = new SymbolFeed(symbol);
        feeds.set(symbol, feed);
      }
      return feed;
    }
    
    function cancelBackgroundPause() {
      clearTimeout(backgroundPauseTimer);
      backgroundPauseTimer = 0;
    }
    
    function scheduleBackgroundPause(epoch) {
      cancelBackgroundPause();
      backgroundPauseTimer = setTimeout(() => {
        backgroundPauseTimer = 0;
        if (tabVisible || epoch !== visibilityEpoch) return;
        for (const feed of feeds.values()) feed.pauseForBackground();
      }, BACKGROUND_GRACE_MS);
    }
    
    self.addEventListener("message", (event) => {
      const message = event.data;
      if (!message || typeof message !== "object") return;
    
      if (message.type === "observability") {
        observabilityEnabled = Boolean(message.enabled);
        return;
      }
    
      if (message.type === "visibility") {
        const nextVisible = Boolean(message.visible);
        if (nextVisible === tabVisible) return;
    
        tabVisible = nextVisible;
        visibilityEpoch += 1;
        const epoch = visibilityEpoch;
    
        if (!tabVisible) {
          clearTimeout(emitTimer);
          emitTimer = 0;
          scheduleBackgroundPause(epoch);
          return;
        }
    
        cancelBackgroundPause();
        syncServerClock(true).catch(() => {});
    
        const resumePrioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        prioritySymbols = resumePrioritySymbols;
        const priorityRank = new Map(
          resumePrioritySymbols.map((symbol, index) => [symbol, index]),
        );
        const active = [...feeds.values()]
          .filter((feed) => feed.subscribers > 0)
          .sort((left, right) => (
            (priorityRank.get(left.symbol) ?? Number.MAX_SAFE_INTEGER)
            - (priorityRank.get(right.symbol) ?? Number.MAX_SAFE_INTEGER)
          ));
        active.forEach((feed, index) => feed.resume(index * RESUME_STAGGER_MS, epoch));
        return;
      }
    
      if (message.type === "priority") {
        prioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        return;
      }
    
      const symbol = String(message.symbol ?? "").toUpperCase();
      if (!symbol.endsWith("USDT")) return;
      if (message.type === "subscribe") {
        getFeed(symbol).addSubscriber();
        return;
      }
      if (message.type === "unsubscribe") {
        feeds.get(symbol)?.removeSubscriber();
        return;
      }
      if (message.type === "refresh") {
        feeds.get(symbol)?.refresh();
      }
    });
    
    syncServerClock(true).catch(() => {});
    scheduleWatchdog();
    post("ready", "");
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/InPuls/InPuls/test-tiger-zero-ms-agg-source-v1.mjs:24:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: Main renders AGG from the selected source and keeps fallback
ok 144 - Main renders AGG from the selected source and keeps fallback
  ---
  duration_ms: 0.313076
  type: 'test'
  ...
# Subtest: Tiger-style zero-ms aggregation joins individual same-time same-side executions
ok 145 - Tiger-style zero-ms aggregation joins individual same-time same-side executions
  ---
  duration_ms: 1.191713
  type: 'test'
  ...
# Subtest: Tape marker threshold remains available
ok 146 - Tape marker threshold remains available
  ---
  duration_ms: 0.21771
  type: 'test'
  ...
# Subtest: percentile and summary are deterministic
ok 147 - percentile and summary are deterministic
  ---
  duration_ms: 2.0922
  type: 'test'
  ...
# Subtest: clock offset uses the lowest RTT samples
ok 148 - clock offset uses the lowest RTT samples
  ---
  duration_ms: 0.386471
  type: 'test'
  ...
# Subtest: combined payload source is classified exactly
ok 149 - combined payload source is classified exactly
  ---
  duration_ms: 0.16091
  type: 'test'
  ...
# Subtest: normalization rejects zero values and wrong symbols
ok 150 - normalization rejects zero values and wrong symbols
  ---
  duration_ms: 0.204879
  type: 'test'
  ...
# Subtest: normalization supports valid trade and aggTrade payloads
ok 151 - normalization supports valid trade and aggTrade payloads
  ---
  duration_ms: 0.219182
  type: 'test'
  ...
# Subtest: raw trades match aggregate by receive and render times
ok 152 - raw trades match aggregate by receive and render times
  ---
  duration_ms: 0.307877
  type: 'test'
  ...
# Subtest: null render timestamps are not treated as painted
ok 153 - null render timestamps are not treated as painted
  ---
  duration_ms: 0.210983
  type: 'test'
  ...
# Subtest: run validity fails on reconnect, invalid payload or hidden tab
ok 154 - run validity fails on reconnect, invalid payload or hidden tab
  ---
  duration_ms: 0.160598
  type: 'test'
  ...
# Subtest: verdict requires valid run, quality and material visual lead
ok 155 - verdict requires valid run, quality and material visual lead
  ---
  duration_ms: 0.392694
  type: 'test'
  ...
# Subtest: v2 browser lab compares streams through their routed endpoints
ok 156 - v2 browser lab compares streams through their routed endpoints
  ---
  duration_ms: 1.187611
  type: 'test'
  ...
# Subtest: frame arrays and projection slots are reused
ok 157 - frame arrays and projection slots are reused
  ---
  duration_ms: 1.486783
  type: 'test'
  ...
# Subtest: unfiltered RAW dots are drawn in a bounded number of Canvas batches
ok 158 - unfiltered RAW dots are drawn in a bounded number of Canvas batches
  ---
  duration_ms: 0.479595
  type: 'test'
  ...
# Subtest: status overlays do not mutate DOM twice per frame
ok 159 - status overlays do not mutate DOM twice per frame
  ---
  duration_ms: 0.455647
  type: 'test'
  ...
# Subtest: batched renderer keeps near-fluid cadence under high trade rate
ok 160 - batched renderer keeps near-fluid cadence under high trade rate
  ---
  duration_ms: 0.266878
  type: 'test'
  ...
# Subtest: 60 FPS painter does not force DOM geometry on every frame
ok 161 - 60 FPS painter does not force DOM geometry on every frame
  ---
  duration_ms: 1.374077
  type: 'test'
  ...
# Subtest: render model reuses immutable nodes and skips full sorting
ok 162 - render model reuses immutable nodes and skips full sorting
  ---
  duration_ms: 0.336837
  type: 'test'
  ...
# Subtest: Tape surface theme lookup is cached outside the frame hot path
ok 163 - Tape surface theme lookup is cached outside the frame hot path
  ---
  duration_ms: 0.491482
  type: 'test'
  ...
# Subtest: replace packets reset only current water renderer state
ok 164 - replace packets reset only current water renderer state
  ---
  duration_ms: 0.366786
  type: 'test'
  ...
# Subtest: adding a neighbouring trade cannot change an existing event coordinate
ok 165 - adding a neighbouring trade cannot change an existing event coordinate
  ---
  duration_ms: 2.067426
  type: 'test'
  ...
# Subtest: viewport changes move the whole historical layer coherently
ok 166 - viewport changes move the whole historical layer coherently
  ---
  duration_ms: 2.040504
  type: 'test'
  ...
# Subtest: clock never steps backward and does not depend on packet cadence
ok 167 - clock never steps backward and does not depend on packet cadence
  ---
  duration_ms: 0.234293
  type: 'test'
  ...
# Subtest: renderer does not feed historical items through collision layout
ok 168 - renderer does not feed historical items through collision layout
  ---
  duration_ms: 0.383297
  type: 'test'
  ...
# Subtest: REST kline is normalized
ok 169 - REST kline is normalized
  ---
  duration_ms: 2.978142
  type: 'test'
  ...
# Subtest: stream kline is normalized
ok 170 - stream kline is normalized
  ---
  duration_ms: 0.363712
  type: 'test'
  ...
# Subtest: live update replaces an open candle
ok 171 - live update replaces an open candle
  ---
  duration_ms: 0.194995
  type: 'test'
  ...
# Subtest: new candles append and respect the history limit
ok 172 - new candles append and respect the history limit
  ---
  duration_ms: 0.115555
  type: 'test'
  ...
# Subtest: one-second candles aggregate into a five-second history
ok 173 - one-second candles aggregate into a five-second history
  ---
  duration_ms: 0.415319
  type: 'test'
  ...
# Subtest: screen density keeps at least one distinct pixel slot per rendered candle
ok 174 - screen density keeps at least one distinct pixel slot per rendered candle
  ---
  duration_ms: 0.149201
  type: 'test'
  ...
# Subtest: second-history fallback walks backward through aggregate-trade pages
ok 175 - second-history fallback walks backward through aggregate-trade pages
  ---
  duration_ms: 122.949133
  type: 'test'
  ...
# Subtest: price-axis drag changes vertical scale
ok 176 - price-axis drag changes vertical scale
  ---
  duration_ms: 0.452437
  type: 'test'
  ...
# Subtest: time-axis drag changes visible history
ok 177 - time-axis drag changes visible history
  ---
  duration_ms: 0.41443
  type: 'test'
  ...
# Subtest: NATR normalizes Wilder ATR as a percentage
ok 178 - NATR normalizes Wilder ATR as a percentage
  ---
  duration_ms: 1.960397
  type: 'test'
  ...
# Subtest: Pearson correlation detects aligned and inverse returns
ok 179 - Pearson correlation detects aligned and inverse returns
  ---
  duration_ms: 0.369731
  type: 'test'
  ...
# Subtest: drawing ruler reports signed price change
ok 180 - drawing ruler reports signed price change
  ---
  duration_ms: 0.167574
  type: 'test'
  ...
# Subtest: Ctrl magnet snaps to nearest candle body or wick extreme
ok 181 - Ctrl magnet snaps to nearest candle body or wick extreme
  ---
  duration_ms: 0.220281
  type: 'test'
  ...
# Subtest: Ctrl magnet resolves the candle centered under the cursor
ok 182 - Ctrl magnet resolves the candle centered under the cursor
  ---
  duration_ms: 0.190576
  type: 'test'
  ...
# Subtest: Ctrl magnet snaps the first drawing anchor before the drawing exists
ok 183 - Ctrl magnet snaps the first drawing anchor before the drawing exists
  ---
  duration_ms: 0.19362
  type: 'test'
  ...
# Subtest: live data keeps the fractional manual viewport offset
ok 184 - live data keeps the fractional manual viewport offset
  ---
  duration_ms: 0.110938
  type: 'test'
  ...
# Subtest: time scale marks trading sessions without a separate day marker
ok 185 - time scale marks trading sessions without a separate day marker
  ---
  duration_ms: 0.191266
  type: 'test'
  ...
# Subtest: progressive time scale selects round divisions
ok 186 - progressive time scale selects round divisions
  ---
  duration_ms: 0.113516
  type: 'test'
  ...
# Subtest: price scale uses round and half-round increments
ok 187 - price scale uses round and half-round increments
  ---
  duration_ms: 0.114568
  type: 'test'
  ...
# Subtest: Worker REST startup uses staggered cancellable host fallback
ok 188 - Worker REST startup uses staggered cancellable host fallback
  ---
  duration_ms: 1.048109
  type: 'test'
  ...
# Subtest: connection lifecycle identifies WebSocket, snapshot, retry and LIVE phases
ok 189 - connection lifecycle identifies WebSocket, snapshot, retry and LIVE phases
  ---
  duration_ms: 0.526908
  type: 'test'
  ...
# Subtest: cross-context timing uses epoch timestamps and source kind
ok 190 - cross-context timing uses epoch timestamps and source kind
  ---
  duration_ms: 0.170885
  type: 'test'
  ...
# Subtest: main-thread render diagnostics cover computation, ladder DOM and skipped TAPE
ok 191 - main-thread render diagnostics cover computation, ladder DOM and skipped TAPE
  ---
  duration_ms: 0.43501
  type: 'test'
  ...
# Subtest: new diagnostic runtime files are in the Service Worker release
ok 192 - new diagnostic runtime files are in the Service Worker release
  ---
  duration_ms: 0.13262
  type: 'test'
  ...
# Subtest: percentChange returns a percentage
ok 193 - percentChange returns a percentage
  ---
  duration_ms: 1.915021
  type: 'test'
  ...
# Subtest: ticker filter keeps UM USDT pairs
ok 194 - ticker filter keeps UM USDT pairs
  ---
  duration_ms: 0.424103
  type: 'test'
  ...
# Subtest: symbol validation canonicalizes user input and rejects markup
ok 195 - symbol validation canonicalizes user input and rejects markup
  ---
  duration_ms: 0.246428
  type: 'test'
  ...
# Subtest: SymbolState calculates 15 second move
ok 196 - SymbolState calculates 15 second move
  ---
  duration_ms: 1.388164
  type: 'test'
  ...
# Subtest: book ticker updates the live midpoint without waiting for mini ticker
ok 197 - book ticker updates the live midpoint without waiting for mini ticker
  ---
  duration_ms: 0.290573
  type: 'test'
  ...
# Subtest: trade flow counts fills and aggressive side
ok 198 - trade flow counts fills and aggressive side
  ---
  duration_ms: 0.247632
  type: 'test'
  ...
# Subtest: funding is exposed for radar sorting
ok 199 - funding is exposed for radar sorting
  ---
  duration_ms: 0.261408
  type: 'test'
  ...
# Subtest: historical minutes warm up NATR without waiting
ok 200 - historical minutes warm up NATR without waiting
  ---
  duration_ms: 0.717236
  type: 'test'
  ...
# Subtest: liquidation snapshots are deduplicated
ok 201 - liquidation snapshots are deduplicated
  ---
  duration_ms: 0.9069
  type: 'test'
  ...
# Subtest: liquidation cascade is not mislabeled as the classic order-book cascade
ok 202 - liquidation cascade is not mislabeled as the classic order-book cascade
  ---
  duration_ms: 0.474622
  type: 'test'
  ...
# Subtest: compression is no longer classified as a scalper pattern
ok 203 - compression is no longer classified as a scalper pattern
  ---
  duration_ms: 0.195398
  type: 'test'
  ...
# Subtest: support and resistance breakouts are separate facts
ok 204 - support and resistance breakouts are separate facts
  ---
  duration_ms: 0.215331
  type: 'test'
  ...
# Subtest: knife and sharpening require a sharp move followed by a one-percent reversal
ok 205 - knife and sharpening require a sharp move followed by a one-percent reversal
  ---
  duration_ms: 1.514577
  type: 'test'
  ...
# Subtest: SignalEvent is an immutable formula-versioned snapshot without future results
ok 206 - SignalEvent is an immutable formula-versioned snapshot without future results
  ---
  duration_ms: 13.559721
  type: 'test'
  ...
# Subtest: marketwide pattern events preserve detector evidence and formula identity
ok 207 - marketwide pattern events preserve detector evidence and formula identity
  ---
  duration_ms: 0.527459
  type: 'test'
  ...
# Subtest: SignalContext keeps current facts separate and marks unavailable inputs partial
ok 208 - SignalContext keeps current facts separate and marks unavailable inputs partial
  ---
  duration_ms: 1.635093
  type: 'test'
  ...
# Subtest: context includes observed density interactions but excludes untouched size-only candidates
ok 209 - context includes observed density interactions but excludes untouched size-only candidates
  ---
  duration_ms: 1.226978
  type: 'test'
  ...
# Subtest: SignalObservation starts pending at the exact 15s, 1m, 3m and 5m horizons
ok 210 - SignalObservation starts pending at the exact 15s, 1m, 3m and 5m horizons
  ---
  duration_ms: 1.006705
  type: 'test'
  ...
# Subtest: tracker fills the 15s observation from a continuous live price path
ok 211 - tracker fills the 15s observation from a continuous live price path
  ---
  duration_ms: 4.390636
  type: 'test'
  ...
# Subtest: an observed result is partial when the price path has a material gap
ok 212 - an observed result is partial when the price path has a material gap
  ---
  duration_ms: 1.058855
  type: 'test'
  ...
# Subtest: MFE and MAE follow the signal direction while return stays market-signed
ok 213 - MFE and MAE follow the signal direction while return stays market-signed
  ---
  duration_ms: 0.958507
  type: 'test'
  ...
# Subtest: a missed horizon after a browser pause is unavailable instead of backfilled
ok 214 - a missed horizon after a browser pause is unavailable instead of backfilled
  ---
  duration_ms: 1.143609
  type: 'test'
  ...
# Subtest: the price path stays alive until later horizons resolve
ok 215 - the price path stays alive until later horizons resolve
  ---
  duration_ms: 1.447491
  type: 'test'
  ...
# Subtest: tracker emits once for a continuous signal and rearms only after a real absence
ok 216 - tracker emits once for a continuous signal and rearms only after a real absence
  ---
  duration_ms: 2.2207
  type: 'test'
  ...
# Subtest: tracker stores one card for overlapping detectors in the same market episode
ok 217 - tracker stores one card for overlapping detectors in the same market episode
  ---
  duration_ms: 1.810171
  type: 'test'
  ...
# Subtest: tracker allows the opposite direction and a genuinely later episode
ok 218 - tracker allows the opposite direction and a genuinely later episode
  ---
  duration_ms: 1.166331
  type: 'test'
  ...
# Subtest: a long render pause does not fabricate an absence or duplicate a continuous signal
ok 219 - a long render pause does not fabricate an absence or duplicate a continuous signal
  ---
  duration_ms: 0.549553
  type: 'test'
  ...
# Subtest: a settings change creates a new formula snapshot even if the signal stays active
ok 220 - a settings change creates a new formula snapshot even if the signal stays active
  ---
  duration_ms: 0.689029
  type: 'test'
  ...
# Subtest: tracker does not create new signal facts from stale market snapshots
ok 221 - tracker does not create new signal facts from stale market snapshots
  ---
  duration_ms: 0.17594
  type: 'test'
  ...
# Subtest: browser runtime captures the contract and ships it in the atomic app shell
ok 222 - browser runtime captures the contract and ships it in the atomic app shell
  ---
  duration_ms: 16.80874
  type: 'test'
  ...
# Subtest: marketwide scanner detects a comparable strong size moving on the same side
ok 223 - marketwide scanner detects a comparable strong size moving on the same side
  ---
  duration_ms: 2.104464
  type: 'test'
  ...
# Subtest: marketwide scanner detects repeated strong best-quote support
ok 224 - marketwide scanner detects repeated strong best-quote support
  ---
  duration_ms: 1.554326
  type: 'test'
  ...
# Subtest: cascade detector requires three staircase highs and enters on the nearest breakout
ok 225 - cascade detector requires three staircase highs and enters on the nearest breakout
  ---
  duration_ms: 0.908089
  type: 'test'
  ...
# Subtest: cascade detector enters short below the newest staircase low
ok 226 - cascade detector enters short below the newest staircase low
  ---
  duration_ms: 0.373945
  type: 'test'
  ...
# Subtest: cascade detector rejects extrema zones wider than five percent
ok 227 - cascade detector rejects extrema zones wider than five percent
  ---
  duration_ms: 0.222496
  type: 'test'
  ...
# Subtest: cascade detector rejects a staircase narrower than one percent
ok 228 - cascade detector rejects a staircase narrower than one percent
  ---
  duration_ms: 0.392515
  type: 'test'
  ...
# Subtest: observability stays disabled without an explicit runtime flag
ok 229 - observability stays disabled without an explicit runtime flag
  ---
  duration_ms: 5.849641
  type: 'test'
  ...
# Subtest: observability aggregates bounded percentile samples when enabled
ok 230 - observability aggregates bounded percentile samples when enabled
  ---
  duration_ms: 2.258581
  type: 'test'
  ...
# Subtest: worker timing metadata is converted into end-to-end metrics
ok 231 - worker timing metadata is converted into end-to-end metrics
  ---
  duration_ms: 13.899941
  type: 'test'
  ...
# Subtest: worker flow diagnostics become per-symbol backpressure metrics
ok 232 - worker flow diagnostics become per-symbol backpressure metrics
  ---
  duration_ms: 2.896976
  type: 'test'
  ...
# Subtest: capture exports connection phases, render skips and five-second intervals
ok 233 - capture exports connection phases, render skips and five-second intervals
  ---
  duration_ms: 2.759218
  type: 'test'
  ...
# Subtest: recent trade history prepends in O(1) while evicting the oldest trade
ok 234 - recent trade history prepends in O(1) while evicting the oldest trade
  ---
  duration_ms: 1.244648
  type: 'test'
  ...
# Subtest: latest TAPE queue drops stale backlog and keeps chronological output
ok 235 - latest TAPE queue drops stale backlog and keeps chronological output
  ---
  duration_ms: 0.349199
  type: 'test'
  ...
# Subtest: production Worker keeps aggTrade for visual RAW and guards trade for AGG
not ok 236 - production Worker keeps aggTrade for visual RAW and guards trade for AGG
  ---
  duration_ms: 2.245505
  type: 'test'
  location: '/home/runner/work/InPuls/InPuls/test/orderbook-backpressure.test.js:32:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /return \[`\$\{name\}@aggTrade`, `\$\{name\}@trade`\];/. Input:
    
    'importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-network.js?v=obs-pr1-1");\n' +
      'importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");\n' +
      'importScripts("./orderbook-depth-projection.js?v=deep-book-v1");\n' +
      'importScripts("./orderbook-events.js?v=orderbook-events-core-v1");\n' +
      'importScripts("./orderbook-density.js?v=density-trades-correlation-v1");\n' +
      '\n' +
      'const MAX_BOOK_LEVELS_PER_SIDE = 20_000;\n' +
      'const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;\n' +
      'const MAX_BUFFERED_DEPTH_EVENTS = 4_000;\n' +
      'const MAX_TRADE_HISTORY = 12_000;\n' +
      'const MAX_RESUME_LEVELS_PER_SIDE = 700;\n' +
      'const RESUME_STAGGER_MS = 180;\n' +
      'const BACKGROUND_GRACE_MS = 2_000;\n' +
      'const RECOVERY_TIMEOUT_MS = 8_000;\n' +
      'const RESUME_STALE_MS = 3_500;\n' +
      'const DEPTH_STALE_NOTICE_MS = 3_000;\n' +
      'const ACTIVE_STALE_MS = 9_000;\n' +
      'const SNAPSHOT_TIMEOUT_MS = 2_800;\n' +
      'const IDLE_CLOSE_MS = 10_000;\n' +
      'const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;\n' +
      'const MAX_TAPE_BATCH_PER_POST = 500;\n' +
      'const MAX_PENDING_TAPE_TRADES = 800;\n' +
      'const TAPE_FLUSH_MS = 25;\n' +
      'const ACTIVE_SOURCE_LAG_MS = 4_000;\n' +
      'const TRADE_ACTIVE_STALE_MS = 30_000;\n' +
      'const RECONNECT_BASE_MS = 400;\n' +
      'const RECONNECT_MAX_MS = 8_000;\n' +
      'const WORKER_HEARTBEAT_MS = 2_000;\n' +
      'const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;\n' +
      '\n' +
      'const feeds = new Map();\n' +
      'let tabVisible = true;\n' +
      'let emitTimer = 0;\n' +
      'let emitCursor = 0;\n' +
      'let visibilityEpoch = 0;\n' +
      'let backgroundPauseTimer = 0;\n' +
      'let watchdogTimer = 0;\n' +
      'let prioritySymbols = [];\n' +
      'let serverClockOffsetMs = null;\n' +
      'let serverClockRttMs = null;\n' +
      'let serverClockSyncAt = 0;\n' +
      'let serverClockSyncPromise = null;\n' +
      'let observabilityEnabled = false;\n' +
      'let observabilityPostCount = 0;\n' +
      '\n' +
      'function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {\n' +
      '  if (!observabilityEnabled) {\n' +
      '    self.postMessage({ type, symbol, ...payload });\n' +
      '    return;\n' +
      '  }\n' +
      '  const observerStartedAt = performance.now();\n' +
      '  const message = { type, symbol, ...payload };\n' +
      '  const samplePayload = observabilityPostCount++ % 50 === 0;\n' +
      '  let payloadBytes = null;\n' +
      '  if (samplePayload) {\n' +
      '    try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}\n' +
      '  }\n' +
      '  const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);\n' +
      '  message.__obs = {\n' +
      '    sentAtEpochMs: Date.now(),\n' +
      '    processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,\n' +
      '    observerOverheadMs: performance.now() - observerStartedAt,\n' +
      '    payloadBytes,\n' +
      '    payloadSampleRate: 50,\n' +
      '    sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,\n' +
      '    sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,\n' +
      '    sourceKind: obsContext.sourceKind ?? null,\n' +
      '  };\n' +
      '  self.postMessage(message);\n' +
      '}\n' +
      '\n' +
      'function diagnose(symbol, phase, details = {}) {\n' +
      '  if (!observabilityEnabled) return;\n' +
      '  post("diagnostic", symbol, {\n' +
      '    diagnostic: {\n' +
      '      phase,\n' +
      '      atEpochMs: Date.now(),\n' +
      '      ...details,\n' +
      '    },\n' +
      '  });\n' +
      '}\n' +
      '\n' +
      'function reconnectDelay(attempt = 0) {\n' +
      '  const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));\n' +
      '  const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));\n' +
      '  return exponential + jitter;\n' +
      '}\n' +
      '\n' +
      'function bookSideQuoteScale(levels, sampleLimit = 1_024) {\n' +
      '  const totalLevels = Number.isFinite(levels?.size)\n' +
      '    ? levels.size\n' +
      '    : (Number(levels?.length) || 0);\n' +
      '  const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));\n' +
      '  const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));\n' +
      '  const sample = [];\n' +
      '  let maximum = 1;\n' +
      '  let validIndex = 0;\n' +
      '  for (const [priceValue, quantityValue] of levels ?? []) {\n' +
      '    const quote = Number(priceValue) * Number(quantityValue);\n' +
      '    if (!Number.isFinite(quote) || quote <= 0) continue;\n' +
      '    if (quote > maximum) maximum = quote;\n' +
      '    if (validIndex % sampleStride === 0) sample.push(quote);\n' +
      '    validIndex += 1;\n' +
      '  }\n' +
      '  sample.sort((left, right) => left - right);\n' +
      '  const quantile = (ratio) => sample.length\n' +
      '    ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]\n' +
      '    : 0;\n' +
      '  const median = quantile(.5);\n' +
      '  const upper = quantile(.95);\n' +
      '  const extreme = quantile(.99);\n' +
      '  return {\n' +
      '    maximum,\n' +
      '    anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),\n' +
      '    sampledLevels: sample.length,\n' +
      '    totalLevels: validIndex,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function bookQuoteScale(bids, asks, sampleLimit = 2_048) {\n' +
      '  const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));\n' +
      '  const bid = bookSideQuoteScale(bids ?? [], sideLimit);\n' +
      '  const ask = bookSideQuoteScale(asks ?? [], sideLimit);\n' +
      '  return {\n' +
      '    maximum: Math.max(bid.maximum, ask.maximum),\n' +
      '    anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),\n' +
      '    bidAnomalyThreshold: bid.anomalyThreshold,\n' +
      '    askAnomalyThreshold: ask.anomalyThreshold,\n' +
      '    sampledLevels: bid.sampledLevels + ask.sampledLevels,\n' +
      '    totalLevels: bid.totalLevels + ask.totalLevels,\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function parsePayload(raw) {\n' +
      '  let payload;\n' +
      '  try { payload = JSON.parse(raw); } catch { return null; }\n' +
      '  if (payload?.result === null || payload?.id) return null;\n' +
      '  return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };\n' +
      '}\n' +
      '\n' +
      'function normalizeTrade(event, sourceHint = null, receivedAt = null) {\n' +
      '  const eventType = String(event?.e ?? "").toLowerCase();\n' +
      '  const inferredRaw = eventType === "trade"\n' +
      '    || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));\n' +
      '  const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";\n' +
      '  const price = Number(event?.p);\n' +
      '  const quantity = Number(event?.q);\n' +
      '  const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);\n' +
      '  const time = timing.tradeTime;\n' +
      '  const id = source === "raw" ? Number(event?.t) : Number(event?.a);\n' +
      '  const firstTradeId = source === "raw" ? id : Number(event?.f);\n' +
      '  const lastTradeId = source === "raw" ? id : Number(event?.l);\n' +
      '  if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;\n' +
      '  if (price <= 0 || quantity <= 0 || time <= 0) return null;\n' +
      '  if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;\n' +
      '  return {\n' +
      '    id,\n' +
      '    firstTradeId,\n' +
      '    lastTradeId,\n' +
      '    source,\n' +
      '    price,\n' +
      '    quantity,\n' +
      '    quote: price * quantity,\n' +
      '    time,\n' +
      '    tradeTime: timing.tradeTime,\n' +
      '    eventTime: timing.eventTime,\n' +
      '    receivedAt: timing.receivedAt,\n' +
      '    rxLatencyMs: timing.rxLatencyMs,\n' +
      '    side: event?.m ? "sell" : "buy",\n' +
      '  };\n' +
      '}\n' +
      '\n' +
      'function calibratedSourceLag(eventTime, receivedAt = Date.now()) {\n' +
      '  const sourceTime = Number(eventTime);\n' +
      '  const localTime = Number(receivedAt);\n' +
      '  if (\n' +
      '    !Number.isFinite(sourceTime)\n' +
      '    || !Number.isFinite(localTime)\n' +
      '    || !Number.isFinite(serverClockOffsetMs)\n' +
      '  ) return null;\n' +
      '  const lag = localTime + serverClockOffsetMs - sourceTime;\n' +
      '  if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;\n' +
      '  return Math.max(0, lag);\n' +
      '}\n' +
      '\n' +
      'function sequenceDecision(lastUpdateId, event, firstEvent = false) {\n' +
      '  const first = Number(event?.U);\n' +
      '  const final = Number(event?.u);\n' +
      '  const previous = Number(event?.pu);\n' +
      '  const local = Number(lastUpdateId);\n' +
      '  if (![first, final, local].every(Number.isFinite)) return "resync";\n' +
      '  if (final <= local) return "ignore";\n' +
      '  if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";\n' +
      '  if (Number.isFinite(previous) && previous !== local) return "resync";\n' +
      '  if (!Number.isFinite(previous) && first > local + 1) return "resync";\n' +
      '  return "apply";\n' +
      '}\n' +
      '\n' +
      'async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {\n' +
      '  const controller = typeof AbortController === "function" ? new AbortController() : null;\n' +
      '  let timer;\n' +
      '  const abortFromExternal = () => controller?.abort();\n' +
      '  if (externalSignal?.aborted) abortFromExternal();\n' +
      '  else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });\n' +
      '  try {\n' +
      '    const timeout = new Promise((_, reject) => {\n' +
      '      timer = setTimeout(() => {\n' +
      '        controller?.abort();\n' +
      '        reject(new Error("timeout"));\n' +
      '      }, timeoutMs);\n' +
      '    });\n' +
      '    const request = fetch(url, {\n' +
      '      cache: "no-store",\n' +
      '      ...(controller ? { signal: controller.signal } : {}),\n' +
      '    }).then(async (response) => {\n' +
      '      if (!response.ok) throw new Error(`HTTP ${response.status}`);\n' +
      '      return response.json();\n' +
      '    });\n' +
      '    return await Promise.race([request, timeout]);\n' +
      '  } finally {\n' +
      '    clearTimeout(timer);\n' +
      '    externalSignal?.removeEventListener?.("abort", abortFromExternal);\n' +
      '  }\n' +
      '}\n' +
      '\n' +
      'async function syncServerClock(force = false) {\n' +
      '  const now = Date.now();\n' +
      '  if (\n' +
      '    !force\n' +
      '    && Number.isFinite(serverClockOffsetMs)\n' +
      '    && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS\n' +
      '  ) return serverClockOffsetMs;\n' +
      '  if (serverClockSyncPromise) return serverClockSyncPromise;\n' +
      '\n' +
      '  serverClockSyncPromise = (async () => {\n' +
      '    const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];\n' +
      '    diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });\n' +
      '    let result = null;\n' +
      '    try {\n' +
      '      result = await self.InPulsOrderBookNetwork.firstSuccessful(\n' +
      '        hosts,\n' +
      '        async (host, { signal }) => {\n' +
      '          const startedAt = Date.now();\n' +
      '          const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);\n' +
      '          const finishedAt = Date.now();\n' +
      '          const serverTime = Number(data?.serverTime);\n' +
      '          if (!Number.isFinite(serverTime)) throw new Error("invalid server time");\n' +
      '          return {\n' +
      '            rtt: Math.max(0, finishedAt - startedAt),\n' +
      '            offset: serverTime - (startedAt + finishedAt) / 2,\n' +
      '          };\n' +
      '        },\n' +
      '        {\n' +
      '          onAttempt: (event) => diagnose("", "clock.rest.host", {\n' +
      '            ...event,\n' +
      '            host: event.target,\n' +
      '            target: undefined,\n' +
      '          }),\n' +
      '        },\n' +
      '      );\n' +
      '    } catch {}\n' +
      '    const best = result?.value;\n' +
      '    if (best) {\n' +
      '      serverClockOffsetMs = best.offset;\n' +
      '      serverClockRttMs = best.rtt;\n' +
      '      serverClockSyncAt = Date.now();\n' +
      '      '... 50929 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    importScripts("./orderbook-tape-guard.js?v=worker-bp-v1");
    importScripts("./orderbook-tape-latency.js?v=worker-bp-v1");
    importScripts("./orderbook-network.js?v=obs-pr1-1");
    importScripts("./orderbook-worker-buffers.js?v=worker-bp-v1");
    importScripts("./orderbook-depth-projection.js?v=deep-book-v1");
    importScripts("./orderbook-events.js?v=orderbook-events-core-v1");
    importScripts("./orderbook-density.js?v=density-trades-correlation-v1");
    
    const MAX_BOOK_LEVELS_PER_SIDE = 20_000;
    const MAX_EMITTED_LEVELS_PER_SIDE = 4_000;
    const MAX_BUFFERED_DEPTH_EVENTS = 4_000;
    const MAX_TRADE_HISTORY = 12_000;
    const MAX_RESUME_LEVELS_PER_SIDE = 700;
    const RESUME_STAGGER_MS = 180;
    const BACKGROUND_GRACE_MS = 2_000;
    const RECOVERY_TIMEOUT_MS = 8_000;
    const RESUME_STALE_MS = 3_500;
    const DEPTH_STALE_NOTICE_MS = 3_000;
    const ACTIVE_STALE_MS = 9_000;
    const SNAPSHOT_TIMEOUT_MS = 2_800;
    const IDLE_CLOSE_MS = 10_000;
    const TRADE_FIRST_MESSAGE_TIMEOUT_MS = 8_000;
    const MAX_TAPE_BATCH_PER_POST = 500;
    const MAX_PENDING_TAPE_TRADES = 800;
    const TAPE_FLUSH_MS = 25;
    const ACTIVE_SOURCE_LAG_MS = 4_000;
    const TRADE_ACTIVE_STALE_MS = 30_000;
    const RECONNECT_BASE_MS = 400;
    const RECONNECT_MAX_MS = 8_000;
    const WORKER_HEARTBEAT_MS = 2_000;
    const CLOCK_SYNC_INTERVAL_MS = 5 * 60_000;
    
    const feeds = new Map();
    let tabVisible = true;
    let emitTimer = 0;
    let emitCursor = 0;
    let visibilityEpoch = 0;
    let backgroundPauseTimer = 0;
    let watchdogTimer = 0;
    let prioritySymbols = [];
    let serverClockOffsetMs = null;
    let serverClockRttMs = null;
    let serverClockSyncAt = 0;
    let serverClockSyncPromise = null;
    let observabilityEnabled = false;
    let observabilityPostCount = 0;
    
    function post(type, symbol, payload = {}, processStartedAt = null, obsContext = {}) {
      if (!observabilityEnabled) {
        self.postMessage({ type, symbol, ...payload });
        return;
      }
      const observerStartedAt = performance.now();
      const message = { type, symbol, ...payload };
      const samplePayload = observabilityPostCount++ % 50 === 0;
      let payloadBytes = null;
      if (samplePayload) {
        try { payloadBytes = new Blob([JSON.stringify(message)]).size; } catch {}
      }
      const sourceEventTimeMs = Number(obsContext.sourceEventTimeMs);
      message.__obs = {
        sentAtEpochMs: Date.now(),
        processMs: Number.isFinite(processStartedAt) ? performance.now() - processStartedAt : null,
        observerOverheadMs: performance.now() - observerStartedAt,
        payloadBytes,
        payloadSampleRate: 50,
        sourceClockOffsetMs: Number.isFinite(serverClockOffsetMs) ? serverClockOffsetMs : null,
        sourceEventTimeMs: Number.isFinite(sourceEventTimeMs) ? sourceEventTimeMs : null,
        sourceKind: obsContext.sourceKind ?? null,
      };
      self.postMessage(message);
    }
    
    function diagnose(symbol, phase, details = {}) {
      if (!observabilityEnabled) return;
      post("diagnostic", symbol, {
        diagnostic: {
          phase,
          atEpochMs: Date.now(),
          ...details,
        },
      });
    }
    
    function reconnectDelay(attempt = 0) {
      const exponential = Math.min(RECONNECT_MAX_MS, RECONNECT_BASE_MS * (2 ** Math.max(0, attempt)));
      const jitter = Math.floor(Math.random() * Math.max(80, exponential * .25));
      return exponential + jitter;
    }
    
    function bookSideQuoteScale(levels, sampleLimit = 1_024) {
      const totalLevels = Number.isFinite(levels?.size)
        ? levels.size
        : (Number(levels?.length) || 0);
      const limit = Math.max(32, Math.floor(Number(sampleLimit) || 1_024));
      const sampleStride = Math.max(1, Math.ceil(totalLevels / limit));
      const sample = [];
      let maximum = 1;
      let validIndex = 0;
      for (const [priceValue, quantityValue] of levels ?? []) {
        const quote = Number(priceValue) * Number(quantityValue);
        if (!Number.isFinite(quote) || quote <= 0) continue;
        if (quote > maximum) maximum = quote;
        if (validIndex % sampleStride === 0) sample.push(quote);
        validIndex += 1;
      }
      sample.sort((left, right) => left - right);
      const quantile = (ratio) => sample.length
        ? sample[Math.min(sample.length - 1, Math.floor((sample.length - 1) * ratio))]
        : 0;
      const median = quantile(.5);
      const upper = quantile(.95);
      const extreme = quantile(.99);
      return {
        maximum,
        anomalyThreshold: Math.max(1, median * 6, upper * 1.35, extreme),
        sampledLevels: sample.length,
        totalLevels: validIndex,
      };
    }
    
    function bookQuoteScale(bids, asks, sampleLimit = 2_048) {
      const sideLimit = Math.max(32, Math.floor((Number(sampleLimit) || 2_048) / 2));
      const bid = bookSideQuoteScale(bids ?? [], sideLimit);
      const ask = bookSideQuoteScale(asks ?? [], sideLimit);
      return {
        maximum: Math.max(bid.maximum, ask.maximum),
        anomalyThreshold: Math.max(bid.anomalyThreshold, ask.anomalyThreshold),
        bidAnomalyThreshold: bid.anomalyThreshold,
        askAnomalyThreshold: ask.anomalyThreshold,
        sampledLevels: bid.sampledLevels + ask.sampledLevels,
        totalLevels: bid.totalLevels + ask.totalLevels,
      };
    }
    
    function parsePayload(raw) {
      let payload;
      try { payload = JSON.parse(raw); } catch { return null; }
      if (payload?.result === null || payload?.id) return null;
      return { stream: String(payload?.stream ?? ""), data: payload?.data ?? payload };
    }
    
    function normalizeTrade(event, sourceHint = null, receivedAt = null) {
      const eventType = String(event?.e ?? "").toLowerCase();
      const inferredRaw = eventType === "trade"
        || (Number.isFinite(Number(event?.t)) && !Number.isFinite(Number(event?.a)));
      const source = sourceHint === "raw" || (sourceHint !== "agg" && inferredRaw) ? "raw" : "agg";
      const price = Number(event?.p);
      const quantity = Number(event?.q);
      const timing = self.InPulsTapeLatency.normalizeTiming(event, receivedAt, serverClockOffsetMs);
      const time = timing.tradeTime;
      const id = source === "raw" ? Number(event?.t) : Number(event?.a);
      const firstTradeId = source === "raw" ? id : Number(event?.f);
      const lastTradeId = source === "raw" ? id : Number(event?.l);
      if (![price, quantity, time, id, firstTradeId, lastTradeId].every(Number.isFinite)) return null;
      if (price <= 0 || quantity <= 0 || time <= 0) return null;
      if (![id, firstTradeId, lastTradeId].every(Number.isInteger) || id < 0 || firstTradeId < 0 || lastTradeId < firstTradeId) return null;
      return {
        id,
        firstTradeId,
        lastTradeId,
        source,
        price,
        quantity,
        quote: price * quantity,
        time,
        tradeTime: timing.tradeTime,
        eventTime: timing.eventTime,
        receivedAt: timing.receivedAt,
        rxLatencyMs: timing.rxLatencyMs,
        side: event?.m ? "sell" : "buy",
      };
    }
    
    function calibratedSourceLag(eventTime, receivedAt = Date.now()) {
      const sourceTime = Number(eventTime);
      const localTime = Number(receivedAt);
      if (
        !Number.isFinite(sourceTime)
        || !Number.isFinite(localTime)
        || !Number.isFinite(serverClockOffsetMs)
      ) return null;
      const lag = localTime + serverClockOffsetMs - sourceTime;
      if (!Number.isFinite(lag) || lag < -100 || lag > 60_000) return null;
      return Math.max(0, lag);
    }
    
    function sequenceDecision(lastUpdateId, event, firstEvent = false) {
      const first = Number(event?.U);
      const final = Number(event?.u);
      const previous = Number(event?.pu);
      const local = Number(lastUpdateId);
      if (![first, final, local].every(Number.isFinite)) return "resync";
      if (final <= local) return "ignore";
      if (firstEvent) return first <= local + 1 && final >= local + 1 ? "apply" : "resync";
      if (Number.isFinite(previous) && previous !== local) return "resync";
      if (!Number.isFinite(previous) && first > local + 1) return "resync";
      return "apply";
    }
    
    async function fetchJson(url, timeoutMs = SNAPSHOT_TIMEOUT_MS, externalSignal = null) {
      const controller = typeof AbortController === "function" ? new AbortController() : null;
      let timer;
      const abortFromExternal = () => controller?.abort();
      if (externalSignal?.aborted) abortFromExternal();
      else externalSignal?.addEventListener?.("abort", abortFromExternal, { once: true });
      try {
        const timeout = new Promise((_, reject) => {
          timer = setTimeout(() => {
            controller?.abort();
            reject(new Error("timeout"));
          }, timeoutMs);
        });
        const request = fetch(url, {
          cache: "no-store",
          ...(controller ? { signal: controller.signal } : {}),
        }).then(async (response) => {
          if (!response.ok) throw new Error(`HTTP ${response.status}`);
          return response.json();
        });
        return await Promise.race([request, timeout]);
      } finally {
        clearTimeout(timer);
        externalSignal?.removeEventListener?.("abort", abortFromExternal);
      }
    }
    
    async function syncServerClock(force = false) {
      const now = Date.now();
      if (
        !force
        && Number.isFinite(serverClockOffsetMs)
        && now - serverClockSyncAt < CLOCK_SYNC_INTERVAL_MS
      ) return serverClockOffsetMs;
      if (serverClockSyncPromise) return serverClockSyncPromise;
    
      serverClockSyncPromise = (async () => {
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        diagnose("", "clock.rest", { state: "scheduled", hosts: hosts.length });
        let result = null;
        try {
          result = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const startedAt = Date.now();
              const data = await fetchJson(`https://${host}/fapi/v1/time`, 1_800, signal);
              const finishedAt = Date.now();
              const serverTime = Number(data?.serverTime);
              if (!Number.isFinite(serverTime)) throw new Error("invalid server time");
              return {
                rtt: Math.max(0, finishedAt - startedAt),
                offset: serverTime - (startedAt + finishedAt) / 2,
              };
            },
            {
              onAttempt: (event) => diagnose("", "clock.rest.host", {
                ...event,
                host: event.target,
                target: undefined,
              }),
            },
          );
        } catch {}
        const best = result?.value;
        if (best) {
          serverClockOffsetMs = best.offset;
          serverClockRttMs = best.rtt;
          serverClockSyncAt = Date.now();
          diagnose("", "clock.rest", {
            state: "succeeded",
            host: result.target,
            durationMs: result.durationMs,
            rttMs: best.rtt,
          });
        } else {
          diagnose("", "clock.rest", { state: "failed" });
        }
        return serverClockOffsetMs;
      })().finally(() => {
        serverClockSyncPromise = null;
      });
      return serverClockSyncPromise;
    }
    
    function depthTransports(symbol, mode) {
      const stream = `${symbol.toLowerCase()}@${mode === "partial" ? "depth20" : "depth"}@100ms`;
      return [
        { name: "public · combined", url: `wss://fstream.binance.com/public/stream?streams=${stream}`, subscribe: false, stream },
        { name: "public · raw", url: `wss://fstream.binance.com/public/ws/${stream}`, subscribe: false, stream },
      ];
    }
    
    function tradeStreams(symbol) {
      const name = symbol.toLowerCase();
      // Production Tape uses the documented USD-M Futures @aggTrade stream.
      // Undocumented @trade remains excluded until a separate shadow study proves
      // continuity, duplication and latency behaviour over a long observation.
      return [`${name}@aggTrade`];
    }
    
    function tradeTransports(streams) {
      const stream = String(streams?.[0] ?? "");
      return [
        { name: "market · combined", url: `wss://fstream.binance.com/market/stream?streams=${stream}`, subscribe: false, streams: [stream] },
        { name: "market · raw", url: `wss://fstream.binance.com/market/ws/${stream}`, subscribe: false, streams: [stream] },
      ];
    }
    
    function trimSide(levels, side, limit) {
      if (levels.size <= limit) return;
      const keys = [...levels.keys()].sort(side === "bid" ? (a, b) => b - a : (a, b) => a - b);
      for (const price of keys.slice(limit)) levels.delete(price);
    }
    
    class SymbolFeed {
      constructor(symbol) {
        this.symbol = symbol;
        this.subscribers = 0;
        this.closeTimer = 0;
        this.socket = null;
        this.tradeSocket = null;
        this.reconnectTimer = 0;
        this.tradeReconnectTimer = 0;
        this.tradeFirstMessageTimer = 0;
        this.firstDepthTimer = 0;
        this.snapshotTimer = 0;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.generation = 0;
        this.bids = new Map();
        this.asks = new Map();
        this.partialBidKeys = new Set();
        this.partialAskKeys = new Set();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.resyncCount = 0;
        this.dirty = false;
        this.forceEmit = false;
        this.lastEmitAt = 0;
        this.cachedSorted = null;
        this.bookEvents = new self.InPulsOrderBookEvents.DepthEventJournal({ symbol });
        this.densityLifecycle = new self.InPulsOrderBookDensity.DensityLifecycleTracker({ symbol });
        this.statusKey = "";
        this.trades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.tradeIds = new Set();
        this.tapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.aggregationTrades = new self.InPulsOrderBookBuffers.RecentRingBuffer(MAX_TRADE_HISTORY);
        this.aggregationTradeIds = new Set();
        this.aggregationTapeBatch = new self.InPulsOrderBookBuffers.LatestBatchQueue(MAX_PENDING_TAPE_TRADES);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.backgroundPaused = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.lastMessageAt = 0;
        this.lastRestartAt = 0;
        this.syncing = false;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.lastResyncAt = 0;
        this.tradeTransportName = "—";
        this.flowWindowStartedAt = Date.now();
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
        this.latestFlowHealth = null;
        this.tapeGuard = new self.InPulsTapeGuard({ rawWarmupTrades: 6, rawStaleMs: 1_500 });
        this.tradeLatency = new self.InPulsTapeLatency.RollingLatency({ windowMs: 2_000, maxSamples: 400, updateMs: 250 });
      }
    
      tradeBoundary() {
        let boundary = null;
        for (const trade of this.trades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeSnapshot(limit = MAX_TRADE_HISTORY) {
        return this.trades.toArray(limit);
      }
    
      aggregationBoundary() {
        let boundary = null;
        for (const trade of this.aggregationTrades) {
          const value = Number(trade?.lastTradeId);
          if (Number.isInteger(value) && value >= 0) boundary = boundary === null ? value : Math.max(boundary, value);
        }
        return boundary;
      }
    
      tradeKey(trade) {
        const firstTradeId = Number.isInteger(Number(trade?.firstTradeId))
          ? Number(trade.firstTradeId)
          : trade?.id;
        const lastTradeId = Number.isInteger(Number(trade?.lastTradeId))
          ? Number(trade.lastTradeId)
          : trade?.id;
        return `${firstTradeId}:${lastTradeId}:${trade?.time}:${trade?.price}:${trade?.quantity}`;
      }
    
      resetTapeGuard() {
        this.tapeGuard.reset({ lastOutputTradeId: this.aggregationBoundary() });
      }
    
      addSubscriber() {
        const wasZero = this.subscribers === 0;
        const wasCoolingDown = Boolean(this.closeTimer);
        this.subscribers += 1;
        clearTimeout(this.closeTimer);
        this.closeTimer = 0;
        if (wasZero && !wasCoolingDown) this.start();
        else this.refresh();
      }
    
      removeSubscriber() {
        this.subscribers = Math.max(0, this.subscribers - 1);
        if (this.subscribers > 0 || this.closeTimer) return;
        this.closeTimer = setTimeout(() => {
          this.closeTimer = 0;
          if (this.subscribers === 0) {
            this.stop();
            feeds.delete(this.symbol);
          }
        }, IDLE_CLOSE_MS);
      }
    
      start() {
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tradeLatency.reset();
        this.resetFlowWindow();
        this.syncing = false;
        this.backgroundPaused = false;
        this.resetTapeGuard();
        this.resetBook("start");
        this.setStatus("loading", "Подключение Worker");
        const generation = this.generation;
        diagnose(this.symbol, "feed.start", {
          state: "started",
          generation,
          subscribers: this.subscribers,
        });
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      stopSockets() {
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        try { this.socket?.close(); } catch {}
        try { this.tradeSocket?.close(); } catch {}
        this.socket = null;
        this.tradeSocket = null;
        this.tapeGuard.disconnect("socket-stop");
      }
    
      stop() {
        diagnose(this.symbol, "feed.stop", {
          state: "started",
          generation: this.generation,
        });
        this.generation += 1;
        this.stopSockets();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
      }
    
      resetBook(reason = "reset") {
        this.bids.clear();
        this.asks.clear();
        this.partialBidKeys.clear();
        this.partialAskKeys.clear();
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.lastUpdateId = null;
        this.depthReady = false;
        this.snapshotLoading = false;
        this.cachedSorted = null;
        this.dirty = false;
        this.lastDepthAt = 0;
        this.lastDepthEventTime = 0;
        this.lastDepthSourceLagMs = null;
        const bookEpoch = this.bookEvents.reset(reason);
        this.densityLifecycle.reset({ bookEpoch, reason });
      }
    
      setStatus(state, text) {
        const key = `${state}:${text}`;
        if (key === this.statusKey) return;
        this.statusKey = key;
        post("status", this.symbol, { state, text });
      }
    
      latencyText() {
        const latency = this.tradeLatency.current();
        if (!Number.isFinite(latency)) return "";
        const value = latency < 1
          ? "<1"
          : latency < 10
            ? latency.toFixed(1)
            : String(Math.round(latency));
        return ` · RX ${value}ms`;
      }
    
      liveStatusText(tapeState = null) {
        const partial = this.mode === "partial" ? " · 20" : "";
        const reconnectingTape = tapeState === "reconnect" || (this.tradeLive && !this.tradeConnected);
        const tape = reconnectingTape
          ? " · TAPE RECONNECT"
          : (this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : "");
        return `LIVE 100ms · WORKER${partial}${tape}${this.latencyText()}`;
      }
    
      publishLiveStatus(tapeState = null) {
        if (this.syncing) {
          this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          return;
        }
        const depthAge = this.lastDepthAt ? Date.now() - this.lastDepthAt : Infinity;
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
          return;
        }
        this.setStatus("online", this.liveStatusText(tapeState));
      }
    
      markDirty(force = false) {
        this.dirty = true;
        if (force) this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      refresh() {
        this.forceEmit = true;
        if (tabVisible) scheduleEmit();
      }
    
      pauseForBackground() {
        if (this.subscribers <= 0 || this.backgroundPaused) return;
        this.backgroundPaused = true;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.generation += 1;
        this.stopSockets();
        this.tradeConnected = false;
        this.tradeLive = false;
        this.tradeLatency.reset();
        this.tapeBatch.clear();
        this.aggregationTapeBatch.clear();
        clearTimeout(this.tapeTimer);
        clearTimeout(this.resumeTimer);
        this.tapeTimer = 0;
        this.resumeTimer = 0;
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Пауза Worker",
        );
      }
    
      resume(delayMs = 0, epoch = visibilityEpoch) {
        clearTimeout(this.resumeTimer);
        this.resumeTimer = setTimeout(() => {
          this.resumeTimer = 0;
          if (!tabVisible || epoch !== visibilityEpoch || this.subscribers <= 0) return;
    
          const now = Date.now();
          const socketOpen = this.socket?.readyState === WebSocket.OPEN;
          const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
          const depthFresh = this.lastDepthAt > 0 && now - this.lastDepthAt <= RESUME_STALE_MS;
    
          if (!this.backgroundPaused && socketOpen && depthFresh) {
            post("tape", this.symbol, {
              replace: true,
              liveOnly: true,
              trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            }, null, { sourceKind: "live-reset" });
            if (!tradeOpen && !this.tradeReconnectTimer) {
              this.connectTrades(this.generation);
            }
            this.forceEmit = true;
            this.emit(now, MAX_RESUME_LEVELS_PER_SIDE, true);
            this.publishLiveStatus(tradeOpen ? null : "reconnect");
            return;
          }
    
          this.backgroundPaused = false;
          this.restartAfterBackground(true);
        }, Math.max(0, delayMs));
      }
    
      restartAfterBackground(force = false) {
        if (this.subscribers <= 0) return;
        const now = Date.now();
        if (!force && now - this.lastRestartAt < 2_500) return;
        this.lastRestartAt = now;
        const preserveLastFrame = this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.stopSockets();
        this.generation += 1;
        this.mode = "deep";
        this.transportIndex = 0;
        this.tradeTransportIndex = 0;
        this.depthReconnectAttempt = 0;
        this.tradeReconnectAttempt = 0;
        this.tradeLive = false;
        this.tradeConnected = false;
        this.tradeTransportName = "—";
        this.lastTradeAt = 0;
        this.lastTradeEventTime = 0;
        this.lastTradeSourceLagMs = null;
        this.tapeGuard.disconnect("background-restart");
        this.resetFlowWindow();
        this.resetBook("background-restart");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Восстановление Worker",
        );
        const generation = this.generation;
        post("tape", this.symbol, {
          replace: true,
          liveOnly: true,
          trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
        }, null, { sourceKind: "live-reset" });
        this.connectDepth(generation);
        this.connectTrades(generation);
      }
    
      resetFlowWindow(now = Date.now()) {
        this.flowWindowStartedAt = now;
        this.depthEventsInWindow = 0;
        this.tradeEventsInWindow = 0;
        this.depthProcessMsInWindow = 0;
        this.tradeProcessMsInWindow = 0;
        this.depthProcessMaxMsInWindow = 0;
        this.tradeProcessMaxMsInWindow = 0;
        this.tapeDroppedInWindow = 0;
      }
    
      recordFlow(kind, startedAt) {
        const durationMs = Math.max(0, performance.now() - startedAt);
        if (kind === "depth") {
          this.depthEventsInWindow += 1;
          this.depthProcessMsInWindow += durationMs;
          this.depthProcessMaxMsInWindow = Math.max(this.depthProcessMaxMsInWindow, durationMs);
          return;
        }
        this.tradeEventsInWindow += 1;
        this.tradeProcessMsInWindow += durationMs;
        this.tradeProcessMaxMsInWindow = Math.max(this.tradeProcessMaxMsInWindow, durationMs);
      }
    
      reportFlow(now = Date.now()) {
        const durationMs = Math.max(1, now - this.flowWindowStartedAt);
        const perSecond = 1_000 / durationMs;
        const snapshot = {
          durationMs,
          depthEventsPerSecond: this.depthEventsInWindow * perSecond,
          tradeEventsPerSecond: this.tradeEventsInWindow * perSecond,
          depthProcessMeanMs: this.depthEventsInWindow
            ? this.depthProcessMsInWindow / this.depthEventsInWindow
            : 0,
          tradeProcessMeanMs: this.tradeEventsInWindow
            ? this.tradeProcessMsInWindow / this.tradeEventsInWindow
            : 0,
          depthProcessMaxMs: this.depthProcessMaxMsInWindow,
          tradeProcessMaxMs: this.tradeProcessMaxMsInWindow,
          tapeQueue: this.tapeBatch.length,
          tapeDropped: this.tapeDroppedInWindow,
          depthSourceLagMs: this.lastDepthSourceLagMs,
          tradeSourceLagMs: this.lastTradeSourceLagMs,
        };
        this.latestFlowHealth = snapshot;
        diagnose(this.symbol, "worker.flow", {
          state: "sampled",
          generation: this.generation,
          ...snapshot,
        });
        this.resetFlowWindow(now);
        return snapshot;
      }
    
      ensureHealthy(now = Date.now()) {
        if (!tabVisible || this.subscribers <= 0) return;
        const recoveryDelayed = this.syncing
          && this.lastRestartAt > 0
          && now - this.lastRestartAt > RECOVERY_TIMEOUT_MS;
        if (recoveryDelayed) {
          this.setStatus("stale", "RECOVERY DELAY · повторное подключение");
          this.restartAfterBackground(true);
          return;
        }
        const socketOpen = this.socket?.readyState === WebSocket.OPEN;
        const socketConnecting = this.socket?.readyState === WebSocket.CONNECTING;
        const reconnectPending = Boolean(this.reconnectTimer || this.firstDepthTimer || this.snapshotLoading);
        const depthAge = this.lastDepthAt > 0 ? now - this.lastDepthAt : Infinity;
        const depthSourceStale = this.depthReady
          && Number.isFinite(this.lastDepthSourceLagMs)
          && this.lastDepthSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const stale = this.depthReady && (depthAge > ACTIVE_STALE_MS || depthSourceStale);
    
        if ((!socketOpen && !socketConnecting && !reconnectPending) || stale) {
          if (stale) {
            diagnose(this.symbol, "depth.freshness", {
              state: "stale",
              generation: this.generation,
              depthAgeMs: depthAge,
              sourceLagMs: this.lastDepthSourceLagMs,
              socketOpen,
            });
          }
          this.setStatus("offline", "RECONNECT · WORKER");
          this.restartAfterBackground();
          return;
        }
    
        if (this.depthReady && depthAge > DEPTH_STALE_NOTICE_MS) {
          this.setStatus("stale", `STALE ${Math.max(1, Math.floor(depthAge / 1_000))}с · WORKER${this.tradeLive && this.tradeConnected ? ` · ${this.tapeGuard.label()}` : ""}`);
        } else if (this.depthReady && this.statusKey.startsWith("stale:")) {
          this.publishLiveStatus();
        }
    
        const tradeOpen = this.tradeSocket?.readyState === WebSocket.OPEN;
        const tradeConnecting = this.tradeSocket?.readyState === WebSocket.CONNECTING;
        const tradeAge = this.lastTradeAt > 0 ? now - this.lastTradeAt : Infinity;
        const tradeSourceStale = Number.isFinite(this.lastTradeSourceLagMs)
          && this.lastTradeSourceLagMs > ACTIVE_SOURCE_LAG_MS;
        const tradeStale = this.tradeConnected
          && (tradeAge > TRADE_ACTIVE_STALE_MS || tradeSourceStale);
        if (tradeOpen && tradeStale) {
          diagnose(this.symbol, "tape.freshness", {
            state: "stale",
            generation: this.generation,
            tradeAgeMs: tradeAge,
            sourceLagMs: this.lastTradeSourceLagMs,
            socketOpen: true,
          });
          this.publishLiveStatus("reconnect");
          try { this.tradeSocket?.close(); } catch {}
          return;
        }
        if (!tradeOpen && !tradeConnecting && !this.tradeReconnectTimer) {
          if (this.tradeLive) this.publishLiveStatus("reconnect");
          this.connectTrades(this.generation);
        }
      }
    
      priorityRank() {
        const index = prioritySymbols.indexOf(this.symbol);
        return index < 0 ? Number.MAX_SAFE_INTEGER : index;
      }
    
      emittedLimit() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1) return MAX_EMITTED_LEVELS_PER_SIDE;
        if (this.priorityRank() === 0) return active <= 2 ? 1_800 : 900;
        if (active === 2) return 900;
        if (active <= 4) return 450;
        return 280;
      }
    
      emitIntervalMs() {
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0).length;
        if (active <= 1 || this.priorityRank() === 0) return 100;
        if (active <= 4) return 220;
        return 320;
      }
    
      bookStorageLimit() {
        // UI emission is throttled separately. Never destroy the deep local
        // book merely because several panels are open.
        return MAX_BOOK_LEVELS_PER_SIDE;
      }
    
      sortedDepth() {
        if (this.cachedSorted) return this.cachedSorted;
        const rawBids = [...this.bids.entries()];
        const rawAsks = [...this.asks.entries()];
        const bids = rawBids.sort((a, b) => b[0] - a[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const asks = rawAsks.sort((a, b) => a[0] - b[0]).slice(0, MAX_EMITTED_LEVELS_PER_SIDE);
        const quoteScale = bookQuoteScale(rawBids, rawAsks);
        this.cachedSorted = {
          bids,
          asks,
          sizeScaleMaxQuote: quoteScale.maximum,
          sizeAnomalyThresholdQuote: quoteScale.anomalyThreshold,
          sizeAnomalyThresholdBidQuote: quoteScale.bidAnomalyThreshold,
          sizeAnomalyThresholdAskQuote: quoteScale.askAnomalyThreshold,
        };
        return this.cachedSorted;
      }
    
      emit(now = Date.now(), requestedLimit = this.emittedLimit(), force = false) {
        const processStartedAt = observabilityEnabled ? performance.now() : null;
        if (!tabVisible || this.subscribers <= 0 || (!force && !this.dirty && !this.forceEmit)) return;
        if (!force && !this.forceEmit && now - this.lastEmitAt < this.emitIntervalMs()) return;
        const fullView = this.sortedDepth();
        const limit = Math.max(100, Math.min(MAX_EMITTED_LEVELS_PER_SIDE, Math.floor(requestedLimit)));
        const view = self.InPulsOrderBookDepthProjection.compactDepthView(fullView, {
          exactLimit: limit,
          densityLimit: 96,
          bandCount: 128,
        });
        if (!view.bids.length || !view.asks.length) return;
        const bestBid = Number(fullView.bids[0][0]);
        const bestAsk = Number(fullView.asks[0][0]);
        const middle = (bestBid + bestAsk) / 2;
        const lowestBid = Number(view.bids.at(-1)?.[0]);
        const highestAsk = Number(view.asks.at(-1)?.[0]);
        this.densityLifecycle.refresh({
          bids: fullView.bids,
          asks: fullView.asks,
          now,
        });
        post("data", this.symbol, {
          data: {
            symbol: this.symbol,
            bids: view.bids,
            asks: view.asks,
            trades: [],
          aggregationTrades: [],
          aggregationSource: "agg",
            bestBid,
            bestAsk,
            lastUpdateId: this.lastUpdateId,
            eventTime: now,
            depthReady: this.depthReady,
            coverage: {
              bidPercent: Number.isFinite(lowestBid) ? Math.max(0, ((middle - lowestBid) / middle) * 100) : 0,
              askPercent: Number.isFinite(highestAsk) ? Math.max(0, ((highestAsk - middle) / middle) * 100) : 0,
            },
            bookLevels: { bids: this.bids.size, asks: this.asks.size },
            sizeScaleMaxQuote: fullView.sizeScaleMaxQuote,
            sizeAnomalyThresholdQuote: fullView.sizeAnomalyThresholdQuote,
            sizeAnomalyThresholdBidQuote: fullView.sizeAnomalyThresholdBidQuote,
            sizeAnomalyThresholdAskQuote: fullView.sizeAnomalyThresholdAskQuote,
            resyncCount: this.resyncCount,
            orderBookEvents: this.bookEvents.summary(),
            densityLifecycle: this.densityLifecycle.summary(now),
            depthProjection: view.metadata,
            health: {
              mode: this.mode,
              depthAgeMs: this.lastDepthAt ? Math.max(0, now - this.lastDepthAt) : null,
              tradeAgeMs: this.lastTradeAt ? Math.max(0, now - this.lastTradeAt) : null,
              depthSourceLagMs: this.lastDepthSourceLagMs,
              tradeSourceLagMs: this.lastTradeSourceLagMs,
              depthBuffer: this.depthBuffer.length,
              tapeQueue: this.tapeBatch.length,
              subscribers: this.subscribers,
              depthTransport: this.transportIndex,
              tradeTransport: this.tradeTransportIndex,
              tradeTransportName: this.tradeTransportName,
              syncing: this.syncing,
              flow: this.latestFlowHealth,
              tape: this.tapeGuard.snapshot(now),
            },
            worker: true,
          },
        }, processStartedAt, {
          sourceEventTimeMs: this.lastDepthEventTime,
          sourceKind: this.lastDepthEventTime ? "live-depth" : "snapshot-depth",
        });
        this.lastEmitAt = now;
        this.dirty = false;
        this.forceEmit = false;
      }
    
      trimBook() {
        const limit = this.bookStorageLimit();
        trimSide(this.bids, "bid", limit);
        trimSide(this.asks, "ask", limit);
      }
    
      applyDepth(event, first = false) {
        const decision = sequenceDecision(this.lastUpdateId, event, first);
        if (decision === "ignore") return true;
        if (decision === "resync") {
          this.resync("Разрыв последовательности");
          return false;
        }
        const bookEvents = this.bookEvents.applyDiff({
          bids: this.bids,
          asks: this.asks,
          event,
          continuity: this.depthReady ? "live" : "recovered",
          receivedAt: Number(event?.__receivedAt) || Date.now(),
        });
        this.densityLifecycle.ingest(bookEvents);
        this.lastUpdateId = Number(event.u);
        this.cachedSorted = null;
        this.trimBook();
        this.markDirty();
        return true;
      }
    
      bufferDepth(event) {
        this.depthBuffer.push(event);
        if (this.depthBuffer.length > MAX_BUFFERED_DEPTH_EVENTS) {
          this.depthBuffer.splice(0, this.depthBuffer.length - MAX_BUFFERED_DEPTH_EVENTS);
          this.resync("Переполнение буфера");
        }
      }
    
      installSnapshot() {
        const snapshot = this.pendingSnapshot;
        if (!snapshot) return false;
        const snapshotId = Number(snapshot.lastUpdateId);
        const applicable = this.depthBuffer.filter((event) => Number(event?.u) > snapshotId);
        const bridgeIndex = applicable.findIndex(
          (event) => Number(event?.U) <= snapshotId + 1 && Number(event?.u) >= snapshotId + 1,
        );
        if (bridgeIndex < 0) {
          const firstU = Number(applicable[0]?.U);
          if (Number.isFinite(firstU) && firstU > snapshotId + 1) {
            diagnose(this.symbol, "depth.snapshot.bridge", {
              state: "gap",
              generation: this.generation,
              snapshotId,
              firstBufferedUpdateId: firstU,
            });
            this.pendingSnapshot = null;
            clearTimeout(this.snapshotTimer);
            this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), 250);
          }
          return false;
        }
        this.bids = new Map();
        this.asks = new Map();
        const snapshotReceivedAt = Date.now();
        const baseline = this.bookEvents.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          snapshot,
          receivedAt: snapshotReceivedAt,
        });
        this.densityLifecycle.seedSnapshot({
          bids: this.bids,
          asks: this.asks,
          bookEpoch: this.bookEvents.bookEpoch,
          receivedAt: snapshotReceivedAt,
        });
        this.lastUpdateId = baseline.snapshotId;
        this.cachedSorted = null;
        for (let index = bridgeIndex; index < applicable.length; index += 1) {
          if (!this.applyDepth(applicable[index], index === bridgeIndex)) return false;
        }
        this.depthBuffer = [];
        this.pendingSnapshot = null;
        this.depthReady = true;
        this.syncing = false;
        this.bookEvents.markReady();
        this.densityLifecycle.markReady();
        diagnose(this.symbol, "depth.live", {
          state: "ready",
          generation: this.generation,
          mode: this.mode,
          snapshotId,
          bufferedEvents: applicable.length - bridgeIndex,
          bids: this.bids.size,
          asks: this.asks.size,
          orderBookEvents: this.bookEvents.summary(),
        });
        this.publishLiveStatus();
        this.markDirty(true);
        return true;
      }
    
      async loadSnapshot(generation) {
        if (generation !== this.generation || this.mode !== "deep" || this.snapshotLoading) return;
        this.snapshotLoading = true;
        const hosts = ["fapi.binance.com", "fapi1.binance.com", "fapi2.binance.com"];
        let snapshot = null;
        let winner = null;
        diagnose(this.symbol, "depth.snapshot", {
          state: "scheduled",
          generation,
          hosts: hosts.length,
        });
        try {
          winner = await self.InPulsOrderBookNetwork.firstSuccessful(
            hosts,
            async (host, { signal }) => {
              const data = await fetchJson(
                `https://${host}/fapi/v1/depth?symbol=${encodeURIComponent(this.symbol)}&limit=1000`,
                SNAPSHOT_TIMEOUT_MS,
                signal,
              );
              if (!Array.isArray(data?.bids) || !Array.isArray(data?.asks) || !Number.isFinite(Number(data?.lastUpdateId))) {
                throw new Error("invalid snapshot");
              }
              return data;
            },
            {
              onAttempt: (event) => diagnose(this.symbol, "depth.snapshot.host", {
                ...event,
                host: event.target,
                target: undefined,
                generation,
              }),
            },
          );
          snapshot = winner.value;
        } catch {}
        this.snapshotLoading = false;
        if (generation !== this.generation || this.mode !== "deep") return;
        if (!snapshot) {
          diagnose(this.symbol, "depth.snapshot", {
            state: "failed",
            generation,
          });
          this.activatePartial(generation);
          return;
        }
        diagnose(this.symbol, "depth.snapshot", {
          state: "succeeded",
          generation,
          host: winner.target,
          durationMs: winner.durationMs,
          snapshotId: Number(snapshot.lastUpdateId),
        });
        this.pendingSnapshot = snapshot;
        this.installSnapshot();
      }
    
      activatePartial(generation) {
        if (generation !== this.generation || this.mode === "partial") return;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.mode = "partial";
        this.transportIndex = 0;
        this.syncing = preserveLastFrame;
        this.resetBook("partial-fallback");
        this.bookEvents.markUnavailable("partial-depth");
        this.densityLifecycle.markUnavailable("partial-depth");
        clearTimeout(this.firstDepthTimer);
        clearTimeout(this.snapshotTimer);
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "Резервный Worker-стакан",
        );
        diagnose(this.symbol, "depth.fallback", {
          state: "activated",
          generation,
          mode: "partial",
        });
        try { this.socket?.close(); } catch {}
        this.socket = null;
        this.reconnectTimer = setTimeout(() => this.connectDepth(generation), 0);
      }
    
      resync(text) {
        if (this.mode !== "deep") return;
        const now = Date.now();
        if (now - this.lastResyncAt < 350) return;
        this.lastResyncAt = now;
        this.resyncCount += 1;
        const preserveLastFrame = this.syncing || this.depthReady || (this.bids.size > 0 && this.asks.size > 0);
        this.syncing = preserveLastFrame;
        this.resetBook(text === "Переполнение буфера" ? "buffer-overflow" : "sequence-gap");
        this.setStatus(
          preserveLastFrame ? "stale" : "loading",
          preserveLastFrame ? `СИНХРОНИЗАЦИЯ · ${text}` : text,
        );
        clearTimeout(this.snapshotTimer);
        const delay = Math.min(2_000, 250 + this.resyncCount * 75);
        this.snapshotTimer = setTimeout(() => this.loadSnapshot(this.generation), delay);
      }
    
      replacePartial(target, previousKeys, rows) {
        const nextKeys = new Set();
        for (const row of rows ?? []) {
          const price = Number(row?.[0]);
          const quantity = Number(row?.[1]);
          if (!Number.isFinite(price) || !Number.isFinite(quantity)) continue;
          nextKeys.add(price);
          if (quantity > 0) target.set(price, quantity);
          else target.delete(price);
        }
        for (const price of previousKeys) if (!nextKeys.has(price)) target.delete(price);
        return nextKeys;
      }
    
      connectDepth(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.reconnectTimer);
        clearTimeout(this.firstDepthTimer);
        const transports = depthTransports(this.symbol, this.mode);
        const transport = transports[this.transportIndex % transports.length];
        const transportIndex = this.transportIndex % transports.length;
        diagnose(this.symbol, "depth.ws.create", {
          state: "started",
          generation,
          mode: this.mode,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "depth.ws.create", {
            state: "failed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.transportIndex += 1;
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
          return;
        }
        this.socket = socket;
        this.firstDepthTimer = setTimeout(() => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.first-message", {
              state: "timeout",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              durationMs: 8_000,
            });
            try { socket.close(); } catch {}
          }
        }, 8_000);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.socket) return;
          diagnose(this.symbol, "depth.ws.open", {
            state: "succeeded",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
          });
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: [transport.stream],
              id: Date.now() % 2_147_483_647,
            }));
          }
          if (this.syncing) this.setStatus("stale", "СИНХРОНИЗАЦИЯ · последний кадр");
          else this.setStatus("loading", this.mode === "deep" ? "Синхронизация Worker" : "Подключаю резерв Worker");
          if (this.mode === "deep") this.loadSnapshot(generation);
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.socket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            this.lastMessageAt = Date.now();
    
            const bids = update?.b ?? update?.bids;
            const asks = update?.a ?? update?.asks;
            if (!Array.isArray(bids) || !Array.isArray(asks)) return;
            const receivedFirstDepth = this.lastDepthAt === 0;
            const receivedAt = Date.now();
            update.__receivedAt = receivedAt;
            this.lastDepthAt = receivedAt;
            const sourceEventTime = Number(update?.E);
            if (Number.isFinite(sourceEventTime)) {
              this.lastDepthEventTime = sourceEventTime;
              this.lastDepthSourceLagMs = calibratedSourceLag(sourceEventTime, receivedAt);
            }
            if (receivedFirstDepth) {
              diagnose(this.symbol, "depth.ws.first-message", {
                state: "received",
                generation,
                mode: this.mode,
                transport: this.transportIndex,
                transportName: transport.name,
              });
            }
            clearTimeout(this.firstDepthTimer);
            this.transportIndex = 0;
            this.depthReconnectAttempt = 0;
    
            if (this.mode === "partial") {
              this.partialBidKeys = this.replacePartial(this.bids, this.partialBidKeys, bids);
              this.partialAskKeys = this.replacePartial(this.asks, this.partialAskKeys, asks);
              this.lastUpdateId = Number(update.u ?? update.lastUpdateId) || this.lastUpdateId;
              this.depthReady = true;
              this.syncing = false;
              this.cachedSorted = null;
              this.publishLiveStatus();
              this.markDirty(true);
              return;
            }
    
            if (!Number.isFinite(Number(update?.U)) || !Number.isFinite(Number(update?.u))) return;
            if (!this.depthReady) {
              this.bufferDepth(update);
              if (!this.pendingSnapshot && !this.snapshotLoading) this.loadSnapshot(generation);
              this.installSnapshot();
              return;
            }
            this.applyDepth(update);
          } finally {
            this.recordFlow("depth", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.socket) return;
          clearTimeout(this.firstDepthTimer);
          this.socket = null;
          this.transportIndex += 1;
          diagnose(this.symbol, "depth.ws.close", {
            state: "closed",
            generation,
            mode: this.mode,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          const preserveLastFrame = this.depthReady || this.syncing;
          this.syncing = preserveLastFrame;
          this.resetBook("reconnect");
          this.setStatus(
            preserveLastFrame ? "stale" : "offline",
            preserveLastFrame ? "СИНХРОНИЗАЦИЯ · последний кадр" : "RECONNECT · WORKER",
          );
          const delay = reconnectDelay(this.depthReconnectAttempt++);
          diagnose(this.symbol, "depth.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.transportIndex % transports.length,
          });
          this.reconnectTimer = setTimeout(() => this.connectDepth(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.socket) {
            diagnose(this.symbol, "depth.ws.error", {
              state: "failed",
              generation,
              mode: this.mode,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      connectTrades(generation) {
        if (generation !== this.generation || this.subscribers <= 0) return;
        clearTimeout(this.tradeReconnectTimer);
        clearTimeout(this.tradeFirstMessageTimer);
        this.tradeFirstMessageTimer = 0;
    
        const streams = tradeStreams(this.symbol);
        const transports = tradeTransports(streams);
        const transport = transports[this.tradeTransportIndex % transports.length];
        const transportIndex = this.tradeTransportIndex % transports.length;
        diagnose(this.symbol, "tape.ws.create", {
          state: "started",
          generation,
          transport: transportIndex,
          transportName: transport.name,
          url: transport.url,
        });
        let socket;
        try { socket = new WebSocket(transport.url); }
        catch (error) {
          diagnose(this.symbol, "tape.ws.create", {
            state: "failed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            errorKind: self.InPulsOrderBookNetwork.errorKind(error),
            message: String(error?.message ?? error ?? "WebSocket creation failed").slice(0, 180),
          });
          this.tradeTransportIndex += 1;
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
          return;
        }
        this.tradeSocket = socket;
        this.tradeTransportName = transport.name;
        let receivedTrade = false;
        this.tradeFirstMessageTimer = setTimeout(() => {
          if (generation !== this.generation || socket !== this.tradeSocket || receivedTrade) return;
          diagnose(this.symbol, "tape.ws.first-message", {
            state: "timeout",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            durationMs: TRADE_FIRST_MESSAGE_TIMEOUT_MS,
          });
          try { socket.close(); } catch {}
        }, TRADE_FIRST_MESSAGE_TIMEOUT_MS);
    
        socket.addEventListener("open", () => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          diagnose(this.symbol, "tape.ws.open", {
            state: "succeeded",
            generation,
            transport: transportIndex,
            transportName: transport.name,
          });
          this.tapeGuard.connect();
          if (transport.subscribe) {
            socket.send(JSON.stringify({
              method: "SUBSCRIBE",
              params: transport.streams,
              id: Date.now() % 2_147_483_647,
            }));
          }
        });
    
        socket.addEventListener("message", (message) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          const payload = parsePayload(message.data);
          if (!payload) return;
          const processStartedAt = performance.now();
          try {
            const update = payload.data;
            const eventType = String(update?.e ?? "").toLowerCase();
            const payloadStream = payload.stream.toLowerCase();
            const rawEvent = eventType === "trade" || payloadStream.endsWith("@trade");
            const aggregateEvent = eventType === "aggtrade" || payloadStream.endsWith("@aggtrade");
            if (!rawEvent && !aggregateEvent) return;
    
            const receivedAt = Date.now();
            const source = rawEvent ? "raw" : "agg";
            const trade = normalizeTrade(update, source, receivedAt);
            if (!trade) return;
            const firstTradeMessage = !receivedTrade;
            receivedTrade = true;
            clearTimeout(this.tradeFirstMessageTimer);
            this.tradeFirstMessageTimer = 0;
            this.lastTradeAt = receivedAt;
            this.lastTradeEventTime = trade.eventTime;
            this.lastTradeSourceLagMs = calibratedSourceLag(trade.eventTime, receivedAt);
            // RX in the header describes the stable production Tape feed. The parallel
            // experimental @trade channel must not double-weight or zero the rolling median.
            if (aggregateEvent) this.tradeLatency.record(trade.rxLatencyMs, receivedAt);
            this.tradeTransportIndex = 0;
            this.tradeReconnectAttempt = 0;
            this.tradeLive = true;
            this.tradeConnected = true;
            if (firstTradeMessage) {
              diagnose(this.symbol, "tape.ws.first-message", {
                state: "received",
                generation,
                transport: transportIndex,
                transportName: transport.name,
                source,
              });
            }
    
            const decision = this.tapeGuard.ingest(trade, this.lastTradeAt);
            let changed = false;
    
            // Preserve the visual RAW contract: it continues to receive only
            // Binance @aggTrade events, exactly as before this feature.
            if (aggregateEvent && this.insertTrade(trade, true)) {
              const matchedDensities = this.densityLifecycle.ingestTrades([trade]);
              if (matchedDensities.length) this.markDirty();
              this.queueTape(trade);
              changed = true;
            }
    
            // The guarded production path follows the same documented @aggTrade
            // stream and keeps continuity/dedup protection without mixing sources.
            if (decision.emit && this.insertAggregationTrade(trade, true)) {
              this.queueAggregationTape(trade);
              changed = true;
            }
    
            this.publishLiveStatus();
            if (!changed && rawEvent && decision.reason !== "raw-warmup") {
              diagnose(this.symbol, "tape.aggregation-source", {
                state: "skipped",
                source,
                reason: decision.reason,
                mode: decision.mode,
              });
            }
          } finally {
            this.recordFlow("trade", processStartedAt);
          }
        });
    
        socket.addEventListener("close", (event) => {
          if (generation !== this.generation || socket !== this.tradeSocket) return;
          clearTimeout(this.tradeFirstMessageTimer);
          this.tradeFirstMessageTimer = 0;
          this.tradeSocket = null;
          this.tradeConnected = false;
          this.tapeGuard.disconnect("socket-close");
          this.tradeTransportIndex += 1;
          diagnose(this.symbol, "tape.ws.close", {
            state: "closed",
            generation,
            transport: transportIndex,
            transportName: transport.name,
            code: Number(event?.code) || null,
            clean: Boolean(event?.wasClean),
            reason: String(event?.reason ?? "").slice(0, 120),
          });
          if (this.tradeLive && this.depthReady) this.publishLiveStatus("reconnect");
          const delay = reconnectDelay(this.tradeReconnectAttempt++);
          diagnose(this.symbol, "tape.ws.retry", {
            state: "scheduled",
            generation,
            delayMs: delay,
            nextTransport: this.tradeTransportIndex % transports.length,
          });
          this.tradeReconnectTimer = setTimeout(() => this.connectTrades(generation), delay);
        });
    
        socket.addEventListener("error", (event) => {
          if (generation === this.generation && socket === this.tradeSocket) {
            diagnose(this.symbol, "tape.ws.error", {
              state: "failed",
              generation,
              transport: transportIndex,
              transportName: transport.name,
              message: String(event?.message ?? "WebSocket error").slice(0, 180),
            });
            try { socket.close(); } catch {}
          }
        });
      }
    
      insertTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.tradeIds.has(key)) return false;
        this.tradeIds.add(key);
        const evicted = newestFirst
          ? this.trades.prepend(trade)
          : this.trades.append(trade);
        if (evicted === trade) {
          this.tradeIds.delete(key);
          return false;
        }
        if (evicted) this.tradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      insertAggregationTrade(trade, newestFirst = true) {
        if (!trade) return false;
        const key = this.tradeKey(trade);
        if (this.aggregationTradeIds.has(key)) return false;
        this.aggregationTradeIds.add(key);
        const evicted = newestFirst
          ? this.aggregationTrades.prepend(trade)
          : this.aggregationTrades.append(trade);
        if (evicted === trade) {
          this.aggregationTradeIds.delete(key);
          return false;
        }
        if (evicted) this.aggregationTradeIds.delete(this.tradeKey(evicted));
        return true;
      }
    
      scheduleTapeFlush() {
        if (!this.tapeTimer) {
          this.tapeTimer = setTimeout(() => this.flushTapeBatch(), TAPE_FLUSH_MS);
        }
      }
    
      queueTape(trade) {
        if (!trade || !tabVisible) return;
        this.tapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      queueAggregationTape(trade) {
        if (!trade || !tabVisible) return;
        this.aggregationTapeBatch.push(trade);
        this.scheduleTapeFlush();
      }
    
      flushTapeBatch() {
        this.tapeTimer = 0;
        if (!tabVisible) {
          this.tapeBatch.clear();
          this.aggregationTapeBatch.clear();
          return;
        }
        const latest = this.tapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const aggregationLatest = this.aggregationTapeBatch.takeLatest(MAX_TAPE_BATCH_PER_POST);
        const trades = latest.items;
        const aggregationTrades = aggregationLatest.items;
        this.tapeDroppedInWindow += latest.dropped + aggregationLatest.dropped;
        if (trades.length || aggregationTrades.length) {
          const sourceEventTimeMs = [...trades, ...aggregationTrades].reduce(
            (latestTime, trade) => Math.max(latestTime, Number(trade?.eventTime) || 0),
            0,
          );
          const guard = this.tapeGuard.snapshot();
          post(
            "tape",
            this.symbol,
            {
              replace: false,
              live: true,
              liveOnly: true,
              trades,
              aggregationTrades,
              aggregationSource: guard.mode,
              aggregationHealth: guard,
              backpressure: {
                dropped: latest.dropped + aggregationLatest.dropped,
                pending: this.tapeBatch.length + this.aggregationTapeBatch.length,
              },
            },
            observabilityEnabled ? performance.now() : null,
            { sourceEventTimeMs, sourceKind: "live-trade-dual" },
          );
        }
      }
    
    }
    
    function scheduleEmit() {
      if (!tabVisible || emitTimer) return;
      emitTimer = setTimeout(() => {
        emitTimer = 0;
        if (!tabVisible) return;
    
        const active = [...feeds.values()].filter((feed) => feed.subscribers > 0);
        if (!active.length) return;
    
        // Не отправляем несколько тяжёлых книг в UI одним залпом.
        // При 3+ стаканах один тик обслуживает один символ по кругу.
        const budget = active.length <= 2 ? active.length : 1;
        const now = Date.now();
        for (let index = 0; index < budget; index += 1) {
          const feed = active[emitCursor % active.length];
          emitCursor = (emitCursor + 1) % active.length;
          feed.emit(now);
        }
    
        if (active.some((feed) => feed.dirty || feed.forceEmit)) scheduleEmit();
      }, 25);
    }
    
    function scheduleWatchdog() {
      clearTimeout(watchdogTimer);
      watchdogTimer = setTimeout(() => {
        watchdogTimer = 0;
        const now = Date.now();
        if (tabVisible) {
          for (const feed of feeds.values()) {
            if (feed.subscribers <= 0) continue;
            feed.reportFlow(now);
            feed.ensureHealthy(now);
          }
        }
        self.postMessage({
          type: "heartbeat",
          time: now,
          visible: tabVisible,
          activeFeeds: [...feeds.values()].filter((feed) => feed.subscribers > 0).length,
          totalFeeds: feeds.size,
        });
        scheduleWatchdog();
      }, WORKER_HEARTBEAT_MS);
    }
    
    function getFeed(symbol) {
      let feed = feeds.get(symbol);
      if (!feed) {
        feed = new SymbolFeed(symbol);
        feeds.set(symbol, feed);
      }
      return feed;
    }
    
    function cancelBackgroundPause() {
      clearTimeout(backgroundPauseTimer);
      backgroundPauseTimer = 0;
    }
    
    function scheduleBackgroundPause(epoch) {
      cancelBackgroundPause();
      backgroundPauseTimer = setTimeout(() => {
        backgroundPauseTimer = 0;
        if (tabVisible || epoch !== visibilityEpoch) return;
        for (const feed of feeds.values()) feed.pauseForBackground();
      }, BACKGROUND_GRACE_MS);
    }
    
    self.addEventListener("message", (event) => {
      const message = event.data;
      if (!message || typeof message !== "object") return;
    
      if (message.type === "observability") {
        observabilityEnabled = Boolean(message.enabled);
        return;
      }
    
      if (message.type === "visibility") {
        const nextVisible = Boolean(message.visible);
        if (nextVisible === tabVisible) return;
    
        tabVisible = nextVisible;
        visibilityEpoch += 1;
        const epoch = visibilityEpoch;
    
        if (!tabVisible) {
          clearTimeout(emitTimer);
          emitTimer = 0;
          scheduleBackgroundPause(epoch);
          return;
        }
    
        cancelBackgroundPause();
        syncServerClock(true).catch(() => {});
    
        const resumePrioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        prioritySymbols = resumePrioritySymbols;
        const priorityRank = new Map(
          resumePrioritySymbols.map((symbol, index) => [symbol, index]),
        );
        const active = [...feeds.values()]
          .filter((feed) => feed.subscribers > 0)
          .sort((left, right) => (
            (priorityRank.get(left.symbol) ?? Number.MAX_SAFE_INTEGER)
            - (priorityRank.get(right.symbol) ?? Number.MAX_SAFE_INTEGER)
          ));
        active.forEach((feed, index) => feed.resume(index * RESUME_STAGGER_MS, epoch));
        return;
      }
    
      if (message.type === "priority") {
        prioritySymbols = Array.isArray(message.prioritySymbols)
          ? message.prioritySymbols.map((symbol) => String(symbol).toUpperCase())
          : [];
        return;
      }
    
      const symbol = String(message.symbol ?? "").toUpperCase();
      if (!symbol.endsWith("USDT")) return;
      if (message.type === "subscribe") {
        getFeed(symbol).addSubscriber();
        return;
      }
      if (message.type === "unsubscribe") {
        feeds.get(symbol)?.removeSubscriber();
        return;
      }
      if (message.type === "refresh") {
        feeds.get(symbol)?.refresh();
      }
    });
    
    syncServerClock(true).catch(() => {});
    scheduleWatchdog();
    post("ready", "");
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/InPuls/InPuls/test/orderbook-backpressure.test.js:35:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: backpressure never coalesces or drops depth sequence events
ok 237 - backpressure never coalesces or drops depth sequence events
  ---
  duration_ms: 0.303889
  type: 'test'
  ...
# Subtest: Worker reports flow rates, processing cost, queue pressure and source freshness
ok 238 - Worker reports flow rates, processing cost, queue pressure and source freshness
  ---
  duration_ms: 1.427688
  type: 'test'
  ...
# Subtest: Worker publishes one full-book size scale without expanding UI depth
ok 239 - Worker publishes one full-book size scale without expanding UI depth
  ---
  duration_ms: 0.26086
  type: 'test'
  ...
# Subtest: multi-book depth keeps distant liquidity through compact projection
ok 240 - multi-book depth keeps distant liquidity through compact projection
  ---
  duration_ms: 0.222943
  type: 'test'
  ...
# Subtest: size alone remains an unrated candidate without claiming importance
ok 241 - size alone remains an unrated candidate without claiming importance
  ---
  duration_ms: 4.144521
  type: 'test'
  ...
# Subtest: only the correct aggressor touching the exact density price is correlated
ok 242 - only the correct aggressor touching the exact density price is correlated
  ---
  duration_ms: 1.296031
  type: 'test'
  ...
# Subtest: matching executions plus a remaining level classify a partial fill
ok 243 - matching executions plus a remaining level classify a partial fill
  ---
  duration_ms: 1.230576
  type: 'test'
  ...
# Subtest: a removed level is consumed only when matching executions cover it
ok 244 - a removed level is consumed only when matching executions cover it
  ---
  duration_ms: 0.82151
  type: 'test'
  ...
# Subtest: a trade arriving after the depth decrease still correlates inside the window
ok 245 - a trade arriving after the depth decrease still correlates inside the window
  ---
  duration_ms: 1.363904
  type: 'test'
  ...
# Subtest: a removal without matching executions settles as pulled
ok 246 - a removal without matching executions settles as pulled
  ---
  duration_ms: 0.352732
  type: 'test'
  ...
# Subtest: a nearby similar replacement is marked moved only as a low-confidence heuristic
ok 247 - a nearby similar replacement is marked moved only as a low-confidence heuristic
  ---
  duration_ms: 0.695881
  type: 'test'
  ...
# Subtest: gap reset and partial depth clear correlation evidence
ok 248 - gap reset and partial depth clear correlation evidence
  ---
  duration_ms: 0.54683
  type: 'test'
  ...
# Subtest: Worker and Legacy fallback ingest the same accepted trades into density correlation
ok 249 - Worker and Legacy fallback ingest the same accepted trades into density correlation
  ---
  duration_ms: 17.584267
  type: 'test'
  ...
# Subtest: density reference uses a robust per-side quote distribution
ok 250 - density reference uses a robust per-side quote distribution
  ---
  duration_ms: 3.510821
  type: 'test'
  ...
# Subtest: snapshot seeds observed densities without fabricating depth events
ok 251 - snapshot seeds observed densities without fabricating depth events
  ---
  duration_ms: 3.806199
  type: 'test'
  ...
# Subtest: a density transitions through strengthening, weakening and replenished
ok 252 - a density transitions through strengthening, weakening and replenished
  ---
  duration_ms: 2.02216
  type: 'test'
  ...
# Subtest: a zero quantity closes a density as removed without claiming execution
ok 253 - a zero quantity closes a density as removed without claiming execution
  ---
  duration_ms: 1.059537
  type: 'test'
  ...
# Subtest: hysteresis closes a weakened level only after the fade grace window
ok 254 - hysteresis closes a weakened level only after the fade grace window
  ---
  duration_ms: 2.531928
  type: 'test'
  ...
# Subtest: gap reset and partial fallback cannot leak lifecycle state
ok 255 - gap reset and partial fallback cannot leak lifecycle state
  ---
  duration_ms: 0.77957
  type: 'test'
  ...
# Subtest: Legacy symbol changes reset cross-symbol lifetime counters
ok 256 - Legacy symbol changes reset cross-symbol lifetime counters
  ---
  duration_ms: 0.383746
  type: 'test'
  ...
# Subtest: active and serialized density collections stay bounded
ok 257 - active and serialized density collections stay bounded
  ---
  duration_ms: 0.644202
  type: 'test'
  ...
# Subtest: Worker and Legacy fallback share the density lifecycle contract
ok 258 - Worker and Legacy fallback share the density lifecycle contract
  ---
  duration_ms: 15.226727
  type: 'test'
  ...
# Subtest: deep projection preserves exact near levels and total quote
ok 259 - deep projection preserves exact near levels and total quote
  ---
  duration_ms: 8.32473
  type: 'test'
  ...
# Subtest: deep projection stays ordered on both sides
ok 260 - deep projection stays ordered on both sides
  ---
  duration_ms: 2.263115
  type: 'test'
  ...
# Subtest: small books pass through without compaction
ok 261 - small books pass through without compaction
  ---
  duration_ms: 0.329736
  type: 'test'
  ...
# Subtest: depth changes map to the four normalized lifecycle event types
ok 262 - depth changes map to the four normalized lifecycle event types
  ---
  duration_ms: 0.793164
  type: 'test'
  ...
# Subtest: snapshot seeds the book as a baseline without fabricating appeared events
ok 263 - snapshot seeds the book as a baseline without fabricating appeared events
  ---
  duration_ms: 2.045206
  type: 'test'
  ...
# Subtest: a continuous diff emits normalized bid and ask events before updating the book
ok 264 - a continuous diff emits normalized bid and ask events before updating the book
  ---
  duration_ms: 0.561666
  type: 'test'
  ...
# Subtest: missing zero levels, unchanged quantities and invalid rows stay silent
ok 265 - missing zero levels, unchanged quantities and invalid rows stay silent
  ---
  duration_ms: 0.236283
  type: 'test'
  ...
# Subtest: journal marks bridge events as recovered and live events as live
ok 266 - journal marks bridge events as recovered and live events as live
  ---
  duration_ms: 0.45189
  type: 'test'
  ...
# Subtest: gap reset opens a new epoch and cannot leak old lifecycle events
ok 267 - gap reset opens a new epoch and cannot leak old lifecycle events
  ---
  duration_ms: 0.24476
  type: 'test'
  ...
# Subtest: journal history is bounded and keeps the newest normalized events
ok 268 - journal history is bounded and keeps the newest normalized events
  ---
  duration_ms: 0.328229
  type: 'test'
  ...
# Subtest: Worker and Legacy fallback share the event-first runtime contract
ok 269 - Worker and Legacy fallback share the event-first runtime contract
  ---
  duration_ms: 9.10035
  type: 'test'
  ...
# Subtest: a fast primary REST host prevents backup requests from starting
ok 270 - a fast primary REST host prevents backup requests from starting
  ---
  duration_ms: 3.395274
  type: 'test'
  ...
# Subtest: a failed primary starts a delayed backup and preserves failure diagnostics
ok 271 - a failed primary starts a delayed backup and preserves failure diagnostics
  ---
  duration_ms: 6.238653
  type: 'test'
  ...
# Subtest: the winning REST response aborts requests that are still in flight
ok 272 - the winning REST response aborts requests that are still in flight
  ---
  duration_ms: 1.312168
  type: 'test'
  ...
# Subtest: network errors are classified without claiming that CORS is proven
ok 273 - network errors are classified without claiming that CORS is proven
  ---
  duration_ms: 0.983244
  type: 'test'
  ...
# Subtest: all orderbook panels share one latest-only frame scheduler
ok 274 - all orderbook panels share one latest-only frame scheduler
  ---
  duration_ms: 1.659643
  type: 'test'
  ...
# Subtest: canvas runtime disables the hidden legacy DOM footprint
ok 275 - canvas runtime disables the hidden legacy DOM footprint
  ---
  duration_ms: 0.350906
  type: 'test'
  ...
# Subtest: Tape draw work yields across animation frames without dropping dirty cards
ok 276 - Tape draw work yields across animation frames without dropping dirty cards
  ---
  duration_ms: 0.475728
  type: 'test'
  ...
# Subtest: Tape ingestion shares each frame between symbols and both trade channels
ok 277 - Tape ingestion shares each frame between symbols and both trade channels
  ---
  duration_ms: 0.859194
  type: 'test'
  ...
# Subtest: footprint renders only dirty cards under the same frame budget
ok 278 - footprint renders only dirty cards under the same frame budget
  ---
  duration_ms: 0.448058
  type: 'test'
  ...
# Subtest: render optimization leaves strict depth sequencing untouched
ok 279 - render optimization leaves strict depth sequencing untouched
  ---
  duration_ms: 0.366534
  type: 'test'
  ...
# Subtest: depth updates add, replace and remove price levels
ok 280 - depth updates add, replace and remove price levels
  ---
  duration_ms: 2.001401
  type: 'test'
  ...
# Subtest: order book view keeps best bids and asks first
ok 281 - order book view keeps best bids and asks first
  ---
  duration_ms: 0.475309
  type: 'test'
  ...
# Subtest: partial depth stream becomes a ready top-20 book without REST snapshot
ok 282 - partial depth stream becomes a ready top-20 book without REST snapshot
  ---
  duration_ms: 0.181011
  type: 'test'
  ...
# Subtest: depth range is aggregated into stable price bands
ok 283 - depth range is aggregated into stable price bands
  ---
  duration_ms: 0.340867
  type: 'test'
  ...
# Subtest: aggregate trade identifies the aggressive market side
ok 284 - aggregate trade identifies the aggressive market side
  ---
  duration_ms: 0.247866
  type: 'test'
  ...
# Subtest: price ladder keeps the market row visible and fills both sides
ok 285 - price ladder keeps the market row visible and fills both sides
  ---
  duration_ms: 1.07819
  type: 'test'
  ...
# Subtest: orderbook wheel uses the trader-facing step direction
ok 286 - orderbook wheel uses the trader-facing step direction
  ---
  duration_ms: 0.190878
  type: 'test'
  ...
# Subtest: reducing the step converges a free viewport toward market and snaps at x1
ok 287 - reducing the step converges a free viewport toward market and snaps at x1
  ---
  duration_ms: 0.148854
  type: 'test'
  ...
# Subtest: price ladder cannot be scrolled below zero
ok 288 - price ladder cannot be scrolled below zero
  ---
  duration_ms: 0.373866
  type: 'test'
  ...
# Subtest: size bars use the complete projected book instead of the visible ladder
ok 289 - size bars use the complete projected book instead of the visible ladder
  ---
  duration_ms: 0.7447
  type: 'test'
  ...
# Subtest: automatic anomaly reference uses the complete book rather than the visible ladder
ok 290 - automatic anomaly reference uses the complete book rather than the visible ladder
  ---
  duration_ms: 0.96778
  type: 'test'
  ...
# Subtest: highlight modes preserve displayed size while AUTO inspects the largest real order
ok 291 - highlight modes preserve displayed size while AUTO inspects the largest real order
  ---
  duration_ms: 0.233055
  type: 'test'
  ...
# Subtest: AUTO keeps one threshold per coin for the whole session
ok 292 - AUTO keeps one threshold per coin for the whole session
  ---
  duration_ms: 0.235741
  type: 'test'
  ...
# Subtest: hover distance uses color for direction and absolute compact text
ok 293 - hover distance uses color for direction and absolute compact text
  ---
  duration_ms: 0.105286
  type: 'test'
  ...
# Subtest: Tape maps every execution to its exact interpolated price coordinate
ok 294 - Tape maps every execution to its exact interpolated price coordinate
  ---
  duration_ms: 0.369065
  type: 'test'
  ...
# Subtest: percent depth gives BTC and alt books the same visible range
ok 295 - percent depth gives BTC and alt books the same visible range
  ---
  duration_ms: 337.287984
  type: 'test'
  ...
# Subtest: trade clusters respect the minimum quote filter
ok 296 - trade clusters respect the minimum quote filter
  ---
  duration_ms: 0.381815
  type: 'test'
  ...
# Subtest: RAW trade path preserves executions and applies the minimum filter per event
ok 297 - RAW trade path preserves executions and applies the minimum filter per event
  ---
  duration_ms: 0.303767
  type: 'test'
  ...
# Subtest: impulse adaptation preserves the user-selected price step
ok 298 - impulse adaptation preserves the user-selected price step
  ---
  duration_ms: 0.098783
  type: 'test'
  ...
# Subtest: AUTO depth fits the working area and scale recovery preserves the user value
ok 299 - AUTO depth fits the working area and scale recovery preserves the user value
  ---
  duration_ms: 0.38901
  type: 'test'
  ...
# Subtest: time window and footprint preserve exact time-price cells
ok 300 - time window and footprint preserve exact time-price cells
  ---
  duration_ms: 0.330186
  type: 'test'
  ...
# Subtest: order book separates public depth and market trade streams
ok 301 - order book separates public depth and market trade streams
  ---
  duration_ms: 3.535536
  type: 'test'
  ...
# Subtest: owner dashboard creates a safe same-origin InPuls orderbook link
ok 302 - owner dashboard creates a safe same-origin InPuls orderbook link
  ---
  duration_ms: 1.360711
  type: 'test'
  ...
# Subtest: owner navigation rejects invalid symbols and unknown actions
ok 303 - owner navigation rejects invalid symbols and unknown actions
  ---
  duration_ms: 1.098882
  type: 'test'
  ...
# Subtest: owner dashboard stays unlinked, local-only and exposes no destructive history controls
ok 304 - owner dashboard stays unlinked, local-only and exposes no destructive history controls
  ---
  duration_ms: 10.148416
  type: 'test'
  ...
# Subtest: catalog has only the agreed pattern groups plus explicit market events
ok 305 - catalog has only the agreed pattern groups plus explicit market events
  ---
  duration_ms: 0.885027
  type: 'test'
  ...
# Subtest: minute structure stores one-minute extrema for cascade research
ok 306 - minute structure stores one-minute extrema for cascade research
  ---
  duration_ms: 0.811667
  type: 'test'
  ...
# Subtest: 59th-minute evidence uses UTC exchange time
ok 307 - 59th-minute evidence uses UTC exchange time
  ---
  duration_ms: 0.240084
  type: 'test'
  ...
# Subtest: density geometry preserves layout and observed move chains without scoring intent
ok 308 - density geometry preserves layout and observed move chains without scoring intent
  ---
  duration_ms: 1.296764
  type: 'test'
  ...
# Subtest: PWA reset recognizes only the InPuls worker at its exact scope
ok 309 - PWA reset recognizes only the InPuls worker at its exact scope
  ---
  duration_ms: 0.838964
  type: 'test'
  ...
# Subtest: PWA reset leaves unrelated registrations and caches untouched
ok 310 - PWA reset leaves unrelated registrations and caches untouched
  ---
  duration_ms: 0.953752
  type: 'test'
  ...
# Subtest: latest scheduler coalesces repeated data for one panel
ok 311 - latest scheduler coalesces repeated data for one panel
  ---
  duration_ms: 1.734519
  type: 'test'
  ...
# Subtest: frame budget yields remaining panels to the next frame
ok 312 - frame budget yields remaining panels to the next frame
  ---
  duration_ms: 0.356375
  type: 'test'
  ...
# Subtest: urgent interaction moves an already queued panel to the front
ok 313 - urgent interaction moves an already queued panel to the front
  ---
  duration_ms: 0.250532
  type: 'test'
  ...
# Subtest: removed panels never render from a stale queue entry
ok 314 - removed panels never render from a stale queue entry
  ---
  duration_ms: 1.157829
  type: 'test'
  ...
# Subtest: static server rejects malformed URLs without terminating
ok 315 - static server rejects malformed URLs without terminating
  ---
  duration_ms: 28.888816
  type: 'test'
  ...
# Subtest: static server blocks private files and missing scripts
ok 316 - static server blocks private files and missing scripts
  ---
  duration_ms: 7.582237
  type: 'test'
  ...
# Subtest: static server sends browser security headers and limits methods
ok 317 - static server sends browser security headers and limits methods
  ---
  duration_ms: 9.289384
  type: 'test'
  ...
# Subtest: owner Signal Lab is served as a protected same-origin page
ok 318 - owner Signal Lab is served as a protected same-origin page
  ---
  duration_ms: 9.713431
  type: 'test'
  ...
# Subtest: owner Signal Lab explains that collection runs in the main InPuls tab
ok 319 - owner Signal Lab explains that collection runs in the main InPuls tab
  ---
  duration_ms: 6.217682
  type: 'test'
  ...
# Subtest: service worker reports whether a same-origin main collector client is open
ok 320 - service worker reports whether a same-origin main collector client is open
  ---
  duration_ms: 1.395947
  type: 'test'
  ...
# Subtest: enriches current events with canonical pattern and facts
ok 321 - enriches current events with canonical pattern and facts
  ---
  duration_ms: 2.371921
  type: 'test'
  ...
# Subtest: groups repeated events into one market episode
ok 322 - groups repeated events into one market episode
  ---
  duration_ms: 0.916911
  type: 'test'
  ...
# Subtest: stored V2 review overrides event review
ok 323 - stored V2 review overrides event review
  ---
  duration_ms: 0.536431
  type: 'test'
  ...
# Subtest: team aliases resolve to canonical pattern ids
ok 324 - team aliases resolve to canonical pattern ids
  ---
  duration_ms: 0.954528
  type: 'test'
  ...
# Subtest: catalog keeps ambiguous team terminology as an explicit limitation
ok 325 - catalog keeps ambiguous team terminology as an explicit limitation
  ---
  duration_ms: 0.280274
  type: 'test'
  ...
# Subtest: candidate validation requires factual identity and direction
ok 326 - candidate validation requires factual identity and direction
  ---
  duration_ms: 0.228289
  type: 'test'
  ...
# Subtest: cascade explanation contains observable facts only
ok 327 - cascade explanation contains observable facts only
  ---
  duration_ms: 0.801761
  type: 'test'
  ...
# Subtest: liquidity explanation does not accuse spoofing
ok 328 - liquidity explanation does not accuse spoofing
  ---
  duration_ms: 76.287448
  type: 'test'
  ...
# Subtest: legacy reviews migrate without losing the manual comment
ok 329 - legacy reviews migrate without losing the manual comment
  ---
  duration_ms: 0.653593
  type: 'test'
  ...
# Subtest: valid review preserves trader markup
ok 330 - valid review preserves trader markup
  ---
  duration_ms: 0.496878
  type: 'test'
  ...
# Subtest: duplicate review requires an episode id
ok 331 - duplicate review requires an episode id
  ---
  duration_ms: 0.253641
  type: 'test'
  ...
# Subtest: same market move is grouped into one episode
ok 332 - same market move is grouped into one episode
  ---
  duration_ms: 1.195955
  type: 'test'
  ...
# Subtest: distant or late event starts a new episode
ok 333 - distant or late event starts a new episode
  ---
  duration_ms: 0.677316
  type: 'test'
  ...
# Subtest: episode state machine blocks impossible resurrection
ok 334 - episode state machine blocks impossible resurrection
  ---
  duration_ms: 0.837002
  type: 'test'
  ...
# Subtest: terminal episode rejects duplicate events
ok 335 - terminal episode rejects duplicate events
  ---
  duration_ms: 1.340135
  type: 'test'
  ...
# Subtest: training sample preserves formula version, outcome and data limitations
ok 336 - training sample preserves formula version, outcome and data limitations
  ---
  duration_ms: 0.720789
  type: 'test'
  ...
# Subtest: readiness gate never treats a few pretty examples as sufficient
ok 337 - readiness gate never treats a few pretty examples as sufficient
  ---
  duration_ms: 0.449162
  type: 'test'
  ...
# Subtest: beta eligibility needs positives, counterexamples, live coverage and one formula version
ok 338 - beta eligibility needs positives, counterexamples, live coverage and one formula version
  ---
  duration_ms: 0.285597
  type: 'test'
  ...
# Subtest: Signal Lab publishes the intended 1d, 3d, 7d and 30d windows
ok 339 - Signal Lab publishes the intended 1d, 3d, 7d and 30d windows
  ---
  duration_ms: 2.239349
  type: 'test'
  ...
# Subtest: event review rows keep unique IDs, real chart evidence and manual verdicts
ok 340 - event review rows keep unique IDs, real chart evidence and manual verdicts
  ---
  duration_ms: 1.897689
  type: 'test'
  ...
# Subtest: impulses stay in raw memory but are excluded from the current Pattern Lab
ok 341 - impulses stay in raw memory but are excluded from the current Pattern Lab
  ---
  duration_ms: 0.582914
  type: 'test'
  ...
# Subtest: primary statistics use only complete live observations and expose every exclusion
ok 342 - primary statistics use only complete live observations and expose every exclusion
  ---
  duration_ms: 1.075625
  type: 'test'
  ...
# Subtest: formula versions and settings never share one statistical group
ok 343 - formula versions and settings never share one statistical group
  ---
  duration_ms: 8.505061
  type: 'test'
  ...
# Subtest: market groups combine symbols while symbol groups keep coin histories separate
ok 344 - market groups combine symbols while symbol groups keep coin histories separate
  ---
  duration_ms: 0.430426
  type: 'test'
  ...
# Subtest: lookback windows use signal time and do not leak older events into 1d
ok 345 - lookback windows use signal time and do not leak older events into 1d
  ---
  duration_ms: 0.565537
  type: 'test'
  ...
# Subtest: large density size is not promoted into importance or outcome statistics
ok 346 - large density size is not promoted into importance or outcome statistics
  ---
  duration_ms: 0.431526
  type: 'test'
  ...
# Subtest: duplicate entity IDs are de-duplicated before aggregation
ok 347 - duplicate entity IDs are de-duplicated before aggregation
  ---
  duration_ms: 0.490965
  type: 'test'
  ...
# Subtest: Signal Lab degrades safely when IndexedDB is unavailable
ok 348 - Signal Lab degrades safely when IndexedDB is unavailable
  ---
  duration_ms: 1.141229
  type: 'test'
  ...
# Subtest: browser runtime wires persistent Signal Lab without adding destructive controls
ok 349 - browser runtime wires persistent Signal Lab without adding destructive controls
  ---
  duration_ms: 9.761269
  type: 'test'
  ...
# Subtest: Signal Lab review UI exposes comments and enriched JSON/CSV export
ok 350 - Signal Lab review UI exposes comments and enriched JSON/CSV export
  ---
  duration_ms: 1.529334
  type: 'test'
  ...
# Subtest: browser entry points keep user version v23 and identify the current release build
ok 351 - browser entry points keep user version v23 and identify the current release build
  ---
  duration_ms: 9.418219
  type: 'test'
  ...
# Subtest: v23 DOM exposes fixed price steps, live Tape and footprint controls
ok 352 - v23 DOM exposes fixed price steps, live Tape and footprint controls
  ---
  duration_ms: 3.528927
  type: 'test'
  ...
# Subtest: INPLAY exposes and applies the NATR 5 filter
ok 353 - INPLAY exposes and applies the NATR 5 filter
  ---
  duration_ms: 2.197455
  type: 'test'
  ...
# Subtest: market symbols are validated before subscriptions and detail rendering avoids dynamic HTML
ok 354 - market symbols are validated before subscriptions and detail rendering avoids dynamic HTML
  ---
  duration_ms: 2.511156
  type: 'test'
  ...
# Subtest: browser entry points carry a restrictive CSP and reset scripts stay external
ok 355 - browser entry points carry a restrictive CSP and reset scripts stay external
  ---
  duration_ms: 3.899443
  type: 'test'
  ...
# Subtest: Service Worker installs atomically and validates cached response types
ok 356 - Service Worker installs atomically and validates cached response types
  ---
  duration_ms: 0.872241
  type: 'test'
  ...
# Subtest: chart pointer work is coalesced through animation frames and first-anchor snapping is available
ok 357 - chart pointer work is coalesced through animation frames and first-anchor snapping is available
  ---
  duration_ms: 1.350628
  type: 'test'
  ...
# Subtest: small panels keep compact menus and smaller resize corners
ok 358 - small panels keep compact menus and smaller resize corners
  ---
  duration_ms: 3.392404
  type: 'test'
  ...
# Subtest: brightness control keeps fixed accents and morphs from sun to moon
ok 359 - brightness control keeps fixed accents and morphs from sun to moon
  ---
  duration_ms: 3.735065
  type: 'test'
  ...
# Subtest: orderbook price text is separated from the size boundary
ok 360 - orderbook price text is separated from the size boundary
  ---
  duration_ms: 2.845313
  type: 'test'
  ...
# Subtest: Event Radar Beta is fully absent while core discovery blocks remain
ok 361 - Event Radar Beta is fully absent while core discovery blocks remain
  ---
  duration_ms: 2.393366
  type: 'test'
  ...
1..361
# tests 361
# suites 0
# pass 357
# fail 4
# cancelled 0
# skipped 0
# todo 0
# duration_ms 1665.612737
