name: Apply hierarchical history rays V3.5

on:
  push:
    branches:
      - agent/signal-lab-structural-extremes-stage1
    paths:
      - scripts/apply-hierarchical-history-rays-v35.py
      - .github/workflows/apply-hierarchical-history-rays-v35.yml

permissions:
  contents: write

jobs:
  apply-test-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out draft branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: agent/signal-lab-structural-extremes-stage1
          fetch-depth: 0
          persist-credentials: true

      - name: Use Node.js 22
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"

      - name: Apply V3.5 patch
        run: python3 scripts/apply-hierarchical-history-rays-v35.py

      - name: Remove temporary patch files
        run: |
          rm -f scripts/apply-hierarchical-history-rays-v35.py
          rm -f .github/workflows/apply-hierarchical-history-rays-v35.yml

      - name: Validate syntax and diff
        run: |
          node --check signal-lab-v7-multi-timeframe-levels.js
          node --check signal-lab-v7-multi-timeframe-review-runtime.js
          node --check chart.js
          git diff --check

      - name: Run structural and multi-TF tests
        run: |
          node --test \
            test/signal-lab-v7-structural-extremes.test.js \
            test/signal-lab-v7-structural-extremes-v3.test.js \
            test/signal-lab-v7-multi-timeframe-levels.test.js

      - name: Run complete regression suite
        run: npm test

      - name: Build standalone calibration page
        run: |
          python3 scripts/build-structural-extremes-review-bundle.py
          test -s structural-extremes-review-standalone.html
          grep -q "Разметка для обучения" structural-extremes-review-standalone.html

      - name: Commit verified V3.5 changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Extend hierarchical history and pin active level rays"
          git push origin HEAD:agent/signal-lab-structural-extremes-stage1
