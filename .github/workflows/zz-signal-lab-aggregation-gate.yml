name: Apply Signal Lab timeframe aggregation gate

on:
  push:
    branches:
      - fix/signal-lab-extreme-collection-performance

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out aggregation branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: fix/signal-lab-extreme-collection-performance
          fetch-depth: 0

      - name: Apply aggregation gate
        run: python3 scripts/apply-signal-lab-aggregation-gate.py

      - name: Run complete test suite
        run: npm test

      - name: Commit verified aggregation gate
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Gate extreme timeframe aggregation by closed minute"
          git push origin HEAD:fix/signal-lab-extreme-collection-performance
