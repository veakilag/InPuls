name: Apply Tape threshold restore

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.head_ref == 'agent/restore-tape-threshold-v2'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply Tape threshold restore
        run: python .github/scripts/apply-restore-tape-threshold-v2.py
      - name: Syntax checks
        run: |
          node --check orderbook.js
          node --check app.js
          node --check test-sealed-agg-round-levels-v1.mjs
          node --check test-tape-threshold-agg-visual-v1.mjs
      - name: Full test suite
        run: npm test
      - name: Commit verified implementation
        shell: bash
        run: |
          rm -f .github/scripts/apply-restore-tape-threshold-v2.py
          rm -f .github/workflows/apply-restore-tape-threshold-v2.yml
          git config user.name "InPuls automation"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "fix: restore Tape marker threshold"
          git push origin HEAD:${{ github.head_ref }}
